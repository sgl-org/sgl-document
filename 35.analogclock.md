# SGL AnalogClock 控件应用指南

## 1. 简介

`AnalogClock`（模拟时钟）是 SGL 中用于**显示指针时钟**的控件，带表盘刻度与时分秒三根指针。  
在 **SGL（Simple Graphics Library）** 中，`AnalogClock` 支持：
- 时间设置（时/分/秒）
- 表盘背景、边框、刻度、中心轴（hub）
- 时针/分针/秒针的独立颜色与宽度
- 刻度数字字体、整体透明度

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 典型用法：SGL 定时器（`sgl_timer`）每秒调用 `set_time()` 更新
> - 适合桌面时钟、仪表盘、状态页等场景

---

## 2. 创建 AnalogClock

使用 `sgl_analogclock_create()` 创建模拟时钟控件：

```c
sgl_obj_t* sgl_analogclock_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* clock = sgl_analogclock_create(page);
sgl_obj_set_pos(clock, 50, 20);
sgl_obj_set_size(clock, 160, 160);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_analogclock_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置时间 | `void sgl_analogclock_set_time(sgl_obj_t *obj, uint8_t hour, uint8_t min, uint8_t sec)` | 时/分/秒（0~23/0~59/0~59） |
| 设置字体 | `void sgl_analogclock_set_font(sgl_obj_t *obj, const sgl_font_t *font)` | 刻度数字字体 |
| 设置背景颜色 | `void sgl_analogclock_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 表盘底色 |
| 设置边框颜色 | `void sgl_analogclock_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 表盘边框色 |
| 设置边框宽度 | `void sgl_analogclock_set_border_width(sgl_obj_t *obj, uint8_t width)` | 表盘边框宽度 |
| 设置刻度颜色 | `void sgl_analogclock_set_scale_color(sgl_obj_t *obj, sgl_color_t color)` | 刻度线颜色 |
| 设置刻度宽度 | `void sgl_analogclock_set_scale_width(sgl_obj_t *obj, uint8_t width)` | 刻度线宽度 |
| 设置时针颜色 | `void sgl_analogclock_set_hour_ptr_color(sgl_obj_t *obj, sgl_color_t color)` | 时针颜色 |
| 设置时针宽度 | `void sgl_analogclock_set_hour_ptr_width(sgl_obj_t *obj, uint8_t width)` | 时针宽度 |
| 设置分针颜色 | `void sgl_analogclock_set_min_ptr_color(sgl_obj_t *obj, sgl_color_t color)` | 分针颜色 |
| 设置分针宽度 | `void sgl_analogclock_set_min_ptr_width(sgl_obj_t *obj, uint8_t width)` | 分针宽度 |
| 设置秒针颜色 | `void sgl_analogclock_set_sec_ptr_color(sgl_obj_t *obj, sgl_color_t color)` | 秒针颜色 |
| 设置秒针宽度 | `void sgl_analogclock_set_sec_ptr_width(sgl_obj_t *obj, uint8_t width)` | 秒针宽度 |
| 设置中心轴颜色 | `void sgl_analogclock_set_hub_color(sgl_obj_t *obj, sgl_color_t color)` | 中心轴颜色 |
| 设置中心轴半径 | `void sgl_analogclock_set_hub_radius(sgl_obj_t *obj, uint8_t radius)` | 中心轴半径 |
| 设置文字颜色 | `void sgl_analogclock_set_text_color(sgl_obj_t *obj, sgl_color_t color)` | 刻度数字颜色 |
| 设置透明度 | `void sgl_analogclock_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |

---

## 4. 应用示例

```c
static sgl_obj_t *g_zhong = NULL;
static uint8_t g_hour = 10, g_min = 30, g_sec = 45;

/* 定时器回调：每秒加一秒 */
static void clock_timer_cb(const sgl_timer_t *timer, void *user_data)
{
    (void)timer; (void)user_data;
    if (g_zhong == NULL) return;

    g_sec++;
    if (g_sec >= 60) {
        g_sec = 0;
        g_min++;
        if (g_min >= 60) {
            g_min = 0;
            g_hour++;
            if (g_hour >= 24) g_hour = 0;
        }
    }
    sgl_analogclock_set_time(g_zhong, g_hour, g_min, g_sec);
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建模拟时钟
    g_zhong = sgl_analogclock_create(page);
    sgl_obj_set_pos(g_zhong, (SGL_SCREEN_WIDTH - 160) / 2, 20);
    sgl_obj_set_size(g_zhong, 160, 160);

    /* 初始时间与字体 */
    sgl_analogclock_set_time(g_zhong, g_hour, g_min, g_sec);
    sgl_analogclock_set_font(g_zhong, &consolas14);

    /* 外观颜色 */
    sgl_analogclock_set_bg_color(g_zhong, SGL_COLOR_DARK_GRAY);
    sgl_analogclock_set_border_color(g_zhong, SGL_COLOR_CYAN);
    sgl_analogclock_set_border_width(g_zhong, 5);
    sgl_analogclock_set_scale_color(g_zhong, SGL_COLOR_WHITE);
    sgl_analogclock_set_scale_width(g_zhong, 2);
    sgl_analogclock_set_hour_ptr_color(g_zhong, SGL_COLOR_CYAN);
    sgl_analogclock_set_hour_ptr_width(g_zhong, 3);
    sgl_analogclock_set_min_ptr_color(g_zhong, SGL_COLOR_LIME);
    sgl_analogclock_set_min_ptr_width(g_zhong, 2);
    sgl_analogclock_set_sec_ptr_color(g_zhong, SGL_COLOR_RED);
    sgl_analogclock_set_sec_ptr_width(g_zhong, 1);
    sgl_analogclock_set_hub_color(g_zhong, SGL_COLOR_GRAY);
    sgl_analogclock_set_hub_radius(g_zhong, 5);
    sgl_analogclock_set_text_color(g_zhong, SGL_COLOR_WHITE);
    sgl_analogclock_set_alpha(g_zhong, 220);

    /* 定时器：每秒走一秒 */
    sgl_timer_t *t = sgl_timer_create();
    if (t) {
        sgl_timer_setup(t, clock_timer_cb, 1000, -1, NULL);
    }
```

![AnalogClock 示例](imag/0/SGL_logo.png)

> 💡 定时器需周期调用 `sgl_timer_handler()` 才会触发（通常与 `sgl_tick_inc()` 同放 SysTick）。

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [AnalogClock 完整示例 sgl_analogclock_demo.c](SGL_DEMO/sgl_analogclock_demo.c) — 实时走秒的模拟时钟

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_ANALOGCLOCK` 开关宏后编译运行。

---

## 6. 总结

- ✅ **三针独立样式**：时针/分针/秒针颜色与宽度分别设置。
- ✅ **定时器驱动**：每秒 `set_time()` 走秒。
- ✅ **表盘样式齐全**：刻度、中心轴、边框、数字均可配置。

---
> 📘 **相关头文件路径**  
> - AnalogClock API：`sgl/source/widgets/analogclock/sgl_analogclock.h`  
> - 定时器组件：`sgl/source/components/timer/sgl_timer.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
