# SGL Progress 控件应用指南

## 1. 简介

`Progress`（进度条）是 SGL 中用于**显示任务完成进度**的控件。  
在 **SGL（Simple Graphics Library）** 中，`Progress` 支持：
- 进度值设置（0~100）与读取
- 填充色 / 轨道色及各自透明度
- 圆角、边框、背景图片
- **填充间隙**（fill_gap）、**填充圆角**（fill_radius）、**填充宽度**（fill_width）等细节样式

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 进度值范围 0~100，配合动画系统可实现加载/进度动效
> - 适合下载进度、加载界面、音量/电量显示等场景

---

## 2. 创建 Progress

使用 `sgl_progress_create()` 创建进度条控件：

```c
sgl_obj_t* sgl_progress_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* pr = sgl_progress_create(page);
sgl_obj_set_pos(pr, 20, 20);
sgl_obj_set_size(pr, 280, 30);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_progress_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置进度值 | `void sgl_progress_set_value(sgl_obj_t *obj, uint8_t value)` | 进度值（0~100） |
| 获取进度值 | `uint8_t sgl_progress_get_value(sgl_obj_t *obj)` | 返回当前进度值 |
| 设置填充颜色 | `void sgl_progress_set_fill_color(sgl_obj_t *obj, sgl_color_t color)` | 进度填充色 |
| 设置轨道颜色 | `void sgl_progress_set_track_color(sgl_obj_t *obj, sgl_color_t color)` | 背景轨道色 |
| 设置填充透明度 | `void sgl_progress_set_fill_alpha(sgl_obj_t *obj, uint8_t alpha)` | 填充部分透明度 |
| 设置轨道透明度 | `void sgl_progress_set_track_alpha(sgl_obj_t *obj, uint8_t alpha)` | 轨道部分透明度 |
| 设置圆角 | `void sgl_progress_set_radius(sgl_obj_t *obj, uint8_t radius)` | 整体圆角半径 |
| 设置边框宽度 | `void sgl_progress_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |
| 设置边框颜色 | `void sgl_progress_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置背景图片 | `void sgl_progress_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 背景图指针 |
| 设置填充间隙 | `void sgl_progress_set_fill_gap(sgl_obj_t *obj, uint8_t gap)` | 填充块之间的间隙（像素） |
| 设置填充圆角 | `void sgl_progress_set_fill_radius(sgl_obj_t *obj, uint8_t radius)` | 填充部分的圆角半径 |
| 设置填充宽度 | `void sgl_progress_set_fill_width(sgl_obj_t *obj, uint8_t width)` | 填充块宽度（配合 gap 实现分段进度条） |

> 💡 设置 `fill_gap` + `fill_width` 后进度条呈现"分段/格栅"效果。

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 基础进度条
    sgl_obj_t* pr = sgl_progress_create(page);
    sgl_obj_set_pos(pr, 20, 20);
    sgl_obj_set_size(pr, 280, 30);
    sgl_progress_set_value(pr, 65);                          // 65%
    sgl_progress_set_fill_color(pr, SGL_COLOR_LIME);
    sgl_progress_set_track_color(pr, SGL_COLOR_DARK_GRAY);
    sgl_progress_set_fill_alpha(pr, 200);
    sgl_progress_set_track_alpha(pr, 100);
    sgl_progress_set_radius(pr, 6);
    sgl_progress_set_border_width(pr, 1);
    sgl_progress_set_border_color(pr, SGL_COLOR_CYAN);
    sgl_progress_set_fill_gap(pr, 2);                        // 分段效果
    sgl_progress_set_fill_radius(pr, 4);
    sgl_progress_set_fill_width(pr, 8);
```

动画进度条（配合动画系统 + 百分比标签）：

```c
static sgl_obj_t *g_progress = NULL;
static sgl_obj_t *g_label = NULL;

static void anim_path_cb(sgl_anim_t *anim, int32_t value)
{
    sgl_progress_set_value(g_progress, (uint8_t)value);      // 更新进度条
    sgl_label_set_text_fmt_dynamic(g_label, "%d%%", (int)value); // 更新百分比
}

    g_progress = sgl_progress_create(page);
    sgl_obj_set_pos(g_progress, 20, 80);
    sgl_obj_set_size(g_progress, 280, 40);
    sgl_progress_set_value(g_progress, 0);
    sgl_progress_set_fill_color(g_progress, SGL_COLOR_RED);
    sgl_progress_set_track_color(g_progress, SGL_COLOR_DARK_GRAY);
    sgl_progress_set_radius(g_progress, 6);

    g_label = sgl_label_create(page);                        // 叠加百分比标签
    sgl_obj_set_pos(g_label, 20, 80);
    sgl_obj_set_size(g_label, 280, 40);
    sgl_label_set_font(g_label, &consolas14);
    sgl_label_set_text(g_label, "0%");
    sgl_label_set_text_color(g_label, SGL_COLOR_WHITE);
    sgl_label_set_text_align(g_label, SGL_ALIGN_CENTER);

    sgl_anim_t *anim = sgl_anim_create();
    sgl_anim_set_data(anim, NULL);
    sgl_anim_set_act_duration(anim, 1000);
    sgl_anim_set_start_value(anim, 0);
    sgl_anim_set_end_value(anim, 100);
    sgl_anim_set_path(anim, anim_path_cb, SGL_ANIM_PATH_LINEAR);
    sgl_anim_start(anim, SGL_ANIM_REPEAT_LOOP);              // 0→100 循环
```

![Progress 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Progress 完整示例 sgl_progress_demo.c](SGL_DEMO/sgl_progress_demo.c) — 全接口进度条 + 动画循环进度条

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_PROGRESS` 开关宏后编译运行。

---

## 6. 总结

- ✅ **进度值 0~100**：`set_value()` 设置，`get_value()` 读取。
- ✅ **分段效果**：`set_fill_gap()` + `set_fill_width()` 组合。
- ✅ **动效靠动画**：动画回调中更新 `set_value()` 即可实现进度动画。

---
> 📘 **相关头文件路径**  
> - Progress API：`sgl/source/widgets/progress/sgl_progress.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
