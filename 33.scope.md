# SGL Scope 控件应用指南

## 1. 简介

`Scope`（示波器）是 SGL 中用于**实时显示波形数据**的图表控件，支持多通道、滚动追加数据与自动量程。  
在 **SGL（Simple Graphics Library）** 中，`Scope` 支持：
- 1~4 通道波形显示
- 通道数据缓冲区绑定 + **实时追加数据**（`append_data`）
- 每通道独立波形颜色
- 自动量程 / 固定范围、Y 轴标签
- 网格（实线/虚线）、边框、背景

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 数据为**滚动追加**模式：`sgl_scope_append_data()` 每次追加一个采样点，超出显示宽度后滚动
> - 典型用法：SGL 定时器（`sgl_timer`）周期性追加 ADC/传感器采样值
> - 适合示波器、心电图、传感器波形监控等场景

---

## 2. 创建 Scope

使用 `sgl_scope_create()` 创建示波器控件：

```c
sgl_obj_t* sgl_scope_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* scope = sgl_scope_create(parent);
sgl_obj_set_pos(scope, 20, 20);
sgl_obj_set_size(scope, 280, 200);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_scope_create()` 返回的对象指针。

### 3.1 通道与数据

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置通道数 | `void sgl_scope_set_channel_count(sgl_obj_t* obj, uint8_t channel_count)` | 通道数（1~4） |
| 绑定通道数据缓冲 | `void sgl_scope_set_channel_data_buffer(sgl_obj_t* obj, uint8_t channel, int16_t *data_buffer, uint32_t data_len)` | 绑定通道数据数组（持久存储） |
| 追加数据点 | `void sgl_scope_append_data(sgl_obj_t* obj, uint8_t channel, int16_t value)` | 追加一个采样点（滚动） |
| 设置通道波形颜色 | `void sgl_scope_set_channel_waveform_color(sgl_obj_t* obj, uint8_t channel, sgl_color_t color)` | 通道波形颜色 |
| 获取通道数据 | `int16_t sgl_scope_get_channel_data(sgl_obj_t* obj, uint8_t channel, uint32_t index)` | 读取指定索引采样值 |
| 设置最大显示点数 | `void sgl_scope_set_max_display_points(sgl_obj_t* obj, uint8_t max_points)` | 同时显示的采样点数量 |

### 3.2 量程与坐标

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置固定范围 | `void sgl_scope_set_range(sgl_obj_t* obj, uint16_t min_value, uint16_t max_value)` | 固定 Y 范围（同时关闭自动量程） |
| 使能自动量程 | `void sgl_scope_enable_auto_scale(sgl_obj_t* obj, bool enable)` | 自动适应数据范围 |
| 显示 Y 轴标签 | `void sgl_scope_show_y_labels(sgl_obj_t* obj, bool show)` | 显示 Y 轴数值标签 |
| 设置 Y 标签字体 | `void sgl_scope_set_y_label_font(sgl_obj_t* obj, const sgl_font_t *font)` | Y 轴标签字体 |
| 设置 Y 标签颜色 | `void sgl_scope_set_y_label_color(sgl_obj_t* obj, sgl_color_t color)` | Y 轴标签颜色 |

### 3.3 外观

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置背景颜色 | `void sgl_scope_set_bg_color(sgl_obj_t* obj, sgl_color_t color)` | 背景色 |
| 设置网格颜色 | `void sgl_scope_set_grid_color(sgl_obj_t* obj, sgl_color_t color)` | 网格颜色 |
| 设置网格样式 | `void sgl_scope_set_grid_line(sgl_obj_t* obj, uint8_t grid)` | 0=实线，非0=虚线 |
| 设置线宽 | `void sgl_scope_set_line_width(sgl_obj_t* obj, uint8_t width)` | 波形线宽 |
| 设置边框颜色 | `void sgl_scope_set_border_color(sgl_obj_t* obj, sgl_color_t color)` | 边框颜色 |
| 设置边框宽度 | `void sgl_scope_set_border_width(sgl_obj_t* obj, uint8_t width)` | 边框宽度 |
| 设置透明度 | `void sgl_scope_set_alpha(sgl_obj_t* obj, uint8_t alpha)` | 0~255 |

---

## 4. 应用示例

```c
#define SCOPE_DATA_LEN   128
#define SCOPE_MAX_POINTS 96

static sgl_obj_t *scope_obj = NULL;
static int16_t sine_buffer[SCOPE_DATA_LEN];      /* 采样缓冲区（持久存储） */
static sgl_timer_t *scope_timer = NULL;

/* 定时器回调：每 30ms 追加一个正弦采样点 */
static void scope_timer_callback(const sgl_timer_t *timer, void *user_data)
{
    (void)timer; (void)user_data;
    static uint16_t phase = 0;
    phase += 5;
    int16_t value = (int16_t)(50 + 45 * sinf(phase * 3.14159265f / 180.0f));
    sgl_scope_append_data(scope_obj, 0, value);   /* 追加数据 */
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建示波器
    scope_obj = sgl_scope_create(page);
    sgl_obj_set_pos(scope_obj, 20, 20);
    sgl_obj_set_size(scope_obj, 280, 200);

    /* 通道配置 */
    sgl_scope_set_channel_count(scope_obj, 1);
    sgl_scope_set_channel_data_buffer(scope_obj, 0, sine_buffer, SCOPE_DATA_LEN);
    sgl_scope_set_channel_waveform_color(scope_obj, 0, sgl_rgb(0x00, 0xFF, 0x80));
    sgl_scope_enable_auto_scale(scope_obj, true);          /* 自动量程 */
    sgl_scope_set_max_display_points(scope_obj, SCOPE_MAX_POINTS);

    /* 外观 */
    sgl_scope_set_bg_color(scope_obj, SGL_COLOR_BLACK);
    sgl_scope_set_grid_color(scope_obj, SGL_COLOR_DARK_GRAY);
    sgl_scope_set_border_color(scope_obj, SGL_COLOR_CYAN);
    sgl_scope_set_border_width(scope_obj, 1);

    /* 创建定时器：30ms 周期追加数据 */
    scope_timer = sgl_timer_create();
    if (scope_timer) {
        sgl_timer_setup(scope_timer, scope_timer_callback, 30, -1, NULL);
    }
```

![Scope 示例](imag/0/SGL_logo.png)

> 💡 定时器需周期调用 `sgl_timer_handler()` 才会触发（通常与 `sgl_tick_inc()` 同放 SysTick）。

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Scope 完整示例 sgl_scope_demo.c](SGL_DEMO/sgl_scope_demo.c) — 正弦波实时滚动显示

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_SCOPE` 开关宏后编译运行。

---

## 6. 总结

- ✅ **滚动追加**：`append_data()` 实时追加采样点。
- ✅ **多通道**：1~4 通道独立数据与颜色。
- ✅ **自动量程**：`enable_auto_scale(true)` 自动适应数据。
- ✅ **定时器驱动**：配合 `sgl_timer` 周期性刷新。

---
> 📘 **相关头文件路径**  
> - Scope API：`sgl/source/widgets/scope/sgl_scope.h`  
> - 定时器组件：`sgl/source/components/timer/sgl_timer.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
