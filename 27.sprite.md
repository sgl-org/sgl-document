# SGL Sprite 控件应用指南

## 1. 简介

`Sprite`（精灵）是 SGL 中**专为游戏设计的高速图片控件**。  
在 **SGL（Simple Graphics Library）** 中，`Sprite` 支持：
- 设置精灵图片（`sgl_pixmap_t`）
- 透明度设置

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - **只支持 ARGB4444 格式**的图片——需要透明效果的图片推荐使用此控件
> - 渲染速度快，适合游戏精灵、特效、透明贴图等场景

---

## 2. 创建 Sprite

使用 `sgl_sprite_create()` 创建精灵控件：

```c
sgl_obj_t* sgl_sprite_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* sprite = sgl_sprite_create(page);
sgl_obj_set_pos(sprite, 60, 60);
sgl_obj_set_size(sprite, 64, 64);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_sprite_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置精灵图片 | `void sgl_sprite_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | `pixmap`：精灵图（**需 ARGB4444 格式**） |
| 设置透明度 | `void sgl_sprite_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | `alpha`：0~255 |

> ⚠️ **格式要求**：`Sprite` 仅支持 `SGL_PIXMAP_FMT_ARGB4444` 格式的图片，该格式自带 Alpha 通道，可实现透明效果。

---

## 4. 应用示例

```c
    extern const sgl_pixmap_t pic1_pixmap;   // 图片资源（需 ARGB4444 格式）

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 精灵 1
    sgl_obj_t* jingling1 = sgl_sprite_create(page);
    sgl_obj_set_pos(jingling1, 60, 60);
    sgl_obj_set_size(jingling1, 64, 64);
    sgl_sprite_set_pixmap(jingling1, &pic1_pixmap);
    sgl_sprite_set_alpha(jingling1, 220);

    // 精灵 2（不同位置，更低透明度）
    sgl_obj_t* jingling2 = sgl_sprite_create(page);
    sgl_obj_set_pos(jingling2, 180, 60);
    sgl_obj_set_size(jingling2, 64, 64);
    sgl_sprite_set_pixmap(jingling2, &pic1_pixmap);
    sgl_sprite_set_alpha(jingling2, 180);
```

![Sprite 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Sprite 完整示例 sgl_sprite_demo.c](SGL_DEMO/sgl_sprite_demo.c) — 多个精灵控件演示

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_SPRITE` 开关宏后编译运行。

---

## 6. 总结

- ✅ **游戏专用**：渲染速度快，适合游戏精灵。
- ✅ **透明首选**：只支持 ARGB4444，自带 Alpha 通道。
- ✅ **图片格式注意**：需将图片转换/取模为 ARGB4444 格式。

---
> 📘 **相关头文件路径**  
> - Sprite API：`sgl/source/widgets/sprite/sgl_sprite.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
