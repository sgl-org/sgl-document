# SGL LED 控件应用指南

## 1. 简介

`LED`（指示灯）是 SGL 中用于**显示开关状态**的指示灯控件，点亮/熄灭两种状态使用不同颜色。  
在 **SGL（Simple Graphics Library）** 中，`LED` 支持：
- 点亮/熄灭状态设置与读取
- 点亮颜色、熄灭颜色、背景色
- 圆角半径、透明度
- 便捷接口 `sgl_led_on()` / `sgl_led_off()`

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 典型用法：按钮切换 LED、开关同步 LED 状态
> - 适合状态指示、电源指示灯、告警灯等场景

---

## 2. 创建 LED

使用 `sgl_led_create()` 创建指示灯控件：

```c
sgl_obj_t* sgl_led_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* led = sgl_led_create(page);
sgl_obj_set_pos(led, 60, 40);
sgl_obj_set_size(led, 60, 60);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_led_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置圆角半径 | `void sgl_led_set_radius(sgl_obj_t *obj, uint8_t radius)` | 圆角半径（设为边长一半即为圆形） |
| 设置点亮颜色 | `void sgl_led_set_on_color(sgl_obj_t *obj, sgl_color_t color)` | 点亮时颜色 |
| 设置熄灭颜色 | `void sgl_led_set_off_color(sgl_obj_t *obj, sgl_color_t color)` | 熄灭时颜色 |
| 设置背景颜色 | `void sgl_led_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 背景色 |
| 设置透明度 | `void sgl_led_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置状态 | `void sgl_led_set_status(sgl_obj_t *obj, bool status)` | `true` 点亮，`false` 熄灭 |
| 获取状态 | `bool sgl_led_get_status(sgl_obj_t *obj)` | 返回当前状态 |
| 点亮 | `void sgl_led_on(sgl_obj_t *obj)` | 快捷点亮 |
| 熄灭 | `void sgl_led_off(sgl_obj_t *obj)` | 快捷熄灭 |

---

## 4. 应用示例

```c
/* 按钮回调 — 切换 LED1 亮灭 */
static void btn_toggle_led(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_CLICKED) {
        sgl_obj_t *led = (sgl_obj_t *)e->event_data;
        if (sgl_led_get_status(led)) {
            sgl_led_off(led);
        } else {
            sgl_led_on(led);
        }
    }
}

/* 开关回调 — 同步 LED2 状态 */
static void switch_toggle_led(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_CLICKED) {
        sgl_obj_t *led = (sgl_obj_t *)e->event_data;
        sgl_obj_t *sw = (sgl_obj_t *)e->obj;
        if (sgl_switch_get_status(sw)) {
            sgl_led_on(led);
        } else {
            sgl_led_off(led);
        }
    }
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // LED1 — 青色，默认点亮，由按钮控制
    sgl_obj_t *led1 = sgl_led_create(page);
    sgl_obj_set_pos(led1, 60, 40);
    sgl_obj_set_size(led1, 60, 60);
    sgl_led_set_radius(led1, 30);                       // 圆形
    sgl_led_set_on_color(led1, SGL_COLOR_CYAN);
    sgl_led_set_off_color(led1, SGL_COLOR_DARK_GRAY);
    sgl_led_set_bg_color(led1, SGL_COLOR_DARK_GRAY);
    sgl_led_set_alpha(led1, 220);
    sgl_led_on(led1);

    sgl_obj_t *btn = sgl_button_create(page);
    sgl_obj_set_pos(btn, 20, 120);
    sgl_obj_set_size(btn, 120, 32);
    sgl_button_set_text(btn, "Toggle LED1");
    sgl_button_set_font(btn, &consolas14);
    sgl_button_set_color(btn, SGL_COLOR_NAVY);
    sgl_button_set_text_color(btn, SGL_COLOR_WHITE);
    sgl_button_set_radius(btn, 4);
    sgl_obj_set_event_cb(btn, btn_toggle_led, led1);    // 传 LED 作为 user_data

    // LED2 — 金色，默认熄灭，由开关控制
    sgl_obj_t *led2 = sgl_led_create(page);
    sgl_obj_set_pos(led2, 200, 40);
    sgl_obj_set_size(led2, 60, 60);
    sgl_led_set_radius(led2, 30);
    sgl_led_set_on_color(led2, SGL_COLOR_GOLD);
    sgl_led_set_off_color(led2, SGL_COLOR_DARK_GRAY);
    sgl_led_set_bg_color(led2, SGL_COLOR_DARK_GRAY);
    sgl_led_off(led2);

    sgl_obj_t *sw = sgl_switch_create(page);
    sgl_obj_set_pos(sw, 190, 120);
    sgl_obj_set_size(sw, 80, 32);
    sgl_switch_set_color(sw, SGL_COLOR_GOLD);
    sgl_switch_set_bg_color(sw, SGL_COLOR_DARK_GRAY);
    sgl_switch_set_knob_color(sw, SGL_COLOR_WHITE);
    sgl_switch_set_radius(sw, 16);
    sgl_switch_set_status(sw, false);
    sgl_obj_set_event_cb(sw, switch_toggle_led, led2);  // 传 LED 作为 user_data
```

![LED 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [LED 完整示例 sgl_led_demo.c](SGL_DEMO/sgl_led_demo.c) — 按钮控制 LED1 + 开关控制 LED2

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_LED` 开关宏后编译运行。

---

## 6. 总结

- ✅ **状态切换**：`set_status()` / `on()` / `off()` 三种方式。
- ✅ **事件数据传参**：`sgl_obj_set_event_cb(obj, cb, led)` 将 LED 作为 user_data 传入回调。
- ✅ **圆形指示灯**：`set_radius(边长/2)`。

---
> 📘 **相关头文件路径**  
> - LED API：`sgl/source/widgets/led/sgl_led.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
