# SGL Line 控件应用指南

## 1. 简介

`Line`（线段）是 SGL 中最基础的**图形绘制控件**，用于在屏幕上绘制一条直线。  
在 **SGL（Simple Graphics Library）** 中，`Line` 支持：
- 设置起点/终点坐标（绝对定位）
- 实线 / **虚线**两种样式（可自定义虚线段长度与间隔）
- 颜色、透明度、线宽设置

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 坐标通过 `sgl_line_set_pos()` 设置，不依赖 `sgl_obj_set_size()`
> - 适合绘制分隔线、坐标轴、指示线、射线动画等场景

---

## 2. 创建 Line

使用 `sgl_line_create()` 创建线段控件：

```c
sgl_obj_t* sgl_line_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* line = sgl_line_create(page);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_line_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置起点终点 | `void sgl_line_set_pos(sgl_obj_t *obj, int16_t x1, int16_t y1, int16_t x2, int16_t y2)` | 线段两个端点的坐标 |
| 设置颜色 | `void sgl_line_set_color(sgl_obj_t *obj, sgl_color_t color)` | `color`：线段颜色（如 `SGL_COLOR_CYAN`） |
| 设置透明度 | `void sgl_line_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | `alpha`：0~255，255 为完全不透明 |
| 设置线宽 | `void sgl_line_set_width(sgl_obj_t *obj, uint8_t width)` | `width`：线段宽度（像素） |
| 设置虚线模式 | `void sgl_line_set_dashed(sgl_obj_t *obj, uint8_t dashed)` | `dashed`：非 0 为虚线，0 为实线 |
| 设置虚线样式 | `void sgl_line_set_dash_pattern(sgl_obj_t *obj, uint16_t dash_len, uint16_t gap_len)` | 虚线段长度与间隔（像素） |

> 💡 线段坐标是**相对父对象**的局部坐标。

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 实线
    sgl_obj_t* xian1 = sgl_line_create(page);
    sgl_line_set_pos(xian1, 20, 50, 300, 50);      // 起点(20,50) → 终点(300,50)
    sgl_line_set_color(xian1, SGL_COLOR_CYAN);
    sgl_line_set_alpha(xian1, 220);
    sgl_line_set_width(xian1, 4);

    // 虚线
    sgl_obj_t* xian2 = sgl_line_create(page);
    sgl_line_set_pos(xian2, 20, 110, 300, 110);
    sgl_line_set_color(xian2, SGL_COLOR_GOLD);
    sgl_line_set_width(xian2, 3);
    sgl_line_set_dashed(xian2, 1);                 // 启用虚线
    sgl_line_set_dash_pattern(xian2, 8, 4);        // 实线段 8px，间隔 4px
```

![Line 示例](imag/0/SGL_logo.png)

> 💡 结合动画系统可实现"射线"效果：创建多条从中心向外辐射的线段，用 `sgl_anim_t` 驱动 `sgl_line_set_alpha()` 依次淡入淡出（详见 SGL_DEMO）。

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Line 完整示例 sgl_line_demo.c](SGL_DEMO/sgl_line_demo.c) — 实线/虚线对比 + 18 条射线循环动画

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_LINE` 开关宏后编译运行。

---

## 6. 总结

- ✅ **先设坐标再设样式**：调用 `sgl_line_set_pos()` 定义线段位置。
- ✅ **虚线适用分割线**：用 `set_dashed(1)` + `set_dash_pattern()` 实现分隔/坐标轴虚线。
- ✅ **配合动画做动效**：线段透明度动画可轻松实现射线、扫描等效果。

---
> 📘 **相关头文件路径**  
> - Line API：`sgl/source/widgets/line/sgl_line.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
