# SGL Spectrum 控件应用指南

## 1. 简介

`Spectrum`（频谱）是 SGL 中用于**显示频谱柱状图**的控件，类似音乐播放器的均衡器（EQ）。  
在 **SGL（Simple Graphics Library）** 中，`Spectrum` 支持：
- 柱数量、每根柱的独立数值
- **四种显示模式**：柱（BAR）、块（BLOCK）、柱+帽（BAR_HAT）、块+帽（BLOCK_HAT）
- 柱颜色、帽（峰值）颜色、帽高度
- 透明度

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 典型用法：SGL 定时器周期性更新每根柱的数值，模拟音频频谱跳动
> - 适合音乐播放器、音频可视化、数据柱状展示等场景

---

## 2. 创建 Spectrum

使用 `sgl_spectrum_create()` 创建频谱控件：

```c
sgl_obj_t* sgl_spectrum_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* spec = sgl_spectrum_create(page);
sgl_obj_set_pos(spec, 20, 20);
sgl_obj_set_size(spec, 280, 160);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_spectrum_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置柱数量 | `void sgl_spectrum_set_bar_number(sgl_obj_t *obj, uint16_t number)` | 柱的个数 |
| 设置柱数值 | `void sgl_spectrum_set_bar_value(sgl_obj_t *obj, uint16_t index, uint16_t value)` | 指定柱的高度值 |
| 设置显示模式 | `void sgl_spectrum_set_bar_mode(sgl_obj_t *obj, uint8_t mode)` | 见下方模式常量 |
| 设置柱颜色 | `void sgl_spectrum_set_bar_color(sgl_obj_t *obj, sgl_color_t color)` | 柱体颜色 |
| 设置帽颜色 | `void sgl_spectrum_set_bar_hat_color(sgl_obj_t *obj, sgl_color_t color)` | 峰值帽颜色 |
| 设置帽高度 | `void sgl_spectrum_set_bar_hat_height(sgl_obj_t *obj, uint8_t height)` | 峰值帽高度 |
| 设置透明度 | `void sgl_spectrum_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |

### 模式常量

| 常量 | 值 | 说明 |
|------|----|----|
| `SGL_SPECTRUM_MODE_BAR` | 1<<0 | 柱模式 |
| `SGL_SPECTRUM_MODE_BLOCK` | 1<<1 | 块模式 |
| `SGL_SPECTRUM_MODE_BAR_HAT` | HAT\|BAR | 柱 + 峰值帽 |
| `SGL_SPECTRUM_MODE_BLOCK_HAT` | HAT\|BLOCK | 块 + 峰值帽 |

> 💡 `SGL_SPECTRUM_MODE_HAT_FLAG` 为帽标志位，可与 BAR/BLOCK 组合。

---

## 4. 应用示例

```c
static sgl_obj_t *g_pu = NULL;   /* 频谱控件全局指针 */
static uint16_t g_frame = 0;

/* 定时器回调：按小星星乐谱更新每根柱的数值 */
static void spec_timer_cb(const sgl_timer_t *timer, void *user_data)
{
    (void)timer; (void)user_data;
    g_frame++;
    for (int i = 0; i < 16; i++) {
        /* 示例：正弦波驱动每根柱的高度 */
        uint16_t val = (uint16_t)(40 + 30 * (sinf((g_frame + i * 8) * 3.14159f / 60.0f) + 1.0f) / 2.0f);
        sgl_spectrum_set_bar_value(g_pu, i, val);
    }
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建频谱
    g_pu = sgl_spectrum_create(page);
    sgl_obj_set_pos(g_pu, 20, 20);
    sgl_obj_set_size(g_pu, 280, 160);
    sgl_spectrum_set_bar_number(g_pu, 16);
    sgl_spectrum_set_bar_mode(g_pu, SGL_SPECTRUM_MODE_BAR_HAT);  // 柱 + 峰值帽
    sgl_spectrum_set_bar_color(g_pu, SGL_COLOR_CYAN);
    sgl_spectrum_set_bar_hat_color(g_pu, SGL_COLOR_RED);
    sgl_spectrum_set_alpha(g_pu, 220);

    // 创建定时器：300ms 周期刷新频谱
    sgl_timer_t *t = sgl_timer_create();
    if (t) {
        sgl_timer_setup(t, spec_timer_cb, 300, -1, NULL);
    }
```

![Spectrum 示例](imag/0/SGL_logo.png)

> 💡 定时器需周期调用 `sgl_timer_handler()` 才会触发（通常与 `sgl_tick_inc()` 同放 SysTick）。

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Spectrum 完整示例 sgl_spectrum_demo.c](SGL_DEMO/sgl_spectrum_demo.c) — 16 根柱 + 峰值帽，定时器驱动（小星星乐谱）

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_SPECTRUM` 开关宏后编译运行。

---

## 6. 总结

- ✅ **柱值驱动**：`set_bar_value(index, value)` 逐柱设置。
- ✅ **峰值帽模式**：`SGL_SPECTRUM_MODE_BAR_HAT` 等显示峰值。
- ✅ **定时器刷新**：配合 `sgl_timer` 实现动态频谱。

---
> 📘 **相关头文件路径**  
> - Spectrum API：`sgl/source/widgets/spectrum/sgl_spectrum.h`  
> - 定时器组件：`sgl/source/components/timer/sgl_timer.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
