# SGL Barchart 控件应用指南

## 1. 简介

`Barchart`（柱形图）是 SGL 中用于**展示分类数据的图表控件**，支持分组柱形、坐标轴、网格与打开动画。  
在 **SGL（Simple Graphics Library）** 中，`Barchart` 支持：
- 多序列（Series）数据绑定（**指针绑定**，数据数组需持久存储）
- X 轴分类标签、Y 轴数值范围/刻度
- 坐标轴网格、刻度线、标签
- 柱间距/分类间距、方向（垂直/水平）
- **打开动画**（柱形从底部/左侧展开）

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - **数据为指针绑定**：`set_series_y_array` 只保存指针，数据数组必须持续有效
> - 坐标轴通过 `SGL_BARCHART_AXIS_X / SGL_BARCHART_AXIS_Y` 标识
> - 适合统计报表、数据展示、监控面板等场景

---

## 2. 创建 Barchart

使用 `sgl_barchart_create()` 创建柱形图控件：

```c
sgl_obj_t *sgl_barchart_create(sgl_obj_t *parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* chart = sgl_barchart_create(page);
sgl_obj_set_pos(chart, 0, 0);
sgl_obj_set_size(chart, SGL_SCREEN_WIDTH, SGL_SCREEN_HEIGHT / 2);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_barchart_create()` 返回的对象指针。

### 3.1 数据与序列

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置序列数量 | `void sgl_barchart_set_series_count(sgl_obj_t *obj, uint8_t count)` | 序列个数（如 2 = 两组柱） |
| 绑定序列 Y 数据 | `void sgl_barchart_set_series_y_array(sgl_obj_t *obj, uint8_t index, const int32_t *y_array, uint8_t num)` | 绑定序列的 Y 数据数组（持久存储） |
| 设置序列颜色 | `void sgl_barchart_set_series_color(sgl_obj_t *obj, uint8_t index, sgl_color_t color, uint8_t alpha)` | 序列柱颜色与透明度 |
| 设置序列标签 | `void sgl_barchart_set_series_label(sgl_obj_t *obj, uint8_t index, const char *label)` | 序列名称（图例） |
| 设置 X 轴标签 | `void sgl_barchart_set_x_labels(sgl_obj_t *obj, const char **labels, uint8_t count)` | X 轴分类标签数组 |
| 请求重绘 | `void sgl_barchart_update(sgl_obj_t *obj)` | 数据更新后请求重绘 |
| 更新单点 | `void sgl_barchart_update_value(sgl_obj_t *obj, uint8_t series_index, uint16_t point_index)` | 更新指定数据点并重绘 |

### 3.2 坐标轴

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置坐标轴范围 | `void sgl_barchart_set_axis_range(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, int32_t min, int32_t max)` | 固定范围（同时关闭自动缩放） |
| 使能自动缩放 | `void sgl_barchart_enable_axis_auto_scale(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, bool enable)` | `true` 自动量程 |
| 设置刻度步长 | `void sgl_barchart_set_axis_step(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, int32_t step)` | 刻度步长 |
| 设置自动划分数 | `void sgl_barchart_set_axis_auto_divisions(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, uint8_t divisions)` | step 为 0 时生效 |
| 使能网格 | `void sgl_barchart_enable_axis_grid(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, bool enable)` | 显示网格线 |
| 设置网格样式 | `void sgl_barchart_set_axis_grid_style(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, uint8_t dashed)` | 0=实线，非0=虚线 |
| 设置网格颜色 | `void sgl_barchart_set_axis_grid_color(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, sgl_color_t color, uint8_t alpha)` | 网格颜色与透明度 |
| 使能刻度标签 | `void sgl_barchart_enable_axis_labels(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, bool enable)` | 显示坐标轴数值标签 |
| 设置标签字体 | `void sgl_barchart_set_axis_label_font(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, const sgl_font_t *font)` | 轴标签字体 |
| 设置标签颜色 | `void sgl_barchart_set_axis_label_color(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, sgl_color_t color, uint8_t alpha)` | 轴标签颜色与透明度 |
| 使能边缘刻度线 | `void sgl_barchart_enable_axis_ticks(sgl_obj_t *obj, sgl_barchart_axis_id_t axis, bool enable)` | 显示坐标轴刻度线 |

```c
// 坐标轴标识（sgl_barchart_axis_id_t）
SGL_BARCHART_AXIS_X   // X 轴
SGL_BARCHART_AXIS_Y   // Y 轴
```

### 3.3 外观与动画

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置背景颜色 | `void sgl_barchart_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 图表背景色 |
| 设置背景透明度 | `void sgl_barchart_set_bg_alpha(sgl_obj_t *obj, uint8_t alpha)` | 背景透明度 |
| 设置边框颜色 | `void sgl_barchart_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置透明度 | `void sgl_barchart_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 全局透明度 |
| 设置柱/分类间距 | `void sgl_barchart_set_bar_spacing(sgl_obj_t *obj, uint8_t bar_gap, uint8_t category_gap)` | 柱间距与分类间距 |
| 设置方向 | `void sgl_barchart_set_orientation(sgl_obj_t *obj, sgl_barchart_orientation_t orientation)` | `SGL_BARCHART_ORIENTATION_VERTICAL` / `_HORIZONTAL` |
| 设置绘图区域 | `void sgl_barchart_set_plot_area_rel(sgl_obj_t *obj, int16_t x, int16_t y, int16_t w, int16_t h)` | 相对控件左上角 |
| 重置绘图区域 | `void sgl_barchart_reset_plot_area(sgl_obj_t *obj)` | 恢复自动布局 |
| 设置布局内边距 | `void sgl_barchart_set_layout_padding(sgl_obj_t *obj, ...)` | 自动布局内边距 |
| 使能打开动画 | `void sgl_barchart_enable_open_anim(sgl_obj_t *obj, bool enable)` | 柱形展开动画 |
| 设置动画方向 | `void sgl_barchart_set_open_anim_dir(sgl_obj_t *obj, sgl_barchart_open_anim_dir_t dir)` | `SGL_BARCHART_OPEN_ANIM_FROM_BOTTOM` 等 |
| 设置动画时长 | `void sgl_barchart_set_open_anim_duration(sgl_obj_t *obj, uint16_t duration)` | 毫秒 |
| 设置动画缓动 | `void sgl_barchart_set_open_anim_path(sgl_obj_t *obj, sgl_anim_path_algo_t path_algo)` | 如 `SGL_ANIM_PATH_EASE_OUT` |

---

## 4. 应用示例

```c
    /* 数据数组必须持久存储（static / 全局），图表只保存指针 */
    static const int32_t sgd_bar_y1[] = { 40, 70, 55, 90, 60 };
    static const int32_t sgd_bar_y2[] = { 30, 50, 80, 45, 70 };
    static const char *sgd_bar_x_labels[] = { "A", "B", "C", "D", "E" };

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建柱形图
    sgl_obj_t *chart = sgl_barchart_create(page);
    sgl_obj_set_pos(chart, 0, 0);
    sgl_obj_set_size(chart, SGL_SCREEN_WIDTH, SGL_SCREEN_HEIGHT / 2);

    /* 数据与序列 */
    sgl_barchart_set_series_count(chart, 2);
    sgl_barchart_set_series_y_array(chart, 0, sgd_bar_y1, 5);
    sgl_barchart_set_series_y_array(chart, 1, sgd_bar_y2, 5);
    sgl_barchart_set_series_color(chart, 0, SGL_COLOR_DODGER_BLUE, SGL_ALPHA_MAX);
    sgl_barchart_set_series_color(chart, 1, SGL_COLOR_ORANGE, SGL_ALPHA_MAX);
    sgl_barchart_set_series_label(chart, 0, "Series1");
    sgl_barchart_set_series_label(chart, 1, "Series2");
    sgl_barchart_set_x_labels(chart, sgd_bar_x_labels, 5);

    /* 坐标轴 */
    sgl_barchart_set_axis_range(chart, SGL_BARCHART_AXIS_Y, 0, 100);   // Y 固定 0~100
    sgl_barchart_enable_axis_auto_scale(chart, SGL_BARCHART_AXIS_X, true);
    sgl_barchart_set_axis_step(chart, SGL_BARCHART_AXIS_Y, 25);
    sgl_barchart_enable_axis_grid(chart, SGL_BARCHART_AXIS_X, true);
    sgl_barchart_enable_axis_grid(chart, SGL_BARCHART_AXIS_Y, true);
    sgl_barchart_set_axis_grid_style(chart, SGL_BARCHART_AXIS_Y, 1);   // 虚线网格
    sgl_barchart_set_axis_grid_color(chart, SGL_BARCHART_AXIS_Y, SGL_COLOR_GRAY, 80);
    sgl_barchart_enable_axis_labels(chart, SGL_BARCHART_AXIS_X, true);
    sgl_barchart_enable_axis_labels(chart, SGL_BARCHART_AXIS_Y, true);
    sgl_barchart_set_axis_label_font(chart, SGL_BARCHART_AXIS_Y, &consolas14);
    sgl_barchart_enable_axis_ticks(chart, SGL_BARCHART_AXIS_X, true);

    /* 外观 */
    sgl_barchart_set_bg_color(chart, SGL_COLOR_BLACK);
    sgl_barchart_set_bg_alpha(chart, SGL_ALPHA_MAX);
    sgl_barchart_set_border_color(chart, SGL_COLOR_DARK_GRAY);
    sgl_barchart_set_bar_spacing(chart, 2, 10);
    sgl_barchart_set_orientation(chart, SGL_BARCHART_ORIENTATION_VERTICAL);

    /* 打开动画 */
    sgl_barchart_enable_open_anim(chart, true);
    sgl_barchart_set_open_anim_dir(chart, SGL_BARCHART_OPEN_ANIM_FROM_BOTTOM);
    sgl_barchart_set_open_anim_duration(chart, 600);
    sgl_barchart_set_open_anim_path(chart, SGL_ANIM_PATH_EASE_OUT);

    /* 数据更新后请求重绘 */
    sgl_barchart_update(chart);
```

![Barchart 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Barchart 完整示例 sgl_barchart_demo.c](SGL_DEMO/sgl_barchart_demo.c) — 分组柱形图 + 坐标轴/网格 + 打开动画

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_BARCHART` 开关宏后编译运行。

---

## 6. 总结

- ✅ **数据指针绑定**：数据数组必须持久存储（static/全局）。
- ✅ **双轴独立配置**：X/Y 轴范围、步长、网格、标签各自设置。
- ✅ **数据更新后重绘**：`update()` / `update_value()` 触发刷新。

---
> 📘 **相关头文件路径**  
> - Barchart API：`sgl/source/widgets/chart/barchart/sgl_barchart.h`  
> - 图表聚合头：`sgl/source/widgets/chart/sgl_chart.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
