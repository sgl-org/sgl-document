# SGL Battery 控件应用指南

## 1. 简介

`Battery`（电池）是 SGL 中用于**显示电量**的半透明 3D 风格电池控件。  
在 **SGL（Simple Graphics Library）** 中，`Battery` 支持：
- 电量百分比设置（0~100）
- **分级变色**：低电量（<20%）、中等（20~50%）、高电量（≥50%）使用不同颜色
- 充电状态与充电指示颜色
- 百分比文字显示/隐藏、字体、颜色
- **水平 / 垂直**两种方向、整体透明度

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 电量分级颜色规则：level < 20% 用 `low_color`，20~50% 用 `medium_color`，≥50% 用 `high_color`；若设置了 `fill_color` 则固定用该颜色
> - 常作为状态栏子控件使用
> - 适合状态栏、电源管理界面、充电显示等场景

---

## 2. 创建 Battery

使用 `sgl_battery_create()` 创建电池控件：

```c
sgl_obj_t* sgl_battery_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* battery = sgl_battery_create(page);
sgl_obj_set_pos(battery, 60, 30);
sgl_obj_set_size(battery, 200, 60);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_battery_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置电量 | `void sgl_battery_set_level(sgl_obj_t* obj, uint8_t level)` | 电量百分比（0~100） |
| 设置边框颜色 | `void sgl_battery_set_border_color(sgl_obj_t* obj, sgl_color_t color)` | 电池外框颜色 |
| 设置填充颜色 | `void sgl_battery_set_fill_color(sgl_obj_t* obj, sgl_color_t color)` | 固定填充色（0 = 按电量自动变色） |
| 设置低电量颜色 | `void sgl_battery_set_low_color(sgl_obj_t* obj, sgl_color_t color)` | 电量 < 20% 时颜色（如红） |
| 设置中等电量颜色 | `void sgl_battery_set_medium_color(sgl_obj_t* obj, sgl_color_t color)` | 电量 20~50% 时颜色（如黄） |
| 设置高电量颜色 | `void sgl_battery_set_high_color(sgl_obj_t* obj, sgl_color_t color)` | 电量 ≥ 50% 时颜色（如绿） |
| 设置背景颜色 | `void sgl_battery_set_bg_color(sgl_obj_t* obj, sgl_color_t color)` | 电池内部底色 |
| 设置充电状态 | `void sgl_battery_set_charging(sgl_obj_t* obj, bool charging)` | `true` 为充电中 |
| 设置充电颜色 | `void sgl_battery_set_charging_color(sgl_obj_t* obj, sgl_color_t color)` | 充电指示颜色 |
| 显示/隐藏百分比 | `void sgl_battery_show_percentage(sgl_obj_t* obj, bool show)` | `true` 显示百分比文字 |
| 设置字体 | `void sgl_battery_set_font(sgl_obj_t* obj, const sgl_font_t *font)` | 百分比文字字体（NULL = 系统字体） |
| 设置文字颜色 | `void sgl_battery_set_text_color(sgl_obj_t* obj, sgl_color_t color)` | 百分比文字颜色 |
| 设置透明度 | `void sgl_battery_set_alpha(sgl_obj_t* obj, uint8_t alpha)` | 0~255 |
| 设置方向 | `void sgl_battery_set_vertical(sgl_obj_t* obj, bool vertical)` | `true` 垂直，`false` 水平 |

> 📌 **说明**：`sgl_statusbar_demo.c` 中使用的 `sgl_battery_set_num_cells()` / `sgl_battery_set_direction()` / `sgl_battery_set_cap_size()` / `sgl_battery_set_cap_pos()` 等接口属于较新分支的扩展接口，当前 `sgl-main` 版本尚未提供；本版本以 `sgl_battery_set_vertical()` 控制方向。

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 水平电池（75%，充电中，显示百分比）
    sgl_obj_t* dianchi1 = sgl_battery_create(page);
    sgl_obj_set_pos(dianchi1, 60, 30);
    sgl_obj_set_size(dianchi1, 200, 60);
    sgl_battery_set_level(dianchi1, 75);
    sgl_battery_set_border_color(dianchi1, SGL_COLOR_GRAY);
    sgl_battery_set_fill_color(dianchi1, SGL_COLOR_LIME);
    sgl_battery_set_low_color(dianchi1, SGL_COLOR_RED);
    sgl_battery_set_medium_color(dianchi1, SGL_COLOR_YELLOW);
    sgl_battery_set_high_color(dianchi1, SGL_COLOR_LIME);
    sgl_battery_set_bg_color(dianchi1, SGL_COLOR_DARK_GRAY);
    sgl_battery_set_charging(dianchi1, true);              // 充电中
    sgl_battery_set_charging_color(dianchi1, SGL_COLOR_CYAN);
    sgl_battery_show_percentage(dianchi1, true);           // 显示百分比
    sgl_battery_set_font(dianchi1, &consolas14);
    sgl_battery_set_text_color(dianchi1, SGL_COLOR_WHITE);

    // 垂直电池（30%，未充电，隐藏百分比）
    sgl_obj_t* dianchi2 = sgl_battery_create(page);
    sgl_obj_set_pos(dianchi2, 120, 120);
    sgl_obj_set_size(dianchi2, 40, 100);
    sgl_battery_set_level(dianchi2, 30);
    sgl_battery_set_border_color(dianchi2, SGL_COLOR_GRAY);
    sgl_battery_set_fill_color(dianchi2, SGL_COLOR_RED);
    sgl_battery_set_bg_color(dianchi2, SGL_COLOR_DARK_GRAY);
    sgl_battery_set_vertical(dianchi2, true);              // 垂直方向
    sgl_battery_set_charging(dianchi2, false);
    sgl_battery_show_percentage(dianchi2, false);
```

![Battery 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Battery 完整示例 sgl_battery_demo.c](SGL_DEMO/sgl_battery_demo.c) — 水平/垂直电池 + 充电与百分比显示
- [Statusbar Demo 中的电池用法](SGL_DEMO/sgl_statusbar_demo.c) — 将 Battery 作为状态栏右侧子控件

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_BATTERY` 开关宏后编译运行。

---

## 6. 总结

- ✅ **分级变色**：低/中/高电量自动切换颜色，直观提示电量状态。
- ✅ **充电指示**：`set_charging(true)` + `set_charging_color()`。
- ✅ **方向可切换**：`set_vertical(true/false)` 控制横竖。

---
> 📘 **相关头文件路径**  
> - Battery API：`sgl/source/widgets/battery/sgl_battery.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
