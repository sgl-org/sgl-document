# SGL Gauge 控件应用指南

## 1. 简介

`Gauge`（仪表盘）是 SGL 中用于**显示数值的圆形仪表**控件，带外圈弧、刻度线与指针。  
在 **SGL（Simple Graphics Library）** 中，`Gauge` 支持：
- 当前值设置/读取（指针指向）
- **表盘扫角范围**（如 150°~390° 即 240° 扫角）
- 刻度起始值、步进值、刻度长度/宽度/角度
- **告警值**：超过该值后刻度切换为告警色
- 指针、外圈弧、中心轴盖（hub）的颜色与宽度
- 刻度数字字体、显示间隔、整体透明度

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 扫角范围、刻度步进、告警值配合可实现标准仪表效果
> - 适合速度表、温度表、压力表、进度仪表等场景

---

## 2. 创建 Gauge

使用 `sgl_gauge_create()` 创建仪表盘控件：

```c
sgl_obj_t* sgl_gauge_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* gauge = sgl_gauge_create(page);
sgl_obj_set_pos(gauge, 50, 20);
sgl_obj_set_size(gauge, 160, 160);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_gauge_create()` 返回的对象指针。

### 3.1 颜色

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置背景颜色 | `void sgl_gauge_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 盘面底色 |
| 设置弧颜色 | `void sgl_gauge_set_arc_color(sgl_obj_t *obj, sgl_color_t color)` | 外圈弧颜色 |
| 设置指针颜色 | `void sgl_gauge_set_pointer_color(sgl_obj_t *obj, sgl_color_t color)` | 指针颜色 |
| 设置刻度颜色 | `void sgl_gauge_set_scale_color(sgl_obj_t *obj, sgl_color_t color)` | 刻度线颜色 |
| 设置文字颜色 | `void sgl_gauge_set_text_color(sgl_obj_t *obj, sgl_color_t color)` | 刻度数字颜色 |
| 设置轴盖颜色 | `void sgl_gauge_set_hub_color(sgl_obj_t *obj, sgl_color_t color)` | 中心轴盖颜色 |

### 3.2 尺寸与宽度

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置弧宽 | `void sgl_gauge_set_arc_width(sgl_obj_t *obj, uint8_t width)` | 外圈弧线宽 |
| 设置指针宽度 | `void sgl_gauge_set_pointer_width(sgl_obj_t *obj, uint8_t width)` | 指针线宽 |
| 设置刻度宽度 | `void sgl_gauge_set_scale_width(sgl_obj_t *obj, uint8_t width)` | 刻度线宽 |
| 设置刻度长度 | `void sgl_gauge_set_scale_length(sgl_obj_t *obj, uint8_t length)` | 刻度线长 |
| 设置轴盖半径 | `void sgl_gauge_set_hub_radiue(sgl_obj_t *obj, uint8_t radius)` | 中心轴盖半径（注意头文件拼写） |

### 3.3 刻度与数值

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置刻度角度 | `void sgl_gauge_set_scale_angle(sgl_obj_t *obj, int16_t angle)` | 每个主刻度间隔角度（度） |
| 设置刻度起始值 | `void sgl_gauge_set_scale_start_value(sgl_obj_t *obj, int16_t value)` | 刻度起始数值 |
| 设置刻度步进值 | `void sgl_gauge_set_scale_step_value(sgl_obj_t *obj, uint16_t value)` | 每个主刻度的数值增量 |
| 设置告警值 | `void sgl_gauge_set_scale_warning_value(sgl_obj_t *obj, int16_t value)` | 超过该值刻度变告警色 |
| 设置文字间隔 | `void sgl_gauge_set_text_interval(sgl_obj_t *obj, uint8_t interval)` | 隔几个主刻度显示一次数字 |
| 设置扫角范围 | `void sgl_gauge_set_angle_range(sgl_obj_t *obj, int16_t start, int16_t end)` | 表盘扫角起止（度） |
| 设置当前值 | `void sgl_gauge_set_value(sgl_obj_t *obj, int16_t value)` | 指针指向的数值 |
| 获取当前值 | `int16_t sgl_gauge_get_value(sgl_obj_t *obj)` | 返回当前值 |
| 设置字体 | `void sgl_gauge_set_font(sgl_obj_t *obj, const sgl_font_t *font)` | 刻度数字字体 |
| 设置透明度 | `void sgl_gauge_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |

---

## 4. 应用示例

```c
static sgl_obj_t *g_yibiao2 = NULL;

/* 动画回调 — 更新仪表指针值 */
static void gauge_anim_path(sgl_anim_t *anim, int32_t value)
{
    sgl_gauge_set_value(g_yibiao2, (int16_t)value);
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 完整功能仪表盘
    sgl_obj_t* yibiao1 = sgl_gauge_create(page);
    sgl_obj_set_pos(yibiao1, (SGL_SCREEN_WIDTH - 160) / 2, 20);
    sgl_obj_set_size(yibiao1, 160, 160);
    sgl_gauge_set_bg_color(yibiao1, SGL_COLOR_DARK_GRAY);
    sgl_gauge_set_arc_color(yibiao1, SGL_COLOR_CYAN);
    sgl_gauge_set_pointer_color(yibiao1, SGL_COLOR_RED);
    sgl_gauge_set_scale_color(yibiao1, SGL_COLOR_WHITE);
    sgl_gauge_set_text_color(yibiao1, SGL_COLOR_WHITE);
    sgl_gauge_set_hub_color(yibiao1, SGL_COLOR_GRAY);
    sgl_gauge_set_arc_width(yibiao1, 2);
    sgl_gauge_set_pointer_width(yibiao1, 3);
    sgl_gauge_set_scale_width(yibiao1, 1);
    sgl_gauge_set_scale_length(yibiao1, 12);
    sgl_gauge_set_scale_step_value(yibiao1, 5);        // 每格 5 个单位
    sgl_gauge_set_scale_angle(yibiao1, 12);
    sgl_gauge_set_scale_warning_value(yibiao1, 80);    // 超过 80 刻度变红
    sgl_gauge_set_angle_range(yibiao1, 150, 390);      // 240° 扫角
    sgl_gauge_set_value(yibiao1, 65);
    sgl_gauge_set_font(yibiao1, &consolas14);
    sgl_gauge_set_hub_radiue(yibiao1, 8);
    sgl_gauge_set_scale_start_value(yibiao1, 0);
    sgl_gauge_set_text_interval(yibiao1, 5);           // 显示 0/10/20...100
    sgl_gauge_set_alpha(yibiao1, 220);

    // 动画仪表盘（值 0→100 循环）
    g_yibiao2 = sgl_gauge_create(page);
    sgl_obj_set_pos(g_yibiao2, (SGL_SCREEN_WIDTH - 140) / 2, 200);
    sgl_obj_set_size(g_yibiao2, 140, 160);
    sgl_gauge_set_bg_color(g_yibiao2, SGL_COLOR_DARK_GRAY);
    sgl_gauge_set_arc_color(g_yibiao2, SGL_COLOR_GOLD);
    sgl_gauge_set_pointer_color(g_yibiao2, SGL_COLOR_ORANGE);
    sgl_gauge_set_scale_color(g_yibiao2, SGL_COLOR_WHITE);
    sgl_gauge_set_angle_range(g_yibiao2, 150, 390);
    sgl_gauge_set_value(g_yibiao2, 0);
    sgl_gauge_set_font(g_yibiao2, &consolas14);
    sgl_gauge_set_scale_start_value(g_yibiao2, 0);
    sgl_gauge_set_scale_step_value(g_yibiao2, 5);

    sgl_anim_t *anim = sgl_anim_create();
    sgl_anim_set_data(anim, NULL);
    sgl_anim_set_act_duration(anim, 3000);
    sgl_anim_set_start_value(anim, 0);
    sgl_anim_set_end_value(anim, 100);
    sgl_anim_set_path(anim, gauge_anim_path, SGL_ANIM_PATH_LINEAR);
    sgl_anim_start(anim, SGL_ANIM_REPEAT_LOOP);
```

![Gauge 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Gauge 完整示例 sgl_gauge_demo.c](SGL_DEMO/sgl_gauge_demo.c) — 全接口仪表盘 + 动画仪表盘

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_GAUGE` 开关宏后编译运行。

---

## 6. 总结

- ✅ **扫角可配**：`set_angle_range(150, 390)` 得到 240° 表盘。
- ✅ **刻度体系**：起始值 + 步进值 + 间隔控制刻度数字。
- ✅ **告警变色**：`set_scale_warning_value()` 超值刻度变红。
- ✅ **动效靠动画**：动画回调中 `set_value()` 驱动指针。

---
> 📘 **相关头文件路径**  
> - Gauge API：`sgl/source/widgets/gauge/sgl_gauge.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
