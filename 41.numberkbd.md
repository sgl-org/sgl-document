# SGL NumberKbd 控件应用指南

## 1. 简介

`NumberKbd`（数字键盘）是 SGL 中用于**数字输入**的软键盘控件，比全键盘更轻量。  
在 **SGL（Simple Graphics Library）** 中，`NumberKbd` 支持：
- 键盘主体与按键样式（颜色、透明度、边框、圆角）
- 按键文字字体/颜色
- **操作码读取**（`get_opcode`）获取按键结果

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 与 `Keyboard` 不同：NumberKbd 专门输入数字，通过轮询 `get_opcode` 获取按键结果
> - 适合密码输入、数值设定、电话拨号等场景

---

## 2. 创建 NumberKbd

使用 `sgl_numberkbd_create()` 创建数字键盘控件：

```c
sgl_obj_t* sgl_numberkbd_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* kbd = sgl_numberkbd_create(page);
sgl_obj_set_pos(kbd, 20, 40);
sgl_obj_set_size(kbd, 280, 200);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_numberkbd_create()` 返回的对象指针。

### 3.1 键盘主体

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置颜色 | `void sgl_numberkbd_set_color(sgl_obj_t *obj, sgl_color_t color)` | 键盘主体颜色 |
| 设置透明度 | `void sgl_numberkbd_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 整体透明度 |
| 设置圆角 | `void sgl_numberkbd_set_radius(sgl_obj_t *obj, uint8_t radius)` | 键盘圆角 |
| 设置边框宽度 | `void sgl_numberkbd_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |
| 设置边框颜色 | `void sgl_numberkbd_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置字体 | `void sgl_numberkbd_set_text_font(sgl_obj_t *obj, const sgl_font_t *font)` | 按键文字字体 |
| 设置文字颜色 | `void sgl_numberkbd_set_text_color(sgl_obj_t *obj, sgl_color_t color)` | 按键文字颜色 |

### 3.2 按键样式

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置按钮边距 | `void sgl_numberkbd_set_btn_margin(sgl_obj_t *obj, uint8_t margin)` | 按键之间的间距 |
| 设置按钮颜色 | `void sgl_numberkbd_set_btn_color(sgl_obj_t *obj, sgl_color_t color)` | 按键背景色 |
| 设置按钮边框宽度 | `void sgl_numberkbd_set_btn_border_width(sgl_obj_t *obj, uint8_t width)` | 按键边框宽度 |
| 设置按钮边框颜色 | `void sgl_numberkbd_set_btn_border_color(sgl_obj_t *obj, sgl_color_t color)` | 按键边框颜色 |
| 设置按钮圆角 | `void sgl_numberkbd_set_btn_radius(sgl_obj_t *obj, uint8_t radius)` | 按键圆角 |

### 3.3 按键结果读取

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 获取操作码 | `uint8_t sgl_numberkbd_get_opcode(sgl_obj_t *obj)` | 读取最近一次按键的操作码 |

> 💡 按键结果通过**轮询** `get_opcode()` 获取（无事件回调），可在主循环或定时器中周期读取。

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建数字键盘
    sgl_obj_t *shuzi = sgl_numberkbd_create(page);
    sgl_obj_set_pos(shuzi, 20, 40);
    sgl_obj_set_size(shuzi, 280, 200);

    /* 键盘主体 */
    sgl_numberkbd_set_color(shuzi, SGL_COLOR_DARK_GRAY);
    sgl_numberkbd_set_alpha(shuzi, 230);
    sgl_numberkbd_set_radius(shuzi, 6);
    sgl_numberkbd_set_border_width(shuzi, 1);
    sgl_numberkbd_set_border_color(shuzi, SGL_COLOR_CYAN);

    /* 按键文字 */
    sgl_numberkbd_set_text_font(shuzi, &consolas14);
    sgl_numberkbd_set_text_color(shuzi, SGL_COLOR_WHITE);

    /* 按键样式 */
    sgl_numberkbd_set_btn_margin(shuzi, 4);
    sgl_numberkbd_set_btn_color(shuzi, SGL_COLOR_NAVY);
    sgl_numberkbd_set_btn_border_width(shuzi, 1);
    sgl_numberkbd_set_btn_border_color(shuzi, SGL_COLOR_CYAN);
    sgl_numberkbd_set_btn_radius(shuzi, 4);

    /* 主循环中轮询按键结果 */
    uint8_t opcode = sgl_numberkbd_get_opcode(shuzi);
    if (opcode != 0) {
        /* 处理按键 opcode（数字/确认/删除等） */
    }
```

![NumberKbd 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [NumberKbd 完整示例 sgl_numberkbd_demo.c](SGL_DEMO/sgl_numberkbd_demo.c) — 数字键盘全部接口演示

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_NUMBERKBD` 开关宏后编译运行。

---

## 6. 总结

- ✅ **数字专用**：比全键盘更轻量，适合数值输入。
- ✅ **轮询取结果**：`get_opcode()` 周期读取按键。
- ✅ **按键样式独立**：`set_btn_xxx` 系列配置按键外观。

---
> 📘 **相关头文件路径**  
> - NumberKbd API：`sgl/source/widgets/numberkbd/sgl_numberkbd.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
