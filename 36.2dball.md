# SGL 2DBall 控件应用指南

## 1. 简介

`2DBall`（二维球体）是 SGL 中用于**显示立体感球体**的图形控件，通过高光/阴影渲染出 3D 效果。  
在 **SGL（Simple Graphics Library）** 中，`2DBall` 支持：
- 球体颜色、背景颜色
- 半径、透明度

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 无需图片资源，纯算法渲染出球体明暗效果
> - 适合球体装饰、加载动画、游戏元素等场景

---

## 2. 创建 2DBall

使用 `sgl_2dball_create()` 创建球体控件：

```c
sgl_obj_t* sgl_2dball_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* ball = sgl_2dball_create(page);
sgl_obj_set_pos(ball, 50, 60);
sgl_obj_set_size(ball, 80, 80);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_2dball_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置球体颜色 | `void sgl_2dball_set_color(sgl_obj_t *obj, sgl_color_t color)` | 球体主体颜色 |
| 设置背景颜色 | `void sgl_2dball_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 背景/阴影颜色 |
| 设置透明度 | `void sgl_2dball_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置半径 | `void sgl_2dball_set_radius(sgl_obj_t *obj, uint16_t radius)` | 球体半径（像素） |

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 青色球体
    sgl_obj_t *qiu1 = sgl_2dball_create(page);
    sgl_obj_set_pos(qiu1, 50, 60);
    sgl_obj_set_size(qiu1, 80, 80);
    sgl_2dball_set_color(qiu1, SGL_COLOR_CYAN);
    sgl_2dball_set_bg_color(qiu1, SGL_COLOR_DARK_GRAY);
    sgl_2dball_set_alpha(qiu1, 200);
    sgl_2dball_set_radius(qiu1, 35);

    // 金色球体
    sgl_obj_t *qiu2 = sgl_2dball_create(page);
    sgl_obj_set_pos(qiu2, 190, 60);
    sgl_obj_set_size(qiu2, 80, 80);
    sgl_2dball_set_color(qiu2, SGL_COLOR_GOLD);
    sgl_2dball_set_bg_color(qiu2, SGL_COLOR_DARK_GRAY);
    sgl_2dball_set_alpha(qiu2, 180);
    sgl_2dball_set_radius(qiu2, 30);
```

![2DBall 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [2DBall 完整示例 sgl_2dball_demo.c](SGL_DEMO/sgl_2dball_demo.c) — 两个球体演示全部接口

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_2DBALL` 开关宏后编译运行。

---

## 6. 总结

- ✅ **纯算法渲染**：无需图片资源即可呈现立体球。
- ✅ **半径独立设置**：`set_radius()` 控制球体大小。
- ✅ **颜色搭配**：主体色 + 背景色形成明暗对比。

---
> 📘 **相关头文件路径**  
> - 2DBall API：`sgl/source/widgets/2dball/sgl_2dball.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
