# SGL Piechart 控件应用指南

## 1. 简介

`Piechart`（饼图）是 SGL 中用于**展示占比数据**的图表控件，支持实心饼图/环形图、图例与打开动画。  
在 **SGL（Simple Graphics Library）** 中，`Piechart` 支持：
- 多扇区设置（数值、颜色、透明度、图例文字）
- **环形图**（设置内径比例）
- **图例**：位置（左/右/上/下）、方向（垂直/水平）、色块、背景
- 扇区平滑、起始角度、打开动画

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 扇区数值之和构成整个饼图（百分比自动计算）
> - `set_inner_radius_rate` 大于 0 即为环形图
> - 适合占比统计、数据分布、仪表盘等场景

---

## 2. 创建 Piechart

使用 `sgl_piechart_create()` 创建饼图控件：

```c
sgl_obj_t* sgl_piechart_create(sgl_obj_t *parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* pie = sgl_piechart_create(page);
sgl_obj_set_pos(pie, 0, 0);
sgl_obj_set_size(pie, SGL_SCREEN_WIDTH, SGL_SCREEN_HEIGHT);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_piechart_create()` 返回的对象指针。

### 3.1 扇区

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置扇区数量 | `void sgl_piechart_set_slice_count(sgl_obj_t *obj, uint8_t count)` | 扇区个数 |
| 设置扇区数值 | `void sgl_piechart_set_slice_value(sgl_obj_t *obj, uint8_t index, uint16_t value)` | 扇区数值 |
| 设置扇区颜色 | `void sgl_piechart_set_slice_color(sgl_obj_t *obj, uint8_t index, sgl_color_t color)` | 扇区颜色 |
| 设置扇区透明度 | `void sgl_piechart_set_slice_alpha(sgl_obj_t *obj, uint8_t index, uint8_t alpha)` | 扇区透明度 |
| 设置扇区图例文字 | `void sgl_piechart_set_slice_label(sgl_obj_t *obj, uint8_t index, const char *label)` | 扇区名称 |
| 便捷设置扇区 | `void sgl_piechart_set_slice(sgl_obj_t *obj, uint8_t index, uint16_t value, sgl_color_t color, const char *label)` | 一次设置数值/颜色/文字 |

### 3.2 饼图外观

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置全局透明度 | `void sgl_piechart_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置起始角度 | `void sgl_piechart_set_start_angle(sgl_obj_t *obj, int16_t angle)` | 第一块扇区起始角 |
| 设置内径比例 | `void sgl_piechart_set_inner_radius_rate(sgl_obj_t *obj, uint16_t rate)` | 0=实心饼，>0=环形图 |
| 设置外径 | `void sgl_piechart_set_radius(sgl_obj_t *obj, int16_t radius)` | 0=按控件尺寸自动计算 |
| 设置扇区平滑 | `void sgl_piechart_set_smooth(sgl_obj_t *obj, bool enable)` | 扇区边缘平滑（抗锯齿） |

### 3.3 图例

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 使能图例 | `void sgl_piechart_enable_legend(sgl_obj_t *obj, bool enable)` | 显示图例 |
| 设置图例位置 | `void sgl_piechart_set_legend_pos(sgl_obj_t *obj, sgl_piechart_legend_pos_t pos)` | 见位置枚举 |
| 设置图例方向 | `void sgl_piechart_set_legend_dir(sgl_obj_t *obj, sgl_piechart_legend_dir_t dir)` | 垂直/水平排列 |
| 设置图例区域大小 | `void sgl_piechart_set_legend_area_size(sgl_obj_t *obj, uint16_t size)` | 左右=宽度，上下=高度 |
| 设置图例字体 | `void sgl_piechart_set_legend_font(sgl_obj_t *obj, const sgl_font_t *font)` | 图例文字字体 |
| 设置图例文字颜色 | `void sgl_piechart_set_legend_text_color(sgl_obj_t *obj, sgl_color_t color)` | 图例文字颜色 |
| 设置图例透明度 | `void sgl_piechart_set_legend_alpha(sgl_obj_t *obj, uint8_t alpha)` | 图例透明度 |
| 设置图例色块大小 | `void sgl_piechart_set_legend_box_size(sgl_obj_t *obj, uint16_t size)` | 色块边长 |
| 设置图例内边距 | `void sgl_piechart_set_legend_padding(sgl_obj_t *obj, uint16_t padding)` | 图例内边距 |
| 设置图例项间距 | `void sgl_piechart_set_legend_item_gap(sgl_obj_t *obj, uint16_t gap)` | 图例项间距 |
| 使能图例背景块 | `void sgl_piechart_enable_legend_bg(sgl_obj_t *obj, bool enable)` | 显示图例背景 |
| 设置图例背景色 | `void sgl_piechart_set_legend_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 图例背景色 |
| 设置图例边框色 | `void sgl_piechart_set_legend_border_color(sgl_obj_t *obj, sgl_color_t color)` | 图例边框色 |

```c
// 图例位置（sgl_piechart_legend_pos_t）
SGL_PIECHART_LEGEND_POS_LEFT
SGL_PIECHART_LEGEND_POS_RIGHT
SGL_PIECHART_LEGEND_POS_TOP
SGL_PIECHART_LEGEND_POS_BOTTOM

// 图例方向（sgl_piechart_legend_dir_t）
SGL_PIECHART_LEGEND_DIR_VERTICAL     // 垂直排列
SGL_PIECHART_LEGEND_DIR_HORIZONTAL   // 水平排列
```

### 3.4 打开动画

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 使能打开动画 | `void sgl_piechart_enable_open_anim(sgl_obj_t *obj, bool enable)` | 首帧 0→360 度展开 |
| 设置动画缓动 | `void sgl_piechart_set_open_anim_path(sgl_obj_t *obj, sgl_anim_path_algo_t path_algo)` | 如 `SGL_ANIM_PATH_EASE_OUT` |

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建饼图
    sgl_obj_t *pie = sgl_piechart_create(page);
    sgl_obj_set_pos(pie, 0, 0);
    sgl_obj_set_size(pie, SGL_SCREEN_WIDTH, SGL_SCREEN_HEIGHT);

    /* 扇区 */
    sgl_piechart_set_slice_count(pie, 4);
    sgl_piechart_set_slice_value(pie, 0, 30);
    sgl_piechart_set_slice_color(pie, 0, SGL_COLOR_RED);
    sgl_piechart_set_slice_alpha(pie, 0, SGL_ALPHA_MAX);
    sgl_piechart_set_slice_label(pie, 0, "Red");
    /* 便捷函数：一次设置数值/颜色/文字 */
    sgl_piechart_set_slice(pie, 1, 25, SGL_COLOR_GREEN, "Green");
    sgl_piechart_set_slice(pie, 2, 20, SGL_COLOR_BLUE, "Blue");
    sgl_piechart_set_slice(pie, 3, 15, SGL_COLOR_ORANGE, "Orange");

    /* 外观：实心饼图 */
    sgl_piechart_set_alpha(pie, SGL_ALPHA_MAX);
    sgl_piechart_set_start_angle(pie, 0);
    sgl_piechart_set_inner_radius_rate(pie, 0);   // 0=实心饼；>0=环形
    sgl_piechart_set_radius(pie, 0);              // 0=自动计算
    sgl_piechart_set_smooth(pie, true);

    /* 图例 */
    sgl_piechart_enable_legend(pie, true);
    sgl_piechart_set_legend_pos(pie, SGL_PIECHART_LEGEND_POS_RIGHT);
    sgl_piechart_set_legend_dir(pie, SGL_PIECHART_LEGEND_DIR_VERTICAL);
    sgl_piechart_set_legend_area_size(pie, 100);
    sgl_piechart_set_legend_font(pie, &consolas14);
    sgl_piechart_set_legend_text_color(pie, SGL_COLOR_WHITE);
    sgl_piechart_set_legend_alpha(pie, SGL_ALPHA_MAX);
    sgl_piechart_set_legend_box_size(pie, 10);
    sgl_piechart_set_legend_padding(pie, 6);
    sgl_piechart_set_legend_item_gap(pie, 4);
    sgl_piechart_enable_legend_bg(pie, true);
    sgl_piechart_set_legend_bg_color(pie, SGL_COLOR_DARK_GRAY);
    sgl_piechart_set_legend_border_color(pie, SGL_COLOR_GRAY);

    /* 打开动画 */
    sgl_piechart_enable_open_anim(pie, true);
    sgl_piechart_set_open_anim_path(pie, SGL_ANIM_PATH_EASE_OUT);
```

![Piechart 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Piechart 完整示例 sgl_piechart_demo.c](SGL_DEMO/sgl_piechart_demo.c) — 实心/环形饼图 + 图例 + 打开动画

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_PIECHART` 开关宏后编译运行。

---

## 6. 总结

- ✅ **扇区即占比**：数值自动换算百分比，`set_slice()` 便捷设置。
- ✅ **环形图**：`set_inner_radius_rate(>0)` 即可。
- ✅ **图例丰富**：位置/方向/色块/背景均可配置。
- ✅ **打开动画**：首帧 0→360 度展开。

---
> 📘 **相关头文件路径**  
> - Piechart API：`sgl/source/widgets/chart/piechart/sgl_piechart.h`  
> - 图表聚合头：`sgl/source/widgets/chart/sgl_chart.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
