# SGL Launcher 控件应用指南

## 1. 简介

`Launcher`（启动器/桌面）是 SGL 中用于**模拟手机桌面启动器**的高级控件，显示图标网格、支持滑动翻页与底部指示点。  
在 **SGL（Simple Graphics Library）** 中，`Launcher` 支持：
- 图标网格（行列数可配）+ 滑动翻页
- 底部翻页指示点（navigbar）
- 每个应用（App）的图标、名称、图标圆角、启动/退出回调
- 应用名称标签颜色、指示点颜色
- **点击图标进入应用**、**长按应用页面弹窗确认返回桌面**（`sgl_launcher_exit_msgbox_cb`）

> ⚠️ **重要特性**：
> - 是**容器控件**，内部自动管理图标网格与翻页
> - 应用列表通过 `sgl_launcher_app_t` 数组声明（含 `start`/`exit` 回调、图标、名称）
> - 需先定义 `sgl_launcher_attr_t` 属性（图标大小、边距、网格行列）
> - 适合桌面启动器、应用菜单、多页图标界面等场景

---

## 2. 数据结构

### 2.1 启动器属性

```c
typedef struct sgl_launcher_attr {
    int16_t icon_size;        /* 图标尺寸（像素） */
    int16_t margin_left;      /* 页面左边距 */
    int16_t margin_top;       /* 页面上边距 */
    int16_t margin_right;     /* 页面右边距 */
    int16_t margin_bottom;    /* 页面下边距 */
    int16_t grid_row;         /* 图标网格行数 */
    int16_t grid_col;         /* 图标网格列数 */
} sgl_launcher_attr_t;
```

### 2.2 应用描述

```c
typedef struct sgl_launcher_app {
    void (*start)(void *private_data);   /* 启动回调（点击图标进入应用） */
    void (*exit)(void);                  /* 退出回调（返回启动器） */
    void *private_data;                  /* 应用私有数据（如名称） */
    const sgl_pixmap_t *icon;            /* 应用图标 */
    const char *name;                    /* 应用名称 */
    uint8_t radius;                      /* 图标圆角 */
} sgl_launcher_app_t;
```

---

## 3. 创建与接口

### 3.1 创建 Launcher

```c
sgl_obj_t *sgl_launcher_create(const sgl_launcher_attr_t *attr,
                               const sgl_font_t *label_font,
                               const sgl_launcher_app_t *apps,
                               int16_t app_num,
                               void (*cb)(sgl_launcher_t *launcher));
```

- **`attr`**：启动器属性（图标大小、边距、网格行列）
- **`label_font`**：应用名称标签字体
- **`apps`**：应用描述数组
- **`app_num`**：应用数量
- **`cb`**：可选回调（可传 `NULL`）

### 3.2 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* launcher`，即通过 `sgl_launcher_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置标签颜色 | `void sgl_launcher_set_label_color(sgl_obj_t *launcher, sgl_color_t color)` | 应用名称文字颜色 |
| 设置指示点颜色 | `void sgl_launcher_set_navigbar_color(sgl_obj_t *launcher, sgl_color_t color)` | 底部翻页指示点颜色 |
| 获取当前页码 | `int16_t sgl_launcher_get_current_page(sgl_obj_t *launcher)` | 返回当前页索引 |
| 设置当前页码 | `void sgl_launcher_set_current_page(sgl_obj_t *launcher, int16_t page)` | 切换到指定页 |

### 3.3 内置退出确认回调

```c
void sgl_launcher_exit_msgbox_cb(sgl_event_t *evt);
```
> 💡 将该回调注册到应用页面（`sgl_obj_set_event_cb(lbl, sgl_launcher_exit_msgbox_cb, NULL)`），长按应用页面会弹出确认框，确认后返回启动器桌面。

---

## 4. 应用示例

```c
extern const sgl_pixmap_t pic1_pixmap;   /* 应用图标资源 */

/* 应用启动回调：点击图标后创建应用页面 */
static void app_start(void *private_data)
{
    const char *name = (const char *)private_data;
    sgl_obj_t *scr = sgl_screen_act();

    sgl_obj_t *lbl = sgl_label_create(scr);
    sgl_obj_set_pos(lbl, 0, 0);
    sgl_obj_set_size(lbl, SGL_SCREEN_WIDTH, SGL_SCREEN_HEIGHT);
    sgl_label_set_font(lbl, &consolas14);
    sgl_label_set_text(lbl, name);
    sgl_label_set_text_align(lbl, SGL_ALIGN_CENTER);
    sgl_label_set_text_color(lbl, SGL_COLOR_WHITE);
    sgl_label_set_bg_color(lbl, SGL_COLOR_DARK_GRAY);
    sgl_obj_set_clickable(lbl);

    /* 长按应用页面弹窗确认返回桌面 */
    sgl_obj_set_event_cb(lbl, sgl_launcher_exit_msgbox_cb, NULL);
}

/* 应用退出回调 */
static void app_exit(void)
{
    sgl_log("Launcher", "app exit");
}

/* 应用列表 */
static const sgl_launcher_app_t apps[] = {
    { .start = app_start, .exit = app_exit, .private_data = "Gallery", .icon = &pic1_pixmap, .name = "Gallery", .radius = 8 },
    { .start = app_start, .exit = app_exit, .private_data = "Camera",  .icon = &pic1_pixmap, .name = "Camera",  .radius = 8 },
    { .start = app_start, .exit = app_exit, .private_data = "Music",   .icon = &pic1_pixmap, .name = "Music",   .radius = 8 },
    { .start = app_start, .exit = app_exit, .private_data = "Settings",.icon = &pic1_pixmap, .name = "Settings",.radius = 8 },
};

/* 启动器属性：3×3 网格，52px 图标 */
static const sgl_launcher_attr_t launcher_attr = {
    .icon_size     = 52,
    .margin_left   = 26,
    .margin_top    = 40,
    .margin_right  = 26,
    .margin_bottom = 40,
    .grid_row      = 3,
    .grid_col      = 3,
};

void my_launcher_demo(sgl_obj_t *parent)
{
    (void)parent;

    /* 创建启动器 */
    sgl_obj_t *qidong = sgl_launcher_create(&launcher_attr, &consolas14,
                                            apps, SGL_ARRAY_SIZE(apps), NULL);
    sgl_launcher_set_label_color(qidong, SGL_COLOR_WHITE);
    sgl_launcher_set_navigbar_color(qidong, SGL_COLOR_DARK_GRAY);

    /* 翻页 */
    sgl_launcher_set_current_page(qidong, 0);
}
```

![Launcher 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Launcher 完整示例 sgl_launcher_demo.c](SGL_DEMO/sgl_launcher_demo.c) — 12 个应用的桌面启动器 + 翻页 + 进入/退出应用

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_LAUNCHER` 开关宏后编译运行。

---

## 6. 总结

- ✅ **桌面即控件**：图标网格 + 滑动翻页 + 指示点一键获得。
- ✅ **应用回调驱动**：`start`/`exit` 回调管理应用生命周期。
- ✅ **长按返回桌面**：注册 `sgl_launcher_exit_msgbox_cb` 即可。

---
> 📘 **相关头文件路径**  
> - Launcher API：`sgl/source/widgets/launcher/sgl_launcher.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
