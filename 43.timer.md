# SGL Timer 定时器组件指南

## 1. 简介

`Timer`（定时器）是 SGL 中的**软件定时器组件**（非控件对象），用于周期性/单次执行回调函数，是实时刷新类应用（示波器、频谱、时钟等）的核心驱动。

在 **SGL（Simple Graphics Library）** 中，`Timer` 支持：
- 周期定时回调（毫秒级）
- 重复次数控制（单次 / 多次 / 无限循环）
- 回调携带用户数据
- 与 `sgl_tick_inc()` 共用毫秒时基

> ⚠️ **重要特性**：
> - **不是控件对象**：`sgl_timer_t` 不继承 `sgl_obj_t`，不参与页面/布局
> - **必须周期性调用 `sgl_timer_handler()`** 才会触发回调，通常与 `sgl_tick_inc(1)` 放在同一个 1ms 定时器中断中，可节省定时器资源
> - 适合周期刷新、延时触发、心跳检测等场景

---

## 2. 创建与配置

### 2.1 创建定时器

```c
sgl_timer_t* sgl_timer_create(void);
```
- 返回值：定时器对象指针（NULL 表示分配失败）

### 2.2 设置定时器

```c
bool sgl_timer_setup(sgl_timer_t *timer, sgl_timer_callback_t callback, uint16_t interval, int32_t repeat_cnt, void *user_data);
```
- **`timer`**：由 `sgl_timer_create()` 创建的定时器
- **`callback`**：定时回调函数，签名见下
- **`interval`**：定时周期（毫秒）
- **`repeat_cnt`**：重复次数；`-1` = 无限循环，`n` = 执行 n 次
- **`user_data`**：回调中携带的用户数据
- 返回值：成功返回 `true`，失败返回 `false`

### 2.3 回调函数类型

```c
typedef void (*sgl_timer_callback_t)(const sgl_timer_t *timer, void *user_data);
```

### 2.4 删除定时器

```c
void sgl_timer_delete(sgl_timer_t *timer);
```

### 2.5 驱动函数（关键！）

```c
void sgl_timer_handler(void);
```
> ⚠️ **必须周期调用**：将 `sgl_timer_handler()` 放在 1ms 中断中（与 `sgl_tick_inc(1)` 一起），定时器才能工作：

```c
// 你的 SysTick 中断处理函数，定时时间为 1ms
void SysTick_Handler(void)
{
    sgl_tick_inc(1);
    sgl_timer_handler();   // 放一起，节省定时器资源
}
```

---

## 3. 应用示例

```c
static uint32_t g_timer_cnt = 0;

/* 定时回调：计数并打印 */
static void timer_callback(const sgl_timer_t *timer, void *user_data)
{
    (void)timer;
    (void)user_data;
    g_timer_cnt++;
    sgl_log("TIMER", "App clicked: %d", g_timer_cnt);
}

void my_timer_demo(void)
{
    /* 创建定时器 */
    sgl_timer_t *timer = sgl_timer_create();
    if (timer == NULL) return;

    /* 设置定时器：周期 100ms，循环 5 次 */
    if (!sgl_timer_setup(timer, timer_callback, 100, 5, NULL)) {
        sgl_timer_delete(timer);
        return;
    }
    /* 定时器创建后无需持有引用，handler 自动驱动；用完可 delete */
}
```

> 💡 无限循环示例：`sgl_timer_setup(t, cb, 30, -1, NULL)` 表示每 30ms 回调一次、永不停止（常用于示波器采样、频谱刷新）。

---

## 4. 与控件配合的典型用法

| 应用 | 定时器配置 | 回调中执行 |
|------|-----------|-----------|
| 示波器 | 30ms 周期，无限循环 | `sgl_scope_append_data()` |
| 频谱 | 300ms 周期，无限循环 | `sgl_spectrum_set_bar_value()` |
| 模拟时钟 | 1000ms 周期，无限循环 | 进位后 `sgl_analogclock_set_time()` |

```c
/* 示例：模拟时钟走秒 */
sgl_timer_t *t = sgl_timer_create();
if (t) {
    sgl_timer_setup(t, clock_timer_cb, 1000, -1, NULL);  // 每秒回调
}
```

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Timer 完整示例 sgl_timer_demo.c](SGL_DEMO/sgl_timer_demo.c) — 100ms 周期、循环 5 次的定时器
- [AnalogClock 示例中的定时器用法](SGL_DEMO/sgl_analogclock_demo.c) — 定时器驱动时钟走秒

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_TIMER` 开关宏后编译运行。

---

## 6. 总结

- ✅ **毫秒周期回调**：`set_interval` 设置周期。
- ✅ **重复次数可控**：`-1` 无限循环，`n` 执行 n 次。
- ✅ **必须调用 handler**：`sgl_timer_handler()` 与 `sgl_tick_inc(1)` 同放中断。

---
> 📘 **相关头文件路径**  
> - Timer API：`sgl/source/components/timer/sgl_timer.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
