# SGL Win 控件应用指南

## 1. 简介

`Win`（窗口）是 SGL 中的**窗口容器控件**，带标题栏与右上角关闭按钮，内部可容纳任意子控件。  
在 **SGL（Simple Graphics Library）** 中，`Win` 支持：
- 标题栏（文字、颜色、字体、高度、对齐、背景色）
- 右上角**关闭按钮**（点击后窗口销毁）
- 背景色、圆角、边框、透明度、背景图片
- 作为容器挂载标签、按钮等子控件

> ⚠️ **重要特性**：
> - 是**容器控件**，可以包含子控件（内容区为窗口子控件）
> - 子控件位置相对窗口内容区（标题栏下方）
> - 点击关闭按钮后窗口被销毁，可用 `sgl_obj_is_destroyed()` 判断后重新创建
> - 适合弹窗、对话框、多窗口应用等场景

---

## 2. 创建 Win

使用 `sgl_win_create()` 创建窗口控件：

```c
sgl_obj_t* sgl_win_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* win = sgl_win_create(page);
sgl_obj_set_pos(win, 30, 30);
sgl_obj_set_size(win, 260, 200);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_win_create()` 返回的对象指针。

### 3.1 外观

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置颜色 | `void sgl_win_set_color(sgl_obj_t *obj, sgl_color_t color)` | 窗口主体填充色 |
| 设置圆角 | `void sgl_win_set_radius(sgl_obj_t *obj, uint8_t radius)` | 圆角半径 |
| 设置透明度 | `void sgl_win_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置边框宽度 | `void sgl_win_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |
| 设置边框颜色 | `void sgl_win_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置背景图片 | `void sgl_win_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 背景图指针 |

### 3.2 标题栏

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置标题文字 | `void sgl_win_set_title_text(sgl_obj_t *obj, const char *text)` | 标题文本 |
| 设置标题颜色 | `void sgl_win_set_title_text_color(sgl_obj_t *obj, sgl_color_t color)` | 标题文字颜色 |
| 设置标题字体 | `void sgl_win_set_title_font(sgl_obj_t *obj, const sgl_font_t *font)` | 标题字体 |
| 设置标题高度 | `void sgl_win_set_title_height(sgl_obj_t *obj, uint16_t height)` | 标题栏高度 |
| 获取标题高度 | `int16_t sgl_win_title_height(sgl_obj_t *obj)` | 返回标题栏高度 |
| 设置标题对齐 | `void sgl_win_set_title_text_align(sgl_obj_t *obj, uint8_t align)` | 标题对齐方式 |
| 设置标题背景色 | `void sgl_win_set_title_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 标题栏背景色 |
| 设置关闭按钮颜色 | `void sgl_win_set_close_btn_color(sgl_obj_t *obj, sgl_color_t color)` | 右上角关闭按钮颜色 |

---

## 4. 应用示例

```c
static sgl_obj_t *g_win2 = NULL;

/* 窗口内按钮回调 — 更新窗口内标签 */
static void btn_inside_win2(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_CLICKED) {
        sgl_obj_t *label = (sgl_obj_t *)sgl_event_get_data(e);
        if (label) {
            sgl_label_set_text(label, "Button clicked");
        }
    }
}

/* 打开窗口按钮回调：若窗口已被关闭销毁则重新创建 */
static void btn_open_win(sgl_event_t *e)
{
    (void)e;
    if (g_win2 && !sgl_obj_is_destroyed(g_win2)) {
        sgl_obj_set_visible(g_win2);
        return;
    }
    g_win2 = NULL;
    create_demo_win2();
}

static void create_demo_win2(void)
{
    g_win2 = sgl_win_create(page);
    sgl_obj_set_pos(g_win2, 80, 130);
    sgl_obj_set_size(g_win2, 200, 140);
    sgl_win_set_color(g_win2, SGL_COLOR_DARK_GRAY);
    sgl_win_set_radius(g_win2, 6);
    sgl_win_set_alpha(g_win2, 220);
    sgl_win_set_border_width(g_win2, 1);
    sgl_win_set_border_color(g_win2, SGL_COLOR_GOLD);
    sgl_win_set_title_text(g_win2, "Control Win");
    sgl_win_set_title_text_color(g_win2, SGL_COLOR_WHITE);
    sgl_win_set_title_font(g_win2, &consolas14);
    sgl_win_set_title_height(g_win2, 28);
    sgl_win_set_title_text_align(g_win2, SGL_ALIGN_LEFT_MID);
    sgl_win_set_title_bg_color(g_win2, SGL_COLOR_SADDLE_BROWN);
    sgl_win_set_close_btn_color(g_win2, SGL_COLOR_RED);

    /* 窗口内容区子控件（位置相对窗口） */
    sgl_obj_t* lab = sgl_label_create(g_win2);
    sgl_obj_set_pos(lab, 12, 42);
    sgl_obj_set_size(lab, 160, 22);
    sgl_label_set_text(lab, "Ready");
    sgl_label_set_font(lab, &consolas14);
    sgl_label_set_text_color(lab, SGL_COLOR_WHITE);

    sgl_obj_t* btn = sgl_button_create(g_win2);
    sgl_obj_set_pos(btn, 12, 74);
    sgl_obj_set_size(btn, 110, 28);
    sgl_button_set_text(btn, "Click Me");
    sgl_button_set_font(btn, &consolas14);
    sgl_obj_set_event_cb(btn, btn_inside_win2, lab);
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 完整功能窗口
    sgl_obj_t* chuangkou = sgl_win_create(page);
    sgl_obj_set_pos(chuangkou, 30, 30);
    sgl_obj_set_size(chuangkou, 260, 200);
    sgl_win_set_color(chuangkou, SGL_COLOR_DARK_GRAY);
    sgl_win_set_radius(chuangkou, 6);
    sgl_win_set_alpha(chuangkou, 220);
    sgl_win_set_border_width(chuangkou, 1);
    sgl_win_set_border_color(chuangkou, SGL_COLOR_CYAN);
    sgl_win_set_title_text(chuangkou, "Window Demo");
    sgl_win_set_title_text_color(chuangkou, SGL_COLOR_WHITE);
    sgl_win_set_title_font(chuangkou, &consolas14);
    sgl_win_set_title_height(chuangkou, 30);
    sgl_win_set_title_text_align(chuangkou, SGL_ALIGN_LEFT_MID);
    sgl_win_set_title_bg_color(chuangkou, SGL_COLOR_NAVY);
    sgl_win_set_close_btn_color(chuangkou, SGL_COLOR_RED);

    /* 窗口内说明文字 */
    sgl_obj_t* lab = sgl_label_create(chuangkou);
    sgl_obj_set_pos(lab, 12, 44);
    sgl_obj_set_size(lab, 220, 52);
    sgl_label_set_text(lab, "This window can host labels, buttons\nand other child widgets.");
    sgl_label_set_font(lab, &consolas14);
    sgl_label_set_text_color(lab, SGL_COLOR_WHITE);
```

![Win 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Win 完整示例 sgl_win_demo.c](SGL_DEMO/sgl_win_demo.c) — 完整功能窗口 + 可重新打开的第二个窗口

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_WIN` 开关宏后编译运行。

---

## 6. 总结

- ✅ **窗口即容器**：内容区可挂载标签、按钮等子控件。
- ✅ **关闭即销毁**：点击右上角关闭按钮销毁窗口，用 `sgl_obj_is_destroyed()` 判断。
- ✅ **重新打开**：销毁后重新 `sgl_win_create()` 即可。

---
> 📘 **相关头文件路径**  
> - Win API：`sgl/source/widgets/win/sgl_win.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
