# SGL Ring 控件应用指南

## 1. 简介

`Ring`（圆环）是 SGL 中的**环形图形控件**，用于绘制空心圆环（甜甜圈形）。  
在 **SGL（Simple Graphics Library）** 中，`Ring` 支持：
- **内外双半径**设置（决定环的粗细）
- 填充颜色、透明度设置

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 环的粗细由 `sgl_ring_set_radius(obj, in, out)` 的内外半径差决定
> - 适合进度环、环形指示灯、装饰图形（如奥运五环）等场景

---

## 2. 创建 Ring

使用 `sgl_ring_create()` 创建圆环控件：

```c
sgl_obj_t* sgl_ring_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* ring = sgl_ring_create(page);
sgl_obj_set_pos(ring, 50, 60);
sgl_obj_set_size(ring, 68, 68);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_ring_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置颜色 | `void sgl_ring_set_color(sgl_obj_t *obj, sgl_color_t color)` | `color`：圆环颜色（如 `SGL_COLOR_YELLOW`） |
| 设置透明度 | `void sgl_ring_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | `alpha`：0~255，255 为完全不透明 |
| 设置内外半径 | `void sgl_ring_set_radius(sgl_obj_t *obj, uint16_t radius_in, uint16_t radius_out)` | 内半径与外半径（像素），外半径>内半径 |

> 💡 控件尺寸建议设置约为外半径的 2 倍（`2 * radius_out`），保证圆环完整显示。

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 单环
    sgl_obj_t* huan = sgl_ring_create(page);
    sgl_obj_set_pos(huan, 50, 60);
    sgl_obj_set_size(huan, 68, 68);
    sgl_ring_set_color(huan, SGL_COLOR_BLUE);
    sgl_ring_set_alpha(huan, 230);
    sgl_ring_set_radius(huan, 29, 34);     // 内径 29，外径 34

    // 多个环组合（奥运五环风格）
    const int step_x = 50;
    for (int i = 0; i < 3; i++) {
        sgl_obj_t* ring = sgl_ring_create(page);
        sgl_obj_set_pos(ring, 50 + i * step_x, 180);
        sgl_obj_set_size(ring, 68, 68);
        sgl_ring_set_color(ring, SGL_COLOR_RED);
        sgl_ring_set_alpha(ring, 230);
        sgl_ring_set_radius(ring, 29, 34);
    }
```

![Ring 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Ring 完整示例 sgl_ring_demo.c](SGL_DEMO/sgl_ring_demo.c) — 用 5 个圆环绘制奥运五环（含交错层次）

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_RING` 开关宏后编译运行。

---

## 6. 总结

- ✅ **内外半径定粗细**：`set_radius(in, out)` 的差值越大环越粗。
- ✅ **绘制顺序定层次**：后创建的环绘制在上层，可利用交错实现环环相扣效果。

---
> 📘 **相关头文件路径**  
> - Ring API：`sgl/source/widgets/ring/sgl_ring.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
