# SGL Icon 控件应用指南

## 1. 简介

`Icon`（图标）是 SGL 中用于**独立显示图标**的控件，可单独设置颜色、透明度与对齐方式。  
在 **SGL（Simple Graphics Library）** 中，`Icon` 支持：
- 设置图标（`sgl_icon_pixmap_t` 图标像素图）
- 图标颜色（单色图标可染色）
- 透明度、对齐方式

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 与 `Label` 文本内嵌图标不同，`Icon` 是独立控件，更适合需要单独布局的图标
> - 图标数据为 `sgl_icon_pixmap_t` 结构（含宽、高、位图指针）
> - 适合工具栏图标、菜单图标、按钮图标等场景

---

## 2. 创建 Icon

使用 `sgl_icon_create()` 创建图标控件：

```c
sgl_obj_t* sgl_icon_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* icon = sgl_icon_create(page);
sgl_obj_set_pos(icon, 60, 60);
sgl_obj_set_size(icon, 60, 60);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_icon_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置图标 | `void sgl_icon_set_icon(sgl_obj_t *obj, const sgl_icon_pixmap_t *icon)` | `icon`：图标像素图指针 |
| 设置颜色 | `void sgl_icon_set_color(sgl_obj_t *obj, sgl_color_t color)` | 图标颜色（单色染色） |
| 设置透明度 | `void sgl_icon_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置对齐 | `void sgl_icon_set_align(sgl_obj_t *obj, sgl_align_type_t align)` | 图标在控件内的对齐方式（如 `SGL_ALIGN_CENTER`） |

### 图标像素图结构

```c
typedef struct sgl_icon_pixmap {
    uint16_t       width;       // 图标宽度
    uint16_t       height;      // 图标高度
    const uint8_t *bitmap;      // 图标位图数据
} sgl_icon_pixmap_t;
```

---

## 4. 应用示例

```c
    // 定义图标数据（示例）
    extern const sgl_icon_pixmap_t icon_bell;    // 图标资源（由工具生成）

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 青色图标
    sgl_obj_t* tubiao1 = sgl_icon_create(page);
    sgl_obj_set_pos(tubiao1, 60, 60);
    sgl_obj_set_size(tubiao1, 60, 60);
    sgl_icon_set_icon(tubiao1, &icon_bell);
    sgl_icon_set_color(tubiao1, SGL_COLOR_CYAN);
    sgl_icon_set_alpha(tubiao1, 220);
    sgl_icon_set_align(tubiao1, SGL_ALIGN_CENTER);

    // 金色图标（第二个）
    sgl_obj_t* tubiao2 = sgl_icon_create(page);
    sgl_obj_set_pos(tubiao2, 180, 60);
    sgl_obj_set_size(tubiao2, 60, 60);
    sgl_icon_set_icon(tubiao2, &icon_bell);
    sgl_icon_set_color(tubiao2, SGL_COLOR_GOLD);
    sgl_icon_set_alpha(tubiao2, 200);
    sgl_icon_set_align(tubiao2, SGL_ALIGN_CENTER);
```

![Icon 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Icon 完整示例 sgl_icon_demo.c](SGL_DEMO/sgl_icon_demo.c) — 多个图标控件演示颜色/透明度/对齐

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_ICON` 开关宏后编译运行。

---

## 6. 总结

- ✅ **独立图标控件**：与 Label 文本内嵌图标相比，Icon 便于单独布局。
- ✅ **单色染色**：`set_color()` 可改变图标颜色。
- ✅ **对齐控制**：`set_align()` 调整图标在控件内的位置。

---
> 📘 **相关头文件路径**  
> - Icon API：`sgl/source/widgets/icon/sgl_icon.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
