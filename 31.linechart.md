# SGL Linechart 控件应用指南

## 1. 简介

`Linechart`（折线图）是 SGL 中用于**展示趋势数据**的图表控件，支持折线/散点/折线+点、区域填充、坐标轴与打开动画。  
在 **SGL（Simple Graphics Library）** 中，`Linechart` 支持：
- 多序列数据绑定（**指针绑定**，数据数组需持久存储）
- 序列模式：折线、散点、折线+点
- 数据点标记（圆形/方形）、折线下方区域填充
- X/Y 坐标轴、网格、刻度、标签
- **打开动画**（从左到右逐段展开）

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - **数据为指针绑定**：`set_series_y_array` / `set_series_data` 只保存指针
> - X 轴可用索引（只绑 Y）或完整坐标（绑 X/Y 两组数组）
> - 适合波形趋势、历史曲线、监控数据等场景

---

## 2. 创建 Linechart

使用 `sgl_linechart_create()` 创建折线图控件：

```c
sgl_obj_t* sgl_linechart_create(sgl_obj_t *parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* chart = sgl_linechart_create(page);
sgl_obj_set_pos(chart, 0, 0);
sgl_obj_set_size(chart, SGL_SCREEN_WIDTH, SGL_SCREEN_HEIGHT / 2);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_linechart_create()` 返回的对象指针。

### 3.1 数据与序列

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置序列数量 | `void sgl_linechart_set_series_count(sgl_obj_t *obj, uint8_t count)` | 序列个数 |
| 绑定 Y 数据 | `void sgl_linechart_set_series_y_array(sgl_obj_t *obj, uint8_t index, const int32_t *y_array, uint8_t num)` | X 自动用索引 0..n-1 |
| 绑定 X/Y 数据 | `void sgl_linechart_set_series_data(sgl_obj_t *obj, uint8_t index, const int32_t *x_array, const int32_t *y_array, uint8_t num)` | 完整 X/Y 坐标 |
| 设置序列模式 | `void sgl_linechart_set_series_mode(sgl_obj_t *obj, uint8_t index, sgl_linechart_series_mode_t mode)` | 见模式枚举 |
| 设置线颜色 | `void sgl_linechart_set_series_line_color(sgl_obj_t *obj, uint8_t index, sgl_color_t color)` | 折线颜色 |
| 设置线透明度 | `void sgl_linechart_set_series_line_alpha(sgl_obj_t *obj, uint8_t index, uint8_t alpha)` | 折线透明度 |
| 设置线宽 | `void sgl_linechart_set_series_line_width(sgl_obj_t *obj, uint8_t index, uint8_t width)` | 折线宽度 |
| 使能数据点 | `void sgl_linechart_enable_series_points(sgl_obj_t *obj, uint8_t index, bool enable)` | 显示数据点标记 |
| 设置点样式 | `void sgl_linechart_set_series_point_style(sgl_obj_t *obj, uint8_t index, sgl_linechart_point_shape_t shape, uint8_t radius)` | 点形状与半径 |
| 使能区域填充 | `void sgl_linechart_enable_series_fill(sgl_obj_t *obj, uint8_t index, bool enable)` | 折线下方区域填充 |
| 设置填充颜色 | `void sgl_linechart_set_series_fill_color(sgl_obj_t *obj, uint8_t index, sgl_color_t color, uint8_t alpha)` | 填充色与透明度 |
| 设置序列标签 | `void sgl_linechart_set_series_label(sgl_obj_t *obj, uint8_t index, const char *label)` | 序列名称 |
| 设置 X 轴标签 | `void sgl_linechart_set_x_labels(sgl_obj_t *obj, const char **labels, uint8_t count)` | X 轴标签数组 |
| 请求重绘 | `void sgl_linechart_update(sgl_obj_t *obj)` | 数据更新后请求重绘 |
| 更新单点 | `void sgl_linechart_update_value(sgl_obj_t *obj, uint8_t series_index, uint16_t point_index)` | 更新指定点并重绘 |

```c
// 序列模式（sgl_linechart_series_mode_t）
SGL_LINECHART_SERIES_MODE_LINE           // 仅折线
SGL_LINECHART_SERIES_MODE_POINT          // 仅散点
SGL_LINECHART_SERIES_MODE_LINE_AND_POINT // 折线 + 点

// 点形状（sgl_linechart_point_shape_t）
SGL_LINECHART_POINT_SHAPE_CIRCLE
SGL_LINECHART_POINT_SHAPE_SQUARE
```

### 3.2 坐标轴

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置坐标轴范围 | `void sgl_linechart_set_axis_range(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, int32_t min, int32_t max)` | 固定范围（关闭自动缩放） |
| 使能自动缩放 | `void sgl_linechart_enable_axis_auto_scale(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, bool enable)` | 自动量程 |
| 设置刻度步长 | `void sgl_linechart_set_axis_step(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, int32_t step)` | 刻度步长 |
| 设置自动划分数 | `void sgl_linechart_set_axis_auto_divisions(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, uint8_t divisions)` | step 为 0 时生效 |
| 使能网格 | `void sgl_linechart_enable_axis_grid(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, bool enable)` | 显示网格 |
| 设置网格样式 | `void sgl_linechart_set_axis_grid_style(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, uint8_t dashed)` | 0=实线，非0=虚线 |
| 设置网格颜色 | `void sgl_linechart_set_axis_grid_color(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, sgl_color_t color, uint8_t alpha)` | 网格颜色与透明度 |
| 使能刻度标签 | `void sgl_linechart_enable_axis_labels(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, bool enable)` | 显示数值标签 |
| 设置标签字体 | `void sgl_linechart_set_axis_label_font(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, const sgl_font_t *font)` | 轴标签字体 |
| 设置标签颜色 | `void sgl_linechart_set_axis_label_color(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, sgl_color_t color, uint8_t alpha)` | 轴标签颜色与透明度 |
| 使能边缘刻度线 | `void sgl_linechart_enable_axis_ticks(sgl_obj_t *obj, sgl_linechart_axis_id_t axis, bool enable)` | 显示刻度线 |

```c
// 坐标轴标识（sgl_linechart_axis_id_t）
SGL_LINECHART_AXIS_X
SGL_LINECHART_AXIS_Y
```

### 3.3 外观与动画

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置背景颜色 | `void sgl_linechart_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 图表背景色 |
| 设置背景透明度 | `void sgl_linechart_set_bg_alpha(sgl_obj_t *obj, uint8_t alpha)` | 背景透明度 |
| 设置边框颜色 | `void sgl_linechart_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置透明度 | `void sgl_linechart_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 全局透明度 |
| 设置绘图区域 | `void sgl_linechart_set_plot_area_rel(sgl_obj_t *obj, int16_t x, int16_t y, int16_t w, int16_t h)` | 相对控件左上角 |
| 重置绘图区域 | `void sgl_linechart_reset_plot_area(sgl_obj_t *obj)` | 恢复自动布局 |
| 使能打开动画 | `void sgl_linechart_enable_open_anim(sgl_obj_t *obj, bool enable)` | 折线展开动画 |
| 设置动画方向 | `void sgl_linechart_set_open_anim_dir(sgl_obj_t *obj, sgl_linechart_open_anim_dir_t dir)` | `SGL_LINECHART_OPEN_ANIM_FROM_LEFT` 等 |
| 设置动画缓动 | `void sgl_linechart_set_open_anim_path(sgl_obj_t *obj, sgl_anim_path_algo_t path_algo)` | 如 `SGL_ANIM_PATH_EASE_OUT` |

---

## 4. 应用示例

```c
    /* 数据数组必须持久存储（static / 全局） */
    static const int32_t sgd_line_y1[] = { 20, 45, 30, 70, 50, 90, 60 };
    static const int32_t sgd_line_x2[] = { 0, 10, 20, 30, 40, 50, 60 };
    static const int32_t sgd_line_y2[] = { 80, 55, 75, 40, 60, 35, 70 };
    static const char *sgd_line_x_labels[] = { "Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun" };

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建折线图
    sgl_obj_t *chart = sgl_linechart_create(page);
    sgl_obj_set_pos(chart, 0, 0);
    sgl_obj_set_size(chart, SGL_SCREEN_WIDTH, SGL_SCREEN_HEIGHT / 2);

    /* 数据与序列 */
    sgl_linechart_set_series_count(chart, 2);
    sgl_linechart_set_series_y_array(chart, 0, sgd_line_y1, 7);          // 只绑 Y
    sgl_linechart_set_series_data(chart, 1, sgd_line_x2, sgd_line_y2, 7); // 绑 X/Y
    sgl_linechart_set_series_mode(chart, 0, SGL_LINECHART_SERIES_MODE_LINE_AND_POINT);
    sgl_linechart_set_series_mode(chart, 1, SGL_LINECHART_SERIES_MODE_LINE);
    sgl_linechart_set_series_line_color(chart, 0, SGL_COLOR_CYAN);
    sgl_linechart_set_series_line_color(chart, 1, SGL_COLOR_ORANGE);
    sgl_linechart_set_series_line_alpha(chart, 0, SGL_ALPHA_MAX);
    sgl_linechart_set_series_line_width(chart, 0, 1);
    sgl_linechart_enable_series_points(chart, 0, true);
    sgl_linechart_set_series_point_style(chart, 0, SGL_LINECHART_POINT_SHAPE_CIRCLE, 3);
    sgl_linechart_enable_series_fill(chart, 0, true);                    // 区域填充
    sgl_linechart_set_series_fill_color(chart, 0, SGL_COLOR_CYAN, 40);
    sgl_linechart_set_series_label(chart, 0, "Series1");
    sgl_linechart_set_series_label(chart, 1, "Series2");
    sgl_linechart_set_x_labels(chart, sgd_line_x_labels, 7);

    /* 坐标轴 */
    sgl_linechart_set_axis_range(chart, SGL_LINECHART_AXIS_Y, 0, 100);
    sgl_linechart_enable_axis_auto_scale(chart, SGL_LINECHART_AXIS_X, true);
    sgl_linechart_set_axis_step(chart, SGL_LINECHART_AXIS_Y, 25);
    sgl_linechart_enable_axis_grid(chart, SGL_LINECHART_AXIS_X, true);
    sgl_linechart_enable_axis_grid(chart, SGL_LINECHART_AXIS_Y, true);
    sgl_linechart_set_axis_grid_style(chart, SGL_LINECHART_AXIS_Y, 1);
    sgl_linechart_set_axis_grid_color(chart, SGL_LINECHART_AXIS_Y, SGL_COLOR_GRAY, 80);
    sgl_linechart_enable_axis_labels(chart, SGL_LINECHART_AXIS_X, true);
    sgl_linechart_enable_axis_labels(chart, SGL_LINECHART_AXIS_Y, true);
    sgl_linechart_set_axis_label_font(chart, SGL_LINECHART_AXIS_Y, &consolas14);
    sgl_linechart_enable_axis_ticks(chart, SGL_LINECHART_AXIS_X, true);

    /* 外观 */
    sgl_linechart_set_bg_color(chart, SGL_COLOR_BLACK);
    sgl_linechart_set_bg_alpha(chart, SGL_ALPHA_MAX);
    sgl_linechart_set_border_color(chart, SGL_COLOR_DARK_GRAY);
    sgl_linechart_set_alpha(chart, SGL_ALPHA_MAX);

    /* 打开动画 */
    sgl_linechart_enable_open_anim(chart, true);
    sgl_linechart_set_open_anim_dir(chart, SGL_LINECHART_OPEN_ANIM_FROM_LEFT);
    sgl_linechart_set_open_anim_path(chart, SGL_ANIM_PATH_EASE_OUT);

    /* 数据更新后请求重绘 */
    sgl_linechart_update(chart);
```

![Linechart 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Linechart 完整示例 sgl_linechart_demo.c](SGL_DEMO/sgl_linechart_demo.c) — 折线/散点/折线+点 + 区域填充 + 坐标轴 + 打开动画

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_LINECHART` 开关宏后编译运行。

---

## 6. 总结

- ✅ **数据指针绑定**：数据数组必须持久存储。
- ✅ **三种序列模式**：折线 / 散点 / 折线+点。
- ✅ **区域填充增强表现**：`enable_series_fill` + `set_series_fill_color`。
- ✅ **数据更新后重绘**：`update()` / `update_value()`。

---
> 📘 **相关头文件路径**  
> - Linechart API：`sgl/source/widgets/chart/linechart/sgl_linechart.h`  
> - 图表聚合头：`sgl/source/widgets/chart/sgl_chart.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
