# SGL Arc Label 控件应用指南

## 1. 简介

`ArcLabel`（弧形标签）是 SGL 中的**旋转文本控件**，可在任意角度下显示文本。  
在 **SGL（Simple Graphics Library）** 中，`ArcLabel` 支持：
- 文本**任意角度旋转**显示
- 文本、字体、颜色、对齐、偏移设置
- 背景色、圆角、透明度
- 原始位置/尺寸重定义（配合旋转布局）

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 与普通 `Label` 的区别：支持 `sgl_arc_label_set_angle()` 旋转整个文本区域
> - 适合仪表盘刻度文字、圆形菜单、旋转标题等场景

---

## 2. 创建 ArcLabel

使用 `sgl_arc_label_create()` 创建弧形标签控件：

```c
sgl_obj_t* sgl_arc_label_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* alabel = sgl_arc_label_create(page);
sgl_obj_set_pos(alabel, 50, 50);
sgl_obj_set_size(alabel, 120, 40);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_arc_label_create()` 返回的对象指针。

### 3.1 文本与字体

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置文本内容 | `void sgl_arc_label_set_text(sgl_obj_t *obj, const char *text)` | `text`：C 字符串（支持 UTF-8） |
| 设置文本缓冲区 | `void sgl_arc_label_set_text_buffer(sgl_obj_t *obj, char *buf, uint16_t buf_size)` | 使用用户缓冲区，避免动态内存 |
| 格式化文本（静态） | `void sgl_arc_label_set_text_fmt(sgl_obj_t *obj, const char *fmt, ...)` | 需先调用 `set_text_buffer()` |
| 格式化文本（动态） | `void sgl_arc_label_set_text_fmt_dynamic(sgl_obj_t *obj, const char *fmt, ...)` | 自动分配内存 |
| 获取文本 | `char* sgl_arc_label_get_text(sgl_obj_t *obj)` | 返回当前文本指针 |
| 更新文本区域 | `void sgl_arc_label_update_text(sgl_obj_t *obj)` | 修改缓冲区内容后调用刷新 |
| 设置字体 | `void sgl_arc_label_set_font(sgl_obj_t *obj, const sgl_font_t *font)` | **必须设置，否则断言失败** |
| 设置文本颜色 | `void sgl_arc_label_set_text_color(sgl_obj_t *obj, sgl_color_t color)` | 文本前景色 |

### 3.2 样式与旋转

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置背景颜色 | `void sgl_arc_label_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 背景填充色 |
| 设置圆角 | `void sgl_arc_label_set_radius(sgl_obj_t *obj, uint8_t radius)` | 圆角半径 |
| 设置文本对齐 | `void sgl_arc_label_set_text_align(sgl_obj_t *obj, sgl_align_type_t align)` | 对齐方式（如 `SGL_ALIGN_CENTER`） |
| 设置透明度 | `void sgl_arc_label_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置文本偏移 | `void sgl_arc_label_set_text_offset(sgl_obj_t *obj, int8_t offset_x, int8_t offset_y)` | 文本绘制偏移（X/Y 双向） |
| 设置旋转角度 | `void sgl_arc_label_set_angle(sgl_obj_t *obj, int16_t angle)` | 文本旋转角度（度，0~360） |
| 获取旋转角度 | `int16_t sgl_arc_label_get_angle(sgl_obj_t *obj)` | 返回当前角度 |
| 设置原始位置 | `void sgl_arc_label_set_orig_pos(sgl_obj_t *obj, int16_t x, int16_t y)` | 旋转前原始位置 |
| 设置原始尺寸 | `void sgl_arc_label_set_orig_size(sgl_obj_t *obj, int16_t w, int16_t h)` | 旋转前原始尺寸 |

> 💡 `set_orig_pos/set_orig_size` 用于记录旋转前的布局，配合 `set_angle` 实现围绕某点旋转。

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 普通弧形标签
    sgl_obj_t* alabel = sgl_arc_label_create(page);
    sgl_obj_set_pos(alabel, 50, 50);
    sgl_obj_set_size(alabel, 150, 40);
    sgl_arc_label_set_text(alabel, "Rotated Text");
    sgl_arc_label_set_font(alabel, &consolas23);
    sgl_arc_label_set_text_color(alabel, SGL_COLOR_CYAN);
    sgl_arc_label_set_angle(alabel, 30);          // 旋转 30 度

    // 动态更新（计数器旋转显示）
    sgl_obj_t* counter = sgl_arc_label_create(page);
    sgl_obj_set_pos(counter, 50, 120);
    sgl_obj_set_size(counter, 100, 40);
    sgl_arc_label_set_font(counter, &consolas23);
    sgl_arc_label_set_text_align(counter, SGL_ALIGN_CENTER);
    sgl_arc_label_set_text_fmt_dynamic(counter, "%d", 42);
    sgl_arc_label_set_angle(counter, 180);        // 旋转 180 度
```

![ArcLabel 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [ArcLabel 完整示例 sgl_arc_label_demo.c](SGL_DEMO/sgl_arc_label_demo.c) — 弧形标签全部接口演示

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_ARC_LABEL` 开关宏后编译运行。

---

## 6. 总结

- ✅ **旋转文本用 ArcLabel**：`set_angle()` 实现任意角度文本。
- ✅ **动态文本用 fmt**：频繁更新的文本用 `set_text_fmt_dynamic()` 或 `set_text_buffer()`。
- ✅ **始终设置字体**：避免断言失败。

---
> 📘 **相关头文件路径**  
> - ArcLabel API：`sgl/source/widgets/arc_label/sgl_arc_label.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
