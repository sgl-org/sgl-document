# SGL MsgBox 控件应用指南

## 1. 简介

`MsgBox`（消息框）是 SGL 中用于**弹出提示/确认对话框**的控件，包含标题、消息文本与左/右两个按钮（如 Yes/No）。  
在 **SGL（Simple Graphics Library）** 中，`MsgBox` 支持：
- 标题、消息文本及颜色、行间距、偏移
- 左/右按钮的文字、文字颜色、按钮颜色
- 主体/边框透明度、圆角、边框、背景图片
- **退出应答**：`set_exit_answer` 绑定指针，消息框销毁后指向用户点击的按钮文字
- `SGL_EVENT_RELEASED` / `SGL_EVENT_DESTROYED` 事件回调

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 点击左/右按钮后消息框自动销毁，可通过 `set_exit_answer` 获取结果
> - 适合确认对话框、提示框、操作确认等场景

---

## 2. 创建 MsgBox

使用 `sgl_msgbox_create()` 创建消息框控件：

```c
sgl_obj_t* sgl_msgbox_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* msgbox = sgl_msgbox_create(page);
sgl_obj_set_pos(msgbox, 30, 40);
sgl_obj_set_size(msgbox, 260, 180);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_msgbox_create()` 返回的对象指针。

### 3.1 外观

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置颜色 | `void sgl_msgbox_set_color(sgl_obj_t *obj, sgl_color_t color)` | 主体填充色 |
| 设置透明度 | `void sgl_msgbox_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 整体透明度 |
| 设置主体透明度 | `void sgl_msgbox_set_main_alpha(sgl_obj_t *obj, uint8_t alpha)` | 仅主体透明度 |
| 设置边框透明度 | `void sgl_msgbox_set_border_alpha(sgl_obj_t *obj, uint8_t alpha)` | 仅边框透明度 |
| 设置圆角 | `void sgl_msgbox_set_radius(sgl_obj_t *obj, uint8_t radius)` | 圆角半径 |
| 设置边框宽度 | `void sgl_msgbox_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |
| 设置边框颜色 | `void sgl_msgbox_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置背景图片 | `void sgl_msgbox_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 背景图指针 |
| 设置字体 | `void sgl_msgbox_set_font(sgl_obj_t *obj, const sgl_font_t *font)` | 全局字体（标题/消息/按钮） |

### 3.2 标题与消息

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置标题文字 | `void sgl_msgbox_set_title_text(sgl_obj_t *obj, const char *text)` | 标题文本 |
| 设置标题颜色 | `void sgl_msgbox_set_title_text_color(sgl_obj_t *obj, sgl_color_t color)` | 标题文字颜色 |
| 设置标题高度 | `void sgl_msgbox_set_title_height(sgl_obj_t *obj, uint8_t height)` | 标题栏高度 |
| 设置消息文本 | `void sgl_msgbox_set_msg_text(sgl_obj_t *obj, const char *text)` | 消息文本（支持 `\n`） |
| 设置消息颜色 | `void sgl_msgbox_set_msg_text_color(sgl_obj_t *obj, sgl_color_t color)` | 消息文字颜色 |
| 设置消息行间距 | `void sgl_msgbox_set_msg_line_margin(sgl_obj_t *obj, uint8_t margin)` | 消息行间距 |
| 设置消息 X 偏移 | `void sgl_msgbox_set_msg_x_offset(sgl_obj_t *obj, uint8_t offset)` | 消息水平偏移 |
| 设置消息 Y 偏移 | `void sgl_msgbox_set_msg_y_offset(sgl_obj_t *obj, uint8_t offset)` | 消息垂直偏移 |

### 3.3 按钮与应答

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置左按钮文字 | `void sgl_msgbox_set_left_btn_text(sgl_obj_t *obj, const char *text)` | 如 "Yes" |
| 设置左按钮文字颜色 | `void sgl_msgbox_set_left_btn_text_color(sgl_obj_t *obj, sgl_color_t color)` | 左按钮文字颜色 |
| 设置左按钮颜色 | `void sgl_msgbox_set_left_btn_color(sgl_obj_t *obj, sgl_color_t color)` | 左按钮背景色 |
| 设置右按钮文字 | `void sgl_msgbox_set_right_btn_text(sgl_obj_t *obj, const char *text)` | 如 "No" |
| 设置右按钮文字颜色 | `void sgl_msgbox_set_right_btn_text_color(sgl_obj_t *obj, sgl_color_t color)` | 右按钮文字颜色 |
| 设置右按钮颜色 | `void sgl_msgbox_set_right_btn_color(sgl_obj_t *obj, sgl_color_t color)` | 右按钮背景色 |
| 设置退出应答指针 | `void sgl_msgbox_set_exit_answer(sgl_obj_t *obj, const char **answer)` | 销毁后指向用户点击的按钮文字 |
| 获取当前按钮文字 | `const char* sgl_msgbox_get_current_btn(sgl_obj_t *obj)` | 返回当前按钮文字 |

---

## 4. 应用示例

```c
static const char *g_answer = NULL;          /* 保存用户点击的按钮文字 */

/* 消息框事件回调 — 按钮销毁后处理应答 */
static void msgbox_event_cb(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_RELEASED || e->type == SGL_EVENT_DESTROYED) {
        if (g_answer) {
            if (strcmp(g_answer, "Yes") == 0) {
                sgl_led_on(g_led);
            } else if (strcmp(g_answer, "No") == 0) {
                sgl_led_off(g_led);
            }
        }
        if (e->type == SGL_EVENT_DESTROYED && e->obj == g_msgbox) {
            g_msgbox = NULL;
        }
    }
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建消息框
    g_msgbox = sgl_msgbox_create(page);
    sgl_obj_set_pos(g_msgbox, 30, 40);
    sgl_obj_set_size(g_msgbox, 260, 180);

    sgl_msgbox_set_color(g_msgbox, SGL_COLOR_DARK_GRAY);
    sgl_msgbox_set_alpha(g_msgbox, 230);
    sgl_msgbox_set_main_alpha(g_msgbox, 220);
    sgl_msgbox_set_border_alpha(g_msgbox, 200);
    sgl_msgbox_set_radius(g_msgbox, 6);
    sgl_msgbox_set_border_width(g_msgbox, 1);
    sgl_msgbox_set_border_color(g_msgbox, SGL_COLOR_CYAN);
    sgl_msgbox_set_font(g_msgbox, &consolas14);

    // 标题与消息
    sgl_msgbox_set_title_text(g_msgbox, "LED Control");
    sgl_msgbox_set_title_text_color(g_msgbox, SGL_COLOR_WHITE);
    sgl_msgbox_set_title_height(g_msgbox, 30);
    sgl_msgbox_set_msg_text(g_msgbox, "Turn the LED on?\nYes = ON, No = OFF");
    sgl_msgbox_set_msg_text_color(g_msgbox, SGL_COLOR_WHITE);
    sgl_msgbox_set_msg_line_margin(g_msgbox, 4);
    sgl_msgbox_set_msg_x_offset(g_msgbox, 8);
    sgl_msgbox_set_msg_y_offset(g_msgbox, 6);

    // 按钮
    sgl_msgbox_set_left_btn_text(g_msgbox, "Yes");
    sgl_msgbox_set_left_btn_text_color(g_msgbox, SGL_COLOR_WHITE);
    sgl_msgbox_set_left_btn_color(g_msgbox, SGL_COLOR_DODGER_BLUE);
    sgl_msgbox_set_right_btn_text(g_msgbox, "No");
    sgl_msgbox_set_right_btn_text_color(g_msgbox, SGL_COLOR_WHITE);
    sgl_msgbox_set_right_btn_color(g_msgbox, SGL_COLOR_TOMATO);

    // 绑定退出应答指针：消息框销毁后 g_answer 指向被点击的按钮文字
    sgl_msgbox_set_exit_answer(g_msgbox, &g_answer);

    // 注册事件回调
    sgl_obj_set_event_cb(g_msgbox, msgbox_event_cb, NULL);
```

![MsgBox 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [MsgBox 完整示例 sgl_msgbox_demo.c](SGL_DEMO/sgl_msgbox_demo.c) — 消息框控制 LED + 外部按钮重新弹出 + 模拟点击 Yes/No

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_MSGBOX` 开关宏后编译运行。

---

## 6. 总结

- ✅ **退出应答取结果**：`set_exit_answer(&ptr)` 在销毁后给出用户选择。
- ✅ **销毁事件判状态**：`SGL_EVENT_DESTROYED` 中清空消息框引用。
- ✅ **可重新弹出**：销毁后用 `sgl_msgbox_create()` 重新创建。

---
> 📘 **相关头文件路径**  
> - MsgBox API：`sgl/source/widgets/msgbox/sgl_msgbox.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
