# SGL Box 控件应用指南

## 1. 简介

`Box`（盒子）是 SGL 中的**可滚动容器控件**，内部可容纳**超出自身尺寸**的子控件，并通过拖动/弹性滚动查看内容。  
在 **SGL（Simple Graphics Library）** 中，`Box` 支持：
- 容纳任意子控件（图片、按钮、长列表等）
- **垂直/水平弹性滚动**（`set_elastic_scroll` 设置四个方向的滚动范围）
- 滚动条显示控制（可分别开关垂直/水平滚动条）
- 背景色、圆角、边框、透明度、背景图片

> ⚠️ **重要特性**：
> - 是**容器控件**，可以包含子控件（不同于 leaf widget）
> - 子控件尺寸可大于 Box 尺寸，超出部分通过滚动查看
> - 适合长图片预览、可滚动面板、游戏场景等

---

## 2. 创建 Box

使用 `sgl_box_create()` 创建盒子控件：

```c
sgl_obj_t* sgl_box_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* box = sgl_box_create(page);
sgl_obj_set_pos(box, 20, 20);
sgl_obj_set_size(box, 200, 150);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_box_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置背景颜色 | `void sgl_box_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 背景填充色 |
| 设置圆角 | `void sgl_box_set_radius(sgl_obj_t *obj, uint8_t radius)` | 圆角半径 |
| 设置边框颜色 | `void sgl_box_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置边框宽度 | `void sgl_box_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |
| 设置背景图片 | `void sgl_box_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 背景图指针 |
| 设置弹性滚动范围 | `void sgl_box_set_elastic_scroll(sgl_obj_t *obj, int16_t up_limit, int16_t down_limit, int16_t left_limit, int16_t right_limit)` | 上下左右四个方向的可滚动距离（像素） |
| 设置滚动条颜色 | `void sgl_box_set_scrollbar_color(sgl_obj_t *obj, sgl_color_t color)` | 滚动条颜色 |
| 显示/隐藏滚动条 | `void sgl_box_set_show_scrollbar(sgl_obj_t *obj, uint8_t show_vertical, uint8_t show_horizontal)` | 分别控制垂直/水平滚动条 |
| 设置透明度 | `void sgl_box_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255，255 为不透明 |

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建可滚动 Box
    sgl_obj_t* box = sgl_box_create(page);
    sgl_obj_set_pos(box, 20, 20);
    sgl_obj_set_size(box, 200, 150);
    sgl_box_set_bg_color(box, SGL_COLOR_LIGHT_GRAY);
    sgl_box_set_radius(box, 8);
    sgl_box_set_border_color(box, SGL_COLOR_DARK_GRAY);
    sgl_box_set_border_width(box, 2);
    sgl_box_set_show_scrollbar(box, 1, 1);        // 垂直/水平滚动条都显示

    // 放入一个大于 Box 的图片（超出部分可滚动查看）
    sgl_obj_t* img = sgl_img_create(box);
    sgl_obj_set_pos(img, 0, 0);
    sgl_obj_set_size(img, 400, 300);              // 远大于 Box
    sgl_img_set_pixmap(img, &big_pixmap);

    // 设置弹性滚动范围（内容超出部分可滚动的距离）
    sgl_box_set_elastic_scroll(box, 0, 150, 0, 200);
```

![Box 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Box 完整示例 sgl_box_demo.c](SGL_DEMO/sgl_box_demo.c) — Box 可滚动容器全部接口演示

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_BOX` 开关宏后编译运行。

---

## 6. 总结

- ✅ **内容可超出容器**：子控件大于 Box 时用滚动查看。
- ✅ **弹性滚动需设范围**：`set_elastic_scroll` 决定可滚动距离。
- ✅ **滚动条可控制**：垂直/水平独立开关。

---
> 📘 **相关头文件路径**  
> - Box API：`sgl/source/widgets/box/sgl_box.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
