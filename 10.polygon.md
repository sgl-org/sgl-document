# SGL Polygon 控件应用指南

## 1. 简介

`Polygon`（多边形）是 SGL 中的**多边形绘制控件**，通过一组顶点坐标绘制任意多边形。  
在 **SGL（Simple Graphics Library）** 中，`Polygon` 支持：
- 通过**顶点数组/顶点坐标**定义形状（三角形、五边形、星形等）
- 填充颜色、边框颜色、边框宽度、透明度
- **文本**显示（居中于多边形）

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 顶点坐标是**相对控件自身左上角**的局部坐标
> - 最大顶点数为 `SGL_POLYGON_VERTEX_MAX`（16）
> - 适合标志绘制（如五角星）、图表区域、装饰图形等场景

---

## 2. 创建 Polygon

使用 `sgl_polygon_create()` 创建多边形控件：

```c
sgl_obj_t* sgl_polygon_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* poly = sgl_polygon_create(page);
sgl_obj_set_pos(poly, 20, 30);
sgl_obj_set_size(poly, 120, 120);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_polygon_create()` 返回的对象指针。

### 3.1 顶点设置（三种方式任选其一）

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置顶点坐标 | `void sgl_polygon_set_vertex_coords(sgl_obj_t *obj, const int16_t *x_coords, const int16_t *y_coords, uint16_t count)` | 分别传入 X 数组、Y 数组与顶点数 |
| 设置顶点数组 | `void sgl_polygon_set_vertex_array(sgl_obj_t *obj, const int16_t (*coords)[2], uint16_t count)` | 传入 `{x,y}` 二维数组 |
| 设置顶点结构体 | `void sgl_polygon_set_vertices(sgl_obj_t *obj, const sgl_polygon_pos_t *vertices, uint16_t count)` | 传入 `sgl_polygon_pos_t` 结构体数组 |

```c
typedef struct { int16_t x; int16_t y; } sgl_polygon_pos_t;
```

### 3.2 样式与文本

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置填充颜色 | `void sgl_polygon_set_fill_color(sgl_obj_t *obj, sgl_color_t color)` | 多边形填充色 |
| 设置边框颜色 | `void sgl_polygon_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 多边形边框色 |
| 设置边框宽度 | `void sgl_polygon_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度（0 = 无边框） |
| 设置透明度 | `void sgl_polygon_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | `alpha`：0~255 |
| 设置文本 | `void sgl_polygon_set_text(sgl_obj_t *obj, const char *text)` | 多边形内显示文本 |
| 设置字体 | `void sgl_polygon_set_font(sgl_obj_t *obj, const sgl_font_t *font)` | 文本字体 |
| 设置文本颜色 | `void sgl_polygon_set_text_color(sgl_obj_t *obj, sgl_color_t color)` | 文本颜色 |

> ⚠️ 头文件中 `sgl_polygon_set_pixmap()` 暂未支持（注释 "waiting for support"）。

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 三角形
    sgl_obj_t* duobian1 = sgl_polygon_create(page);
    sgl_obj_set_pos(duobian1, 20, 30);
    sgl_obj_set_size(duobian1, 120, 120);
    {
        int16_t x[] = {60, 10, 110};
        int16_t y[] = {10, 110, 110};
        sgl_polygon_set_vertex_coords(duobian1, x, y, 3);
    }
    sgl_polygon_set_fill_color(duobian1, SGL_COLOR_CYAN);
    sgl_polygon_set_border_color(duobian1, SGL_COLOR_NAVY);
    sgl_polygon_set_border_width(duobian1, 3);
    sgl_polygon_set_alpha(duobian1, 200);
    sgl_polygon_set_text(duobian1, "Tri");
    sgl_polygon_set_font(duobian1, &consolas14);
    sgl_polygon_set_text_color(duobian1, SGL_COLOR_BLACK);

    // 五边形
    sgl_obj_t* duobian2 = sgl_polygon_create(page);
    sgl_obj_set_pos(duobian2, 160, 30);
    sgl_obj_set_size(duobian2, 140, 120);
    {
        int16_t x[] = {70, 10, 30, 110, 130};
        int16_t y[] = {10, 45, 110, 110, 45};
        sgl_polygon_set_vertex_coords(duobian2, x, y, 5);
    }
    sgl_polygon_set_fill_color(duobian2, SGL_COLOR_GOLD);
    sgl_polygon_set_border_width(duobian2, 0);      // 无边框
    sgl_polygon_set_alpha(duobian2, 220);
    sgl_polygon_set_text(duobian2, "Pent");
    sgl_polygon_set_font(duobian2, &consolas14);
```

![Polygon 示例](imag/0/SGL_logo.png)

> 💡 星形绘制技巧：交替使用外半径/内半径（内半径为外半径 × 0.382）生成 10 个顶点，即可画出五角星（详见 SGL_DEMO 中 `create_star_polygon()`）。

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Polygon 完整示例 sgl_polygon_demo.c](SGL_DEMO/sgl_polygon_demo.c) — 三角形、五边形 + 五星红旗绘制

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_POLYGON` 开关宏后编译运行。

---

## 6. 总结

- ✅ **顶点坐标相对控件**：顶点以控件左上角为原点。
- ✅ **先定尺寸再设顶点**：顶点坐标按控件尺寸设计（如 120×120 内）。
- ✅ **支持文本标注**：可配合字体在多边形中心显示文字。

---
> 📘 **相关头文件路径**  
> - Polygon API：`sgl/source/widgets/polygon/sgl_polygon.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
