# SGL Keyboard 控件应用指南

## 1. 简介

`Keyboard`（键盘）是 SGL 中用于**文字输入**的软键盘控件，包含按键网格与输入缓冲区。  
在 **SGL（Simple Graphics Library）** 中，`Keyboard` 支持：
- 键盘主体与按键的双层样式（颜色、透明度、边框、圆角）
- 按键文字颜色/字体、按键背景图片
- **操作码读取**（`get_opcode`）与**输入缓冲区**（`set_textarea`）
- 按键事件回调（`SGL_EVENT_PRESSED`）

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 需先调用 `sgl_keyboard_set_textarea()` 绑定输入缓冲区，按键内容会写入该缓冲区
> - 关闭/销毁后可通过按钮重新调出（判断 `obj->destroyed`）
> - 适合登录输入、搜索框、文本编辑等场景

---

## 2. 创建 Keyboard

使用 `sgl_keyboard_create()` 创建键盘控件：

```c
sgl_obj_t* sgl_keyboard_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* kbd = sgl_keyboard_create(page);
sgl_obj_set_pos(kbd, 10, 60);
sgl_obj_set_size(kbd, 300, 200);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_keyboard_create()` 返回的对象指针。

### 3.1 键盘主体

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置颜色 | `void sgl_keyboard_set_color(sgl_obj_t *obj, sgl_color_t color)` | 键盘主体颜色 |
| 设置透明度 | `void sgl_keyboard_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 整体透明度 |
| 设置主体透明度 | `void sgl_keyboard_set_main_alpha(sgl_obj_t *obj, uint8_t alpha)` | 仅主体透明度 |
| 设置圆角 | `void sgl_keyboard_set_radius(sgl_obj_t *obj, uint8_t radius)` | 键盘圆角 |
| 设置背景图片 | `void sgl_keyboard_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 背景图（可传 NULL） |
| 设置边框颜色 | `void sgl_keyboard_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置边框宽度 | `void sgl_keyboard_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |
| 设置边框透明度 | `void sgl_keyboard_set_border_alpha(sgl_obj_t *obj, uint8_t alpha)` | 边框透明度 |

### 3.2 按键文字

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置文字颜色 | `void sgl_keyboard_set_text_color(sgl_obj_t *obj, sgl_color_t color)` | 按键文字颜色 |
| 设置文字字体 | `void sgl_keyboard_set_text_font(sgl_obj_t *obj, const sgl_font_t *font)` | 按键文字字体 |

### 3.3 按键样式

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置按钮圆角 | `void sgl_keyboard_set_btn_radius(sgl_obj_t *obj, uint8_t radius)` | 按键圆角 |
| 设置按钮透明度 | `void sgl_keyboard_set_btn_alpha(sgl_obj_t *obj, uint8_t alpha)` | 按键整体透明度 |
| 设置按钮主体透明度 | `void sgl_keyboard_set_btn_main_alpha(sgl_obj_t *obj, uint8_t alpha)` | 按键主体透明度 |
| 设置按钮颜色 | `void sgl_keyboard_set_btn_color(sgl_obj_t *obj, sgl_color_t color)` | 按键背景色 |
| 设置按钮背景图 | `void sgl_keyboard_set_btn_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 按键背景图（可传 NULL） |
| 设置按钮边框颜色 | `void sgl_keyboard_set_btn_border_color(sgl_obj_t *obj, sgl_color_t color)` | 按键边框颜色 |
| 设置按钮边框宽度 | `void sgl_keyboard_set_btn_border_width(sgl_obj_t *obj, uint8_t width)` | 按键边框宽度 |
| 设置按钮边框透明度 | `void sgl_keyboard_set_btn_border_alpha(sgl_obj_t *obj, uint8_t alpha)` | 按键边框透明度 |

### 3.4 输入与操作码

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置输入缓冲区 | `void sgl_keyboard_set_textarea(sgl_obj_t *obj, char *buffer, int buf_max_len)` | 绑定输入缓冲区，按键内容写入 |
| 获取操作码 | `uint8_t sgl_keyboard_get_opcode(sgl_obj_t *obj)` | 读取最近一次按键的操作码 |

---

## 4. 应用示例

```c
static char g_edit_buf[32];
static sgl_obj_t *g_jianpan = NULL;
static sgl_obj_t *g_parent = NULL;

/* 键盘事件回调：按键后刷新显示 */
static void keyboard_event(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_PRESSED) {
        sgl_obj_set_dirty(g_disp);        /* 触发重绘，显示缓冲区内容 */
    }
}

/* 创建并配置键盘 */
static void create_keyboard(void)
{
    if (g_parent == NULL) return;

    g_jianpan = sgl_keyboard_create(g_parent);
    sgl_obj_set_pos(g_jianpan, 10, 60);
    sgl_obj_set_size(g_jianpan, 300, 200);

    /* 键盘主体 */
    sgl_keyboard_set_color(g_jianpan, SGL_COLOR_DARK_GRAY);
    sgl_keyboard_set_alpha(g_jianpan, 230);
    sgl_keyboard_set_main_alpha(g_jianpan, 220);
    sgl_keyboard_set_radius(g_jianpan, 4);
    sgl_keyboard_set_border_color(g_jianpan, SGL_COLOR_CYAN);
    sgl_keyboard_set_border_width(g_jianpan, 1);
    sgl_keyboard_set_border_alpha(g_jianpan, 200);

    /* 按键文字与样式 */
    sgl_keyboard_set_text_color(g_jianpan, SGL_COLOR_WHITE);
    sgl_keyboard_set_text_font(g_jianpan, &consolas14);
    sgl_keyboard_set_btn_radius(g_jianpan, 4);
    sgl_keyboard_set_btn_alpha(g_jianpan, 230);
    sgl_keyboard_set_btn_main_alpha(g_jianpan, 220);
    sgl_keyboard_set_btn_color(g_jianpan, SGL_COLOR_NAVY);
    sgl_keyboard_set_btn_border_color(g_jianpan, SGL_COLOR_CYAN);
    sgl_keyboard_set_btn_border_width(g_jianpan, 1);
    sgl_keyboard_set_btn_border_alpha(g_jianpan, 200);

    /* 输入缓冲区与事件 */
    sgl_keyboard_set_textarea(g_jianpan, g_edit_buf, sizeof(g_edit_buf));
    sgl_obj_set_event_cb(g_jianpan, keyboard_event, NULL);
}

/* 调出键盘按钮回调 */
static void btn_open_kbd(sgl_event_t *e)
{
    (void)e;
    g_edit_buf[0] = '\0';
    if (g_jianpan == NULL || g_jianpan->destroyed) {
        create_keyboard();                /* 被销毁则重新创建 */
    } else {
        g_jianpan->hide = 0;              /* 未销毁则重新显示 */
    }
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);
    g_parent = page;

    // 输入显示区（文本框）
    g_disp = sgl_textbox_create(page);
    sgl_obj_set_pos(g_disp, 20, 15);
    sgl_obj_set_size(g_disp, 280, 36);
    sgl_textbox_set_text_font(g_disp, &consolas14);
    sgl_textbox_set_text_color(g_disp, SGL_COLOR_WHITE);
    sgl_textbox_set_bg_color(g_disp, SGL_COLOR_DARK_GRAY);
    sgl_textbox_set_text(g_disp, g_edit_buf);

    // 创建键盘
    create_keyboard();

    // 调出键盘按钮
    sgl_obj_t *btn = sgl_button_create(page);
    sgl_obj_set_pos(btn, 20, 275);
    sgl_obj_set_size(btn, 120, 32);
    sgl_button_set_text(btn, "Show KBD");
    sgl_button_set_font(btn, &consolas14);
    sgl_button_set_color(btn, SGL_COLOR_NAVY);
    sgl_button_set_text_color(btn, SGL_COLOR_WHITE);
    sgl_button_set_radius(btn, 4);
    sgl_obj_set_event_cb(btn, btn_open_kbd, NULL);
```

![Keyboard 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Keyboard 完整示例 sgl_keyboard_demo.c](SGL_DEMO/sgl_keyboard_demo.c) — 键盘输入实时显示 + 按钮重新调出键盘

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_KEYBOARD` 开关宏后编译运行。

---

## 6. 总结

- ✅ **先绑缓冲区**：`set_textarea()` 后才能接收输入。
- ✅ **双层样式**：键盘主体与按键各自独立配置。
- ✅ **可重新调出**：判断 `destroyed` 后重建或显示。

---
> 📘 **相关头文件路径**  
> - Keyboard API：`sgl/source/widgets/keyboard/sgl_keyboard.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
