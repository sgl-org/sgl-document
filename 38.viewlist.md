# SGL ViewList 控件应用指南

## 1. 简介

`ViewList`（视图列表）是 SGL 中用于**显示复杂子项列表**的滚动容器，每个列表项可以是任意控件（按钮、标签、图片等）。  
在 **SGL（Simple Graphics Library）** 中，`ViewList` 支持：
- **追加任意控件**作为列表项（`append_obj`）
- 列表项高度、边距（水平/垂直）
- 背景、边框、圆角、透明度
- 每个子项可独立注册事件回调

> ⚠️ **重要特性**：
> - 是**容器控件**，可容纳任意子控件作为列表项
> - 与 `TextList` 的区别：TextList 只支持纯文本项，ViewList 支持任意控件
> - 适合复杂列表、联系人列表、设置列表（带开关/按钮）等场景

---

## 2. 创建 ViewList

使用 `sgl_viewlist_create()` 创建视图列表控件：

```c
sgl_obj_t* sgl_viewlist_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* list = sgl_viewlist_create(page);
sgl_obj_set_pos(list, 20, 20);
sgl_obj_set_size(list, 280, 200);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_viewlist_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置圆角 | `void sgl_viewlist_set_radius(sgl_obj_t *obj, uint8_t radius)` | 圆角半径 |
| 设置背景颜色 | `void sgl_viewlist_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 背景填充色 |
| 设置透明度 | `void sgl_viewlist_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置边框宽度 | `void sgl_viewlist_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |
| 设置边框颜色 | `void sgl_viewlist_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置背景图片 | `void sgl_viewlist_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 背景图指针 |
| 设置列表项高度 | `void sgl_viewlist_set_item_height(sgl_obj_t *obj, uint16_t height)` | 每个列表项的高度 |
| 设置列表项边距 | `void sgl_viewlist_set_item_margin(sgl_obj_t *obj, uint8_t margin_x, uint8_t margin_y)` | 列表项水平/垂直边距 |
| 追加子项 | `void sgl_viewlist_append_obj(sgl_obj_t *list, sgl_obj_t *obj)` | 追加一个控件作为列表项 |

---

## 4. 应用示例

```c
/* 列表项点击回调 */
static void viewlist_click_cb(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_CLICKED) {
        sgl_obj_t *label = (sgl_obj_t *)sgl_event_get_data(e);
        if (label) {
            SGL_LOG_INFO("Clicked: %s", sgl_label_get_text(label));
        }
    }
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建视图列表
    sgl_obj_t *list = sgl_viewlist_create(page);
    sgl_obj_set_pos(list, 20, 20);
    sgl_obj_set_size(list, 280, 200);
    sgl_viewlist_set_radius(list, 4);
    sgl_viewlist_set_bg_color(list, SGL_COLOR_DARK_GRAY);
    sgl_viewlist_set_alpha(list, 220);
    sgl_viewlist_set_border_width(list, 1);
    sgl_viewlist_set_border_color(list, SGL_COLOR_CYAN);
    sgl_viewlist_set_item_height(list, 40);
    sgl_viewlist_set_item_margin(list, 4, 4);

    // 追加按钮作为列表项
    for (int i = 0; i < 6; i++) {
        char text[16];
        snprintf(text, sizeof(text), "Item %d", i + 1);
        sgl_obj_t *btn = sgl_button_create(list);
        sgl_button_set_text(btn, text);
        sgl_button_set_font(btn, &consolas14);
        sgl_button_set_color(btn, SGL_COLOR_NAVY);
        sgl_button_set_text_color(btn, SGL_COLOR_WHITE);
        sgl_button_set_radius(btn, 4);
        sgl_viewlist_append_obj(list, btn);          // 追加为列表项
    }
```

![ViewList 示例](imag/0/SGL_logo.png)

> 💡 也可追加标签并为其注册事件回调，实现"点击列表项"效果（详见头文件注释示例）。

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [ViewList 完整示例 sgl_viewlist_demo.c](SGL_DEMO/sgl_viewlist_demo.c) — 按钮列表项演示全部接口

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_VIEWLIST` 开关宏后编译运行。

---

## 6. 总结

- ✅ **任意子项**：`append_obj()` 追加任意控件。
- ✅ **点击交互**：给子项注册事件回调实现列表点击。
- ✅ **边距控制**：`set_item_height` + `set_item_margin` 布局列表。

---
> 📘 **相关头文件路径**  
> - ViewList API：`sgl/source/widgets/viewlist/sgl_viewlist.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
