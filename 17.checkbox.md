# SGL Checkbox 控件应用指南

## 1. 简介

`Checkbox`（复选框）是 SGL 中用于**多选**的交互控件，由勾选框 + 文本组成。  
在 **SGL（Simple Graphics Library）** 中，`Checkbox` 支持：
- 选中状态设置与读取（`true`=选中，`false`=未选中）
- 勾选框颜色、勾选标记颜色
- 文本、字体、文字颜色
- 圆角、透明度

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 点击可切换选中状态，配合事件回调实时响应
> - 适合多选项表单、设置项、任务清单等场景

---

## 2. 创建 Checkbox

使用 `sgl_checkbox_create()` 创建复选框控件：

```c
sgl_obj_t* sgl_checkbox_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* cb = sgl_checkbox_create(page);
sgl_obj_set_pos(cb, 20, 20);
sgl_obj_set_size(cb, 200, 30);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_checkbox_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置文本 | `void sgl_checkbox_set_text(sgl_obj_t *obj, const char *text)` | 复选框右侧文本 |
| 设置字体 | `void sgl_checkbox_set_font(sgl_obj_t *obj, const sgl_font_t *font)` | 文本字体（**必须设置**） |
| 设置文字颜色 | `void sgl_checkbox_set_text_color(sgl_obj_t *obj, sgl_color_t color)` | 文本颜色 |
| 设置框体颜色 | `void sgl_checkbox_set_box_color(sgl_obj_t *obj, sgl_color_t color)` | 勾选框颜色 |
| 设置勾选颜色 | `void sgl_checkbox_set_check_color(sgl_obj_t *obj, sgl_color_t color)` | 勾选标记颜色 |
| 设置圆角 | `void sgl_checkbox_set_radius(sgl_obj_t *obj, int16_t radius)` | 勾选框圆角 |
| 设置透明度 | `void sgl_checkbox_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置选中状态 | `void sgl_checkbox_set_status(sgl_obj_t *obj, bool status)` | `true`=选中，`false`=未选中 |
| 获取选中状态 | `bool sgl_checkbox_get_status(sgl_obj_t *obj)` | 返回当前状态 |

---

## 4. 应用示例

```c
/* 复选框点击回调 — 更新对应的状态标签 */
static void on_cb_click(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_CLICKED) {
        sgl_obj_t *cb = sgl_event_get_target(e);
        bool st = sgl_checkbox_get_status(cb);
        sgl_label_set_text_fmt_dynamic(g_cb_label, "Option: %s", st ? "ON" : "OFF");
    }
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 单个复选框
    sgl_obj_t* cb = sgl_checkbox_create(page);
    sgl_obj_set_pos(cb, 20, 20);
    sgl_obj_set_size(cb, 200, 30);
    sgl_checkbox_set_text(cb, "Demo Checkbox");
    sgl_checkbox_set_font(cb, &consolas14);
    sgl_checkbox_set_status(cb, true);                    // 默认选中
    sgl_checkbox_set_text_color(cb, SGL_COLOR_BLACK);
    sgl_checkbox_set_box_color(cb, SGL_COLOR_DARK_GRAY);
    sgl_checkbox_set_check_color(cb, SGL_COLOR_GREEN);
    sgl_checkbox_set_radius(cb, 3);
    sgl_checkbox_set_alpha(cb, 255);
    sgl_obj_set_event_cb(cb, on_cb_click, NULL);

    // 多选列表（多个复选框 + 状态标签）
    for (int i = 0; i < 4; i++) {
        sgl_obj_t* c = sgl_checkbox_create(page);
        sgl_obj_set_pos(c, 20, 60 + i * 30);
        sgl_obj_set_size(c, 150, 25);
        sgl_checkbox_set_font(c, &consolas14);
        sgl_checkbox_set_text(c, "Option");
        sgl_checkbox_set_status(c, (i == 0) ? true : false);
        sgl_checkbox_set_box_color(c, SGL_COLOR_DARK_GRAY);
        sgl_checkbox_set_check_color(c, SGL_COLOR_GREEN);
        sgl_obj_set_event_cb(c, on_cb_click, NULL);
    }
```

![Checkbox 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Checkbox 完整示例 sgl_checkbox_demo.c](SGL_DEMO/sgl_checkbox_demo.c) — 单个复选框 + 4 选项多选列表 + 实时状态标签

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_CHECKBOX` 开关宏后编译运行。

---

## 6. 总结

- ✅ **状态读写**：`set_status()` / `get_status()` 管理选中状态。
- ✅ **事件响应**：在 `SGL_EVENT_CLICKED` 中读取状态并更新 UI。
- ✅ **多选组合**：多个 Checkbox 组成选项列表。

---
> 📘 **相关头文件路径**  
> - Checkbox API：`sgl/source/widgets/checkbox/sgl_checkbox.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
