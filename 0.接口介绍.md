# SGL (Simple Graphics Library) 接口文档

## 1. 简介
![SGL_LOGO](imag/0/SGL_logo.png)
SGL (Simple Graphics Library) 是一个轻量级的嵌入式图形库，专为资源受限的设备设计。它提供了丰富的UI控件和高效的渲染机制，支持多种像素格式和硬件加速。


## 2. 核心API接口

### 2.1 初始化与主循环

#### sgl_init()

```c
int sgl_init(void);
```

SGL全局初始化函数。应在使用任何SGL功能之前调用此函数，并且应在注册帧缓冲设备之后调用。
**返回 `int`，需要检查返回值（0 表示成功，-1 表示失败）。**

#### sgl_tick_inc

```c
void sgl_tick_inc(uint32_t ms);
```

SGL动画系统的核心部分，用于追踪时间流逝**实时更新**：通常在系统1毫秒中断处理程序中调用，或者也可以通过轮询方式调用。

#### sgl_task_handler()

```c
void sgl_task_handler(void);
```
SGL任务处理函数。应定期在主循环、定时器或线程中调用，负责处理事件、动画和绘制任务。
**注意：旧版本的 `sgl_task_handle()` 已重命名为 `sgl_task_handler()`。**

另有 `sgl_task_handler_sync()` 可同步强制刷新一帧（常用于需要立即出图的场景）。

> ⚠️ <font color="#c00000">禁止将 sgl_task_handler() 与 sgl_tick_inc() 放在一起调用</font>，`sgl_tick_inc()` 应由 1ms 中断或独立低优先级轮询驱动。

#### sgl_fbdev_register()

```c
int sgl_fbdev_register(sgl_fbinfo_t *fbinfo);
```
注册帧缓冲设备。必须在调用 `sgl_init()` 之前调用此函数。
**注意：旧版本的 `sgl_device_fb_register(sgl_device_fb_t *)` 已改为 `sgl_fbdev_register(sgl_fbinfo_t *)`，结构体 `sgl_device_fb_t` 也更名为 `sgl_fbinfo_t`，刷新回调签名由 `(x1,y1,x2,y2,src)` 改为 `(sgl_area_t *area, sgl_color_t *src)`。**

参数:
```c
- fbinfo: 帧缓冲信息结构体指针（sgl_fbinfo_t）
```

`sgl_fbinfo_t` 结构体字段：
```c
typedef struct sgl_fbinfo {
    void      *buffer[SGL_DRAW_BUFFER_MAX];  // 帧缓冲（双缓冲时 buffer[0]/buffer[1]）
    uint32_t   buffer_size;                  // 帧缓冲字节数
    int16_t    xres;                         // 屏幕宽度
    int16_t    yres;                         // 屏幕高度
    void       (*flush_area)(sgl_area_t *area, sgl_color_t *src);  // 区域刷新回调
} sgl_fbinfo_t;
```

另有简化注册接口（适合单缓冲/简单场景）：
```c
int sgl_fbdev_register_dev(sgl_color_t *buffer0, sgl_color_t *buffer1, uint32_t buffer_size, uint16_t resolution_x, uint16_t resolution_y);
```

#### 初始化运用

```c
static void panel_flush_area(sgl_area_t *area, sgl_color_t *src)
{
    if (src == NULL || area == NULL)
        return;
    // 使用驱动的图片绘制函数将像素数据发送到屏幕
    //st7789v_spi_draw_picture_blocking(area->x1, area->y1, area->x2, area->y2, (st7789v_spi_color_t *)src);
    //更换为你的驱动屏幕函数
}

void app_main(void)
{
    sgl_fbinfo_t fbinfo = {0};

    fbinfo.xres = 320;
    fbinfo.yres = 240;
    fbinfo.buffer[0] = panel_buffer0;              // 帧缓冲0
    fbinfo.buffer[1] = panel_buffer1;              // 帧缓冲1（双缓冲可加速，单缓冲可为NULL）
    fbinfo.buffer_size = SGL_ARRAY_SIZE(panel_buffer0);
    fbinfo.flush_area = panel_flush_area;

    if (sgl_fbdev_register(&fbinfo) != 0) {
        // 注册失败处理
        return;
    }
    if (sgl_init() != 0) {
        // 初始化失败处理
        return;
    }

    // 1ms 中断或独立线程中调用（与主循环分开）
    // static void anim_tick_thread(void *arg) {
    //     while (1) {
    //         sgl_tick_inc(1);
    //         vTaskDelay(pdMS_TO_TICKS(1));
    //     }
    // }

    while (1) {
        sgl_task_handler();       // 事件 + 动画 + 绘制
    }
}
```

### 2.2 对象管理

#### sgl_obj_create()

```c
sgl_obj_t* sgl_obj_create(sgl_obj_t *parent);
```
创建一个新的对象。**如果 `parent` 为 NULL，会自动创建一个 Page（页面）对象。**

参数:
```c
- parent: 父对象指针
```
返回值:
- 新创建的对象指针

#### sgl_obj_delete()

```c
void sgl_obj_delete(sgl_obj_t *obj);
```
删除对象及其所有子对象。
- `obj` 为 NULL 或当前页面时：删除当前页面上的所有子对象（页面本身保留）。

参数:
```c
- obj: 要删除的对象指针
```
另有立即生效的 `sgl_obj_delete_sync()`。

#### sgl_obj_set_pos()

```c
void sgl_obj_set_pos(sgl_obj_t *obj, int16_t x, int16_t y);
```
设置对象的位置。

参数:
```c
- obj: 对象指针
- x: X坐标
- y: Y坐标
```

#### sgl_obj_set_size()

```c
void sgl_obj_set_size(sgl_obj_t *obj, int16_t width, int16_t height);
```
设置对象的尺寸。

参数:
```c
- obj: 对象指针
- width: 宽度
- height: 高度
```

#### 常用通用接口

```c
void sgl_obj_set_radius(sgl_obj_t *obj, size_t radius);          // 圆角半径
void sgl_obj_set_border_width(sgl_obj_t *obj, uint8_t border);   // 边框宽度
void sgl_obj_set_pos_align(sgl_obj_t *obj, sgl_align_type_t type); // 相对父对象对齐
void sgl_obj_set_event_cb(sgl_obj_t *obj, void (*event_fn)(sgl_event_t *e), void *data); // 事件回调
void sgl_obj_set_clickable(sgl_obj_t *obj);      // 启用点击
void sgl_obj_set_unclickable(sgl_obj_t *obj);    // 禁用点击
void sgl_obj_set_flexible(sgl_obj_t *obj);       // 弹性缩放（按下反馈）
void sgl_obj_set_visible(sgl_obj_t *obj);        // 显示
void sgl_obj_set_hidden(sgl_obj_t *obj);         // 隐藏
```

#### 布局管理（新接口）

```c
void sgl_obj_set_layout(sgl_obj_t *obj, sgl_layout_desc_t *desc);
```
**注意：传入布局描述结构体 `sgl_layout_desc_t`。**

```c
typedef struct sgl_layout_desc {
    sgl_layout_type_t type;      // SGL_LAYOUT_NONE / HORIZONTAL / VERTICAL / GRID
    int16_t col_num;             // 网格布局列数（GRID 时必填）
    int16_t row_num;             // 网格布局行数（GRID 时必填）
    int16_t col_space;           // 列间距
    int16_t row_space;           // 行间距
    int16_t left_space;          // 左边距
    int16_t right_space;         // 右边距
    int16_t top_space;           // 上边距
    int16_t bottom_space;        // 下边距
} sgl_layout_desc_t;
```

使用布局时**不要**再手动调用 `sgl_obj_set_size()/sgl_obj_set_pos()` 设置子控件位置，布局引擎会自动分配。

## 3. 控件接口

### 3.1 控件创建

```c
sgl_obj_t* sgl_xxxx_create(sgl_obj_t* parent);
```
创建对应控件。

相关设置函数:
```c
- sgl_xxxx_set_color(sgl_obj_t *obj, sgl_color_t color)  - 设置颜色
- sgl_xxxx_set_alpha(sgl_obj_t *obj, uint8_t alpha) - 设置透明度
- sgl_xxxx_set_radius(sgl_obj_t *obj, uint16_t radius) - 设置圆角半径
- sgl_xxxx_set_border_width(sgl_obj_t *obj, uint8_t width) - 设置边框宽度
- sgl_xxxx_set_border_color(sgl_obj_t *obj, sgl_color_t color) - 设置边框颜色
- sgl_xxxx_set_text(sgl_obj_t *obj, const char *text) - 设置文本
- sgl_xxxx_set_font(sgl_obj_t *obj, const sgl_font_t *font) - 设置字体
- sgl_xxxx_set_text_color(sgl_obj_t *obj, sgl_color_t color) - 设置文本颜色
- sgl_xxxx_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap) - 设置背景图
- sgl_xxxx_set_bg_color(sgl_obj_t *obj, sgl_color_t color) - 设置背景颜色
```
> ⚠️  xxxx为控件名，为了方便介绍这里写的比较笼统，里面有部分接口没有写上来，如设置文本偏移只有标签控件才有；上面列的东西大多数控件都有但也有少部分只支持部分接口。因此，你可以查看相应控件的文档本部分仅为介绍。

### 3.2 控件清单（最新版）

基础控件：
- `sgl_rect_create` 矩形容器
- `sgl_label_create` 标签（另见 `sgl_label_ext_create` 支持旋转）
- `sgl_button_create` 按钮
- `sgl_textline_create` 文本行
- `sgl_textbox_create` 文本框
- `sgl_img_create` 图片（支持多帧/GIF 顺序播放）
- `sgl_img_ext_create` 外部图片（读取外部 Flash/压缩图片，见第 5 讲）
- `sgl_icon_create` 图标
- `sgl_line_create` / `sgl_circle_create` / `sgl_ring_create` / `sgl_arc_create` / `sgl_polygon_create` / `sgl_rect_ext_create` 基础图形

交互控件：
- `sgl_slider_create` 滑动条
- `sgl_switch_create` 开关
- `sgl_checkbox_create` 复选框
- `sgl_dropdown_create` 下拉菜单
- `sgl_roller_create` 滚轮选择
- `sgl_keyboard_create` / `sgl_numberkbd_create` 键盘
- `sgl_msgbox_create` 消息框
- `sgl_win_create` 窗口
- `sgl_textlist_create` / `sgl_viewlist_create` / `sgl_launcher_create` / `sgl_filebrowser_create` 列表类

数据/显示控件：
- `sgl_progress_create` 进度条
- `sgl_bar_create` 条形图
- `sgl_barchart_create` / `sgl_linechart_create` / `sgl_piechart_create` 图表
- `sgl_gauge_create` 仪表盘
- `sgl_scope_create` 波形图
- `sgl_spectrum_create` 频谱
- `sgl_led_create` LED
- `sgl_battery_create` 电池
- `sgl_2dball_create` 2D 球
- `sgl_analogclock_create` 模拟时钟
- `sgl_statusbar_create` 状态栏
- `sgl_qrcode_create` 二维码
- `sgl_sprite_create` 精灵
- `sgl_canvas_create` 画布
- `sgl_box_create` 3D 盒子
- `sgl_timer_create` 定时器（components）



### 3.3 进度条控件 (Progress Bar)

```c
sgl_obj_t* sgl_progress_create(sgl_obj_t* parent);
```
创建进度条控件。

相关设置函数:
```c
- sgl_progress_set_color(sgl_obj_t *obj, sgl_color_t color)  - 设置进度条颜色
- sgl_progress_set_track_color(sgl_obj_t *obj, sgl_color_t color) - 设置轨道颜色
- sgl_progress_set_value(sgl_obj_t *obj, uint8_t value)  - 设置进度值
```

### 3.4 滑动条控件 (Slider)

```c
sgl_obj_t* sgl_slider_create(sgl_obj_t* parent);
```
创建滑动条控件。

### 3.5 开关控件 (Switch)

```c
sgl_obj_t* sgl_switch_create(sgl_obj_t* parent);
```
创建开关控件。

## 4. 事件处理

### 4.1 事件发送

```c
void sgl_event_send_pos(sgl_event_pos_t pos, sgl_event_type_t type);
void sgl_event_send_obj(struct sgl_obj *obj, sgl_event_type_t type);
```

触摸/按键底层输入（推荐）：
```c
void sgl_event_pos_input(int16_t x, int16_t y, bool flag);  // flag: true=按下, false=抬起
```

### 4.2 事件类型

```c
- SGL_EVENT_NULL         (0)    空事件
- SGL_EVENT_NORMAL       (1)    普通事件
- SGL_EVENT_PRESSED      (2)    按下事件
- SGL_EVENT_RELEASED     (3)    释放事件
- SGL_EVENT_CLICKED      (4)    点击事件（按下+释放）
- SGL_EVENT_LONG_CLICKED (5)    长按点击
- SGL_EVENT_MOTION       (6)    移动事件
- SGL_EVENT_LONG_PRESSED (11)   长按事件
- SGL_EVENT_FOCUSED      (14)   获得焦点
- SGL_EVENT_UNFOCUSED    (15)   失去焦点
- SGL_EVENT_KEY_UP/DOWN/LEFT/RIGHT/ESC (17~21) 按键事件
```

### 4.3 注册事件回调

```c
static void my_event_cb(sgl_event_t *event)
{
    if (event->type == SGL_EVENT_CLICKED) {
        // 处理点击
    }
}

sgl_obj_set_event_cb(btn, my_event_cb, NULL);  // 第三个参数为自定义数据
```
回调内可通过 `sgl_event_get_target(event)` / `sgl_event_get_data(event)` 获取事件目标和用户数据。

## 5. 动画系统

### 5.1 动画创建与管理

```c
sgl_anim_t* sgl_anim_create(void);
void sgl_anim_init(sgl_anim_t *anim);          // 静态初始化
void sgl_anim_delete(sgl_anim_t *anim);        // 删除动画对象
void sgl_anim_stop(sgl_anim_t *anim);          // 停止动画
```

### 5.2 动画设置

```c
void sgl_anim_set_data(sgl_anim_t *anim, void *data);                    // 绑定用户数据
void sgl_anim_set_act_duration(sgl_anim_t *anim, uint32_t duration_ms);  // 时长(ms)
void sgl_anim_set_act_delay(sgl_anim_t *anim, uint32_t delay_ms);        // 延时启动(ms)
void sgl_anim_set_start_value(sgl_anim_t *anim, int32_t start);          // 起始值
void sgl_anim_set_end_value(sgl_anim_t *anim, int32_t end);              // 结束值
void sgl_anim_set_path(sgl_anim_t *anim, sgl_anim_path_cb_t path_cb, sgl_anim_path_algo_t path_algo); // 路径回调+缓动算法
void sgl_anim_set_finish_cb(sgl_anim_t *anim, void (*finish_cb)(sgl_anim_t *anim)); // 完成回调
```


```c
void sgl_anim_start(sgl_anim_t *anim, uint32_t repeat_cnt);
// repeat_cnt: SGL_ANIM_REPEAT_ONCE(1)=播放一次, SGL_ANIM_REPEAT_LOOP=无限循环, n=播放n次
```

内置缓动算法（`sgl_anim_path_algo_t`）：
```c
SGL_ANIM_PATH_LINEAR            线性
SGL_ANIM_PATH_EASE_IN           缓入
SGL_ANIM_PATH_EASE_OUT          缓出
SGL_ANIM_PATH_EASE_IN_OUT       缓入缓出（默认推荐）
SGL_ANIM_PATH_OVERSHOOT         回弹过冲
SGL_ANIM_PATH_EASE_OUT_BACK     弹出回弹
SGL_ANIM_PATH_EASE_IN_OUT_SINE  正弦缓入缓出
SGL_ANIM_PATH_EASE_OUT_BOUNCE   落地弹跳
SGL_ANIM_PATH_SINE_WAVE         正弦波动（循环）
SGL_ANIM_PATH_DAMPED_SPRING     衰减弹簧
SGL_ANIM_PATH_STEP              阶梯跳变
```

### 5.3 便捷动画接口

```c
void sgl_anim_apply_obj_hori(sgl_obj_t *obj, int16_t distance, uint16_t duration, sgl_anim_path_algo_t effect);       // 水平移动
void sgl_anim_apply_obj_vert(sgl_obj_t *obj, int16_t distance, uint16_t duration, sgl_anim_path_algo_t effect);       // 垂直移动
void sgl_anim_run_once(int16_t start, int16_t end, uint16_t duration, sgl_anim_path_cb_t cb, sgl_anim_path_algo_t effect); // 一次性动画
```

### 5.4 动画示例

```c
static void move_path_cb(sgl_anim_t *anim, int32_t value)
{
    sgl_obj_t *obj = (sgl_obj_t *)anim->data;
    sgl_obj_set_pos(obj, value, sgl_obj_get_pos(obj).y);
}

sgl_anim_t *anim = sgl_anim_create();
sgl_anim_set_data(anim, btn);                       // 绑定要动画的对象
sgl_anim_set_act_duration(anim, 1000);              // 1秒
sgl_anim_set_start_value(anim, 0);
sgl_anim_set_end_value(anim, 200);
sgl_anim_set_path(anim, move_path_cb, SGL_ANIM_PATH_EASE_IN_OUT);
sgl_anim_start(anim, SGL_ANIM_REPEAT_ONCE);         // 播放一次
```

## 6. 日志系统

```c
void sgl_log(const char *level, const char *format, ...);
// 或使用宏：
SGL_LOG_INFO("value = %d\n", value);
SGL_LOG_WARN("warning\n");
SGL_LOG_ERROR("error\n");
```
日志输出需先注册输出设备：`sgl_logdev_register(puts)`（`CONFIG_SGL_DEBUG` 使能时生效）。

## 7. 相关 Demo

SGL 提供了覆盖全部控件的示例工程（`SGL_DEMO` 文件夹），可对照学习：

- [SGL_DEMO 示例总入口 sgl_demo.h](SGL_DEMO/sgl_demo.h) — Demo 开关宏与函数声明
- [SGL_DEMO 示例源码（.c 文件）](SGL_DEMO/) — 每个控件一个独立示例，如 `sgl_label_demo.c`、`sgl_button_demo.c` 等

> 💡 使用方式：在 `sgl_demo.h` 中将需要测试的 Demo 对应开关宏（如 `SGD_ENABLE_BUTTON`）设为 1，在工程中添加对应 `.c` 文件即可。
