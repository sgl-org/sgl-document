# SGL Roller 控件应用指南

## 1. 简介

`Roller`（滚轮选择器）是 SGL 中用于**滚动选择选项**的交互控件，类似 iOS 的滚轮（Picker）。  
在 **SGL（Simple Graphics Library）** 中，`Roller` 支持：
- 静态/动态两种选项设置方式
- **环形（infinite）模式**：滚到末尾自动接回开头
- 可见行数、选中项高亮颜色
- 背景、边框、圆角、透明度、文字样式
- 选中项索引/文本读取、事件回调

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 支持按键（上/下/左/右）与触摸两种交互方式
> - 适合日期/时间选择、数字调节、参数选择等场景

---

## 2. 创建 Roller

使用 `sgl_roller_create()` 创建滚轮选择器控件：

```c
sgl_obj_t* sgl_roller_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* roller = sgl_roller_create(page);
sgl_obj_set_pos(roller, 30, 30);
sgl_obj_set_size(roller, 120, 120);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_roller_create()` 返回的对象指针。

### 3.1 选项与选择

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置静态选项 | `void sgl_roller_set_option_static(sgl_obj_t *obj, const char *text)` | 用 `\n` 分隔；**不拷贝**，text 须持续有效 |
| 设置动态选项 | `void sgl_roller_set_option_dynamic(sgl_obj_t *obj, const char *text)` | 用 `\n` 分隔；**内部拷贝** |
| 获取选中索引 | `int sgl_roller_get_selected_index(sgl_obj_t *obj)` | 返回选中项索引（-1 为无选中） |
| 获取选中文本 | `bool sgl_roller_get_selected_text(sgl_obj_t *obj, char *buf, int buf_size)` | 拷贝选中文本到 buf，成功返回 true |
| 设置环形模式 | `void sgl_roller_set_infinite_mode(sgl_obj_t *obj, bool enable)` | `true` 为环形（滚到底接回开头） |

### 3.2 样式设置

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置可见行数 | `void sgl_roller_set_visible_rows(sgl_obj_t *obj, uint8_t rows)` | 可见选项行数 |
| 设置文字颜色 | `void sgl_roller_set_text_color(sgl_obj_t *obj, sgl_color_t color)` | 选项文字颜色 |
| 设置选中高亮 | `void sgl_roller_set_selected_color(sgl_obj_t *obj, sgl_color_t color)` | 选中项高亮背景色 |
| 设置背景颜色 | `void sgl_roller_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 背景填充色 |
| 设置边框颜色 | `void sgl_roller_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置字体 | `void sgl_roller_set_text_font(sgl_obj_t *obj, const sgl_font_t *font)` | 选项字体（**必须设置**） |
| 设置透明度 | `void sgl_roller_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置圆角 | `void sgl_roller_set_radius(sgl_obj_t *obj, int16_t radius)` | 圆角半径 |
| 设置边框宽度 | `void sgl_roller_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |

---

## 4. 应用示例

```c
/* 滚轮事件回调 — 实时显示选中项 */
static void roller_event_cb(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_PRESSED || e->type == SGL_EVENT_RELEASED ||
        e->type == SGL_EVENT_MOVE_DOWN || e->type == SGL_EVENT_MOVE_UP ||
        e->type == SGL_EVENT_KEY_UP || e->type == SGL_EVENT_KEY_DOWN ||
        e->type == SGL_EVENT_KEY_LEFT || e->type == SGL_EVENT_KEY_RIGHT) {
        char buf[32];
        int idx = sgl_roller_get_selected_index(e->obj);
        if (sgl_roller_get_selected_text(e->obj, buf, sizeof(buf))) {
            sgl_label_set_text_fmt_dynamic(g_label, "Selected: [%d] %s", idx, buf);
        }
    }
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建滚轮（动态选项）
    sgl_obj_t* gunlun = sgl_roller_create(page);
    sgl_obj_set_pos(gunlun, 30, 30);
    sgl_obj_set_size(gunlun, 120, 120);
    sgl_roller_set_option_dynamic(gunlun, "Red\nGreen\nBlue\nYellow\nPink\nCyan");
    sgl_roller_set_visible_rows(gunlun, 5);
    sgl_roller_set_text_color(gunlun, SGL_COLOR_WHITE);
    sgl_roller_set_selected_color(gunlun, SGL_COLOR_NAVY);
    sgl_roller_set_bg_color(gunlun, SGL_COLOR_DARK_GRAY);
    sgl_roller_set_border_color(gunlun, SGL_COLOR_CYAN);
    sgl_roller_set_text_font(gunlun, &consolas14);
    sgl_roller_set_alpha(gunlun, 220);
    sgl_roller_set_radius(gunlun, 4);
    sgl_roller_set_border_width(gunlun, 1);
    sgl_obj_set_event_cb(gunlun, roller_event_cb, g_label);

    // 环形滚轮（静态选项）
    sgl_obj_t* gunlun2 = sgl_roller_create(page);
    sgl_obj_set_pos(gunlun2, 170, 30);
    sgl_obj_set_size(gunlun2, 120, 120);
    sgl_roller_set_option_static(gunlun2, "A\nB\nC\nD\nE\nF");
    sgl_roller_set_infinite_mode(gunlun2, true);       // 环形模式
    sgl_roller_set_visible_rows(gunlun2, 3);
    sgl_roller_set_text_color(gunlun2, SGL_COLOR_GOLD);
    sgl_roller_set_selected_color(gunlun2, SGL_COLOR_GRAY);
    sgl_roller_set_bg_color(gunlun2, SGL_COLOR_DARK_GRAY);
    sgl_roller_set_text_font(gunlun2, &consolas23);
    sgl_obj_set_event_cb(gunlun2, roller_event_cb, g_label2);
```

![Roller 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Roller 完整示例 sgl_roller_demo.c](SGL_DEMO/sgl_roller_demo.c) — 普通滚轮 + 环形滚轮 + 实时选中标签

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_ROLLER` 开关宏后编译运行。

---

## 6. 总结

- ✅ **环形模式**：`set_infinite_mode(true)` 实现循环选择。
- ✅ **事件响应**：在 PRESSED/MOVE/KEY 事件中读取选中项。
- ✅ **按键可用**：支持上下左右方向键操作。

---
> 📘 **相关头文件路径**  
> - Roller API：`sgl/source/widgets/roller/sgl_roller.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
