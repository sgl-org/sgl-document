# SGL IMG 控件应用指南

## 1. 简介

`IMG`（图片）是 SGL 中用于**显示图片**的基础图片控件。  
在 **SGL（Simple Graphics Library）** 中，`IMG` 支持：
- 设置图片数据（`sgl_pixmap_t`）
- 透明度设置
- **多帧（GIF 风格）顺序播放**：通过 `set_pixmap_num` 设置图片数量，用 `set_pixmap_index` 切换当前显示帧
- 配合 SGL 定时器或动画系统实现动画播放

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 多帧图片是一组连续存储的图片（帧），通过 Index 控制显示顺序（第一、第二、第三…）
> - 若需读取**外部 Flash/压缩图片**，请使用 `IMG_EXT` 控件（见第 5 讲）
> - 适合图标、背景图、简单动画等场景

---

## 2. 创建 IMG

使用 `sgl_img_create()` 创建图片控件：

```c
sgl_obj_t* sgl_img_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* img = sgl_img_create(page);
sgl_obj_set_pos(img, 50, 30);
sgl_obj_set_size(img, 64, 64);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_img_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置图片数据 | `void sgl_img_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | `pixmap`：图片像素图指针 |
| 设置透明度 | `void sgl_img_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | `alpha`：0~255，255 为完全不透明 |
| 设置图片数量 | `void sgl_img_set_pixmap_num(sgl_obj_t *obj, uint16_t num, bool loop)` | 多帧图片帧数；`loop` 是否循环 |
| 设置当前帧索引 | `void sgl_img_set_pixmap_index(sgl_obj_t *obj, uint16_t index)` | 切换到第几帧（0 起） |

> 💡 图片数据为 `sgl_pixmap_t` 结构（含宽、高、格式、位图指针），多帧图片的每一帧按序存放。

---

## 4. 应用示例

```c
    extern const sgl_pixmap_t pic1_pixmap;   // 图片资源（工程 images.c 提供）

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 单帧图片
    sgl_obj_t* tupian = sgl_img_create(page);
    sgl_obj_set_pos(tupian, (SGL_SCREEN_WIDTH - 120) / 2, 30);
    sgl_obj_set_size(tupian, 64, 64);
    sgl_img_set_pixmap(tupian, &pic1_pixmap);
    sgl_img_set_alpha(tupian, 220);

    // 第二个图片（多帧 GIF 风格）
    sgl_obj_t* tupian2 = sgl_img_create(page);
    sgl_obj_set_pos(tupian2, (SGL_SCREEN_WIDTH - 80) / 2, 180);
    sgl_obj_set_size(tupian2, 80, 80);
    sgl_img_set_pixmap(tupian2, &pic1_pixmap);
    sgl_img_set_alpha(tupian2, 180);
    sgl_img_set_pixmap_num(tupian2, 1, false);   // 1 帧，不循环
    sgl_img_set_pixmap_index(tupian2, 0);        // 显示第 0 帧
```

![IMG 示例](imag/0/SGL_logo.png)

> 💡 多帧动画：可用 SGL 定时器（`sgl_timer_create`）周期性调用 `sgl_img_set_pixmap_index()` 切换帧，即可实现逐帧动画播放。

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [IMG 完整示例 sgl_img_demo.c](SGL_DEMO/sgl_img_demo.c) — 单帧/多帧图片 + 透明度演示；文件注释中亦说明了 `img_ext` 外部图片控件设计思路

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_IMG` 开关宏后编译运行。

---

## 6. 总结

- ✅ **先设图再显示**：`set_pixmap()` 指定图片数据。
- ✅ **多帧播放**：`set_pixmap_num()` + `set_pixmap_index()` 配合定时器实现动画。
- ✅ **外部图片用 IMG_EXT**：需要读 Flash/压缩图片时改用 `sgl_img_ext`。

---
> 📘 **相关头文件路径**  
> - IMG API：`sgl/source/widgets/img/sgl_img.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
