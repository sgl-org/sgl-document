# SGL 3DVortex 控件应用指南

## 1. 简介

`3DVortex`（3D 粒子漩涡）是 SGL 中**用于展示 3D 粒子动画**的控件，内部模拟数百个粒子围绕中心旋转形成漩涡效果。  
在 **SGL（Simple Graphics Library）** 中，`3DVortex` 支持：
- 运行/停止控制
- 拖尾（trail）透明度
- 粒子系统重置

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 粒子数量上限 `SGL_VORTEX_MAX_PARTICLES`（150），初始粒子数 `SGL_VORTEX_INIT_PARTICLES`（150）
> - 动画由控件内部驱动（`running` 状态控制），无需外部定时器
> - 建议配深色背景衬底，效果更佳
> - 适合开机动画、加载特效、装饰背景等场景

---

## 2. 创建 3DVortex

使用 `sgl_3dvortex_create()` 创建 3D 粒子漩涡控件：

```c
sgl_obj_t* sgl_3dvortex_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* vortex = sgl_3dvortex_create(page);
sgl_obj_set_pos(vortex, 0, 0);
sgl_obj_set_size(vortex, 60, 60);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_3dvortex_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置运行状态 | `void sgl_3dvortex_set_running(sgl_obj_t *obj, bool running)` | `true` 启动粒子动画，`false` 停止 |
| 设置拖尾透明度 | `void sgl_3dvortex_set_trail_alpha(sgl_obj_t *obj, uint8_t alpha)` | 粒子拖尾透明度 |
| 重置粒子系统 | `void sgl_3dvortex_reset(sgl_obj_t *obj)` | 重新初始化粒子位置 |

> 💡 常量：`SGL_VORTEX_MAX_PARTICLES`（150）、`SGL_VORTEX_INIT_PARTICLES`（150）、`SGL_VORTEX_TRAIL_ALPHA`（38 默认拖尾透明度）。

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 深色背景衬底（增强漩涡视觉效果）
    sgl_obj_t *bg = sgl_rect_create(page);
    sgl_obj_set_pos(bg, 0, 0);
    sgl_obj_set_size(bg, SGL_SCREEN_WIDTH, SGL_SCREEN_HEIGHT);
    sgl_rect_set_color(bg, sgl_rgb(0x08, 0x08, 0x12));
    sgl_rect_set_alpha(bg, 255);

    // 创建 3D 粒子漩涡
    sgl_obj_t *xuanwo = sgl_3dvortex_create(page);
    sgl_obj_set_pos(xuanwo, 0, 0);
    sgl_obj_set_size(xuanwo, 60, 60);

    /* 启动粒子动画 */
    sgl_3dvortex_set_running(xuanwo, true);

    /* 设置拖尾透明度 */
    sgl_3dvortex_set_trail_alpha(xuanwo, 38);

    /* 重置粒子系统（重新初始化粒子位置） */
    sgl_3dvortex_reset(xuanwo);
```

![3DVortex 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [3DVortex 完整示例 sgl_3dvortex_demo.c](SGL_DEMO/sgl_3dvortex_demo.c) — 深色背景 + 粒子漩涡动画

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_3DVORTEX` 开关宏后编译运行。

---

## 6. 总结

- ✅ **开箱即动**：`set_running(true)` 即可播放动画。
- ✅ **拖尾可调**：`set_trail_alpha()` 控制拖尾浓度。
- ✅ **可重置**：`reset()` 重新初始化粒子。
- ✅ **配深色背景**：效果最佳。

---
> 📘 **相关头文件路径**  
> - 3DVortex API：`sgl/source/components/3dvortex/sgl_3dvortex.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
