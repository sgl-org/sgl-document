# SGL Bar 控件应用指南

## 1. 简介

`Bar`（条形图）是 SGL 中用于**直观显示数值大小**的控件，类似进度条但更偏重数据展示。  
在 **SGL（Simple Graphics Library）** 中，`Bar` 支持：
- 数值设置（0~127）与读取
- **水平 / 垂直**两种方向
- 填充色、轨道色、圆角、边框、透明度
- 背景图片

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 值范围 0~127（比 Progress 的 0~100 精度更高）
> - 适合音量条、柱状数据、电量条等场景

---

## 2. 创建 Bar

使用 `sgl_bar_create()` 创建条形图控件：

```c
sgl_obj_t* sgl_bar_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* bar = sgl_bar_create(page);
sgl_obj_set_pos(bar, 20, 20);
sgl_obj_set_size(bar, 280, 24);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_bar_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置值 | `void sgl_bar_set_value(sgl_obj_t *obj, uint8_t value)` | 当前值（0~127） |
| 获取值 | `uint8_t sgl_bar_get_value(sgl_obj_t *obj)` | 返回当前值 |
| 设置填充颜色 | `void sgl_bar_set_fill_color(sgl_obj_t *obj, sgl_color_t color)` | 填充（已填充部分）颜色 |
| 设置轨道颜色 | `void sgl_bar_set_track_color(sgl_obj_t *obj, sgl_color_t color)` | 轨道（未填充部分）颜色 |
| 设置方向 | `void sgl_bar_set_direct(sgl_obj_t *obj, uint8_t direct)` | `SGL_DIRECT_HORIZONTAL`(0) / `SGL_DIRECT_VERTICAL`(1) |
| 设置圆角 | `void sgl_bar_set_radius(sgl_obj_t *obj, uint8_t radius)` | 圆角半径 |
| 设置边框宽度 | `void sgl_bar_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |
| 设置边框颜色 | `void sgl_bar_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置背景图片 | `void sgl_bar_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 背景图指针（接口待评估，可能移除） |
| 设置透明度 | `void sgl_bar_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |

> 💡 方向常量定义于 `sgl_types.h`：`SGL_DIRECT_HORIZONTAL` = 0，`SGL_DIRECT_VERTICAL` = 1。

---

## 4. 应用示例

```c
static sgl_obj_t *g_bar_anim = NULL;
static sgl_obj_t *g_bar_label = NULL;

/* 动画路径回调 — 更新条形图值和标签 */
static void bar_anim_path(sgl_anim_t *anim, int32_t value)
{
    sgl_bar_set_value(g_bar_anim, (uint8_t)value);
    sgl_label_set_text_fmt_dynamic(g_bar_label, "%d%%", (int)value);
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 基础条形图
    sgl_obj_t* bar = sgl_bar_create(page);
    sgl_obj_set_pos(bar, 20, 20);
    sgl_obj_set_size(bar, 280, 24);
    sgl_bar_set_value(bar, 80);                        // 0~127
    sgl_bar_set_fill_color(bar, SGL_COLOR_RED);
    sgl_bar_set_track_color(bar, SGL_COLOR_TOMATO);
    sgl_bar_set_direct(bar, SGL_DIRECT_HORIZONTAL);
    sgl_bar_set_radius(bar, 4);
    sgl_bar_set_border_width(bar, 1);
    sgl_bar_set_alpha(bar, 200);

    // 动画条形图（0→100 循环）
    g_bar_anim = sgl_bar_create(page);
    sgl_obj_set_pos(g_bar_anim, 20, 60);
    sgl_obj_set_size(g_bar_anim, 280, 30);
    sgl_bar_set_value(g_bar_anim, 0);
    sgl_bar_set_fill_color(g_bar_anim, SGL_COLOR_GOLD);
    sgl_bar_set_track_color(g_bar_anim, SGL_COLOR_DARK_GRAY);
    sgl_bar_set_radius(g_bar_anim, 4);

    g_bar_label = sgl_label_create(page);              // 叠加百分比标签
    sgl_obj_set_pos(g_bar_label, 20, 60);
    sgl_obj_set_size(g_bar_label, 280, 30);
    sgl_label_set_font(g_bar_label, &consolas14);
    sgl_label_set_text(g_bar_label, "0%");
    sgl_label_set_text_color(g_bar_label, SGL_COLOR_WHITE);
    sgl_label_set_text_align(g_bar_label, SGL_ALIGN_CENTER);

    sgl_anim_t *anim = sgl_anim_create();
    sgl_anim_set_data(anim, NULL);
    sgl_anim_set_act_duration(anim, 2000);
    sgl_anim_set_start_value(anim, 0);
    sgl_anim_set_end_value(anim, 100);
    sgl_anim_set_path(anim, bar_anim_path, SGL_ANIM_PATH_LINEAR);
    sgl_anim_start(anim, SGL_ANIM_REPEAT_LOOP);
```

![Bar 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Bar 完整示例 sgl_bar_demo.c](SGL_DEMO/sgl_bar_demo.c) — 全接口条形图 + 动画循环条形图

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_BAR` 开关宏后编译运行。

---

## 6. 总结

- ✅ **0~127 高精度**：比 Progress（0~100）精度更高。
- ✅ **方向可切换**：`SGL_DIRECT_HORIZONTAL` / `SGL_DIRECT_VERTICAL`。
- ✅ **动效靠动画**：动画回调中 `set_value()` 即可实现增长动效。

---
> 📘 **相关头文件路径**  
> - Bar API：`sgl/source/widgets/bar/sgl_bar.h`  
> - 方向常量：`sgl/source/include/sgl_types.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
