# SGL Dropdown 控件应用指南

## 1. 简介

`Dropdown`（下拉菜单）是 SGL 中用于**从选项列表中选择一项**的交互控件。  
在 **SGL（Simple Graphics Library）** 中，`Dropdown` 支持：
- 选项的添加、删除（按文本/按索引）、静态/动态两种存储方式
- 展开时可见行数控制、选中项高亮颜色
- 背景、边框、圆角、透明度、文字样式
- 选中项索引/文本读取、`SGL_EVENT_CLICKED` 事件回调

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 静态选项（`set_option_static`）不拷贝字符串，省内存；动态选项（`set_option_dynamic`）内部拷贝，可增删
> - 适合设置选择、筛选器、模式切换等场景

---

## 2. 创建 Dropdown

使用 `sgl_dropdown_create()` 创建下拉菜单控件：

```c
sgl_obj_t* sgl_dropdown_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* dd = sgl_dropdown_create(page);
sgl_obj_set_pos(dd, 20, 20);
sgl_obj_set_size(dd, 280, 40);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_dropdown_create()` 返回的对象指针。

### 3.1 选项管理

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置静态选项 | `void sgl_dropdown_set_option_static(sgl_obj_t *obj, const char *text)` | 用 `\n` 分隔选项；**不拷贝**，text 须持续有效 |
| 设置动态选项 | `void sgl_dropdown_set_option_dynamic(sgl_obj_t *obj, const char *text)` | 用 `\n` 分隔选项；**内部拷贝**，可增删 |
| 追加选项 | `void sgl_dropdown_add_option(sgl_obj_t *obj, const char *text)` | 追加单个选项 |
| 按文本删除 | `void sgl_dropdown_delete_option_by_text(sgl_obj_t *obj, const char *text)` | 删除指定文本的选项 |
| 按索引删除 | `void sgl_dropdown_delete_option_by_index(sgl_obj_t *obj, int index)` | 删除指定索引的选项 |
| 获取选中索引 | `int sgl_dropdown_get_selected_index(sgl_obj_t *obj)` | 返回选中项索引（-1 为无选中） |
| 获取选中文本 | `bool sgl_dropdown_get_selected_text(sgl_obj_t *obj, char *buf, int buf_size)` | 拷贝选中文本到 buf，成功返回 true |

### 3.2 样式设置

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置可见行数 | `void sgl_dropdown_set_visible_rows(sgl_obj_t *obj, int16_t rows)` | 展开时最多可见行数 |
| 设置背景颜色 | `void sgl_dropdown_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 背景填充色 |
| 设置边框宽度 | `void sgl_dropdown_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |
| 设置边框颜色 | `void sgl_dropdown_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置选中高亮 | `void sgl_dropdown_set_selected_color(sgl_obj_t *obj, sgl_color_t color)` | 选中项高亮背景色 |
| 设置圆角 | `void sgl_dropdown_set_radius(sgl_obj_t *obj, uint8_t radius)` | 圆角半径 |
| 设置透明度 | `void sgl_dropdown_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置文字颜色 | `void sgl_dropdown_set_text_color(sgl_obj_t *obj, sgl_color_t color)` | 选项文字颜色 |
| 设置文字字体 | `void sgl_dropdown_set_text_font(sgl_obj_t *obj, const sgl_font_t *font)` | 选项字体（**必须设置**） |

> 💡 禁用下拉：`sgl_obj_set_unclickable(dd)` + 低透明度，可得到"只读"效果。

---

## 4. 应用示例

```c
/* 选中事件回调 — 显示当前选中项 */
static void dd_event(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_CLICKED) {
        sgl_obj_t *obj = (sgl_obj_t *)e->obj;
        int idx = sgl_dropdown_get_selected_index(obj);
        char buf[32];
        if (sgl_dropdown_get_selected_text(obj, buf, sizeof(buf))) {
            sgl_label_set_text_fmt_dynamic(g_info, "Selected: [%d] %s", idx, buf);
        }
    }
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建下拉菜单
    sgl_obj_t* dd = sgl_dropdown_create(page);
    sgl_obj_set_pos(dd, 20, 20);
    sgl_obj_set_size(dd, 280, 40);
    sgl_dropdown_set_text_font(dd, &consolas24);
    sgl_dropdown_set_text_color(dd, SGL_COLOR_GRAY);
    sgl_dropdown_set_bg_color(dd, SGL_COLOR_DARK_GRAY);
    sgl_dropdown_set_border_color(dd, SGL_COLOR_GRAY);
    sgl_dropdown_set_border_width(dd, 1);
    sgl_dropdown_set_radius(dd, 4);
    sgl_dropdown_set_selected_color(dd, SGL_COLOR_NAVY);
    sgl_dropdown_set_visible_rows(dd, 5);

    // 设置选项（动态文本，可增删）
    sgl_dropdown_set_option_dynamic(dd,
        "Option A\nOption B\nOption C\nOption D\nOption E");
    sgl_dropdown_add_option(dd, "Option F");       // 追加
    sgl_dropdown_delete_option_by_text(dd, "Option C");  // 按文本删除
    sgl_dropdown_delete_option_by_index(dd, 1);    // 按索引删除

    // 注册事件回调
    sgl_obj_set_event_cb(dd, dd_event, NULL);
```

![Dropdown 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Dropdown 完整示例 sgl_dropdown_demo.c](SGL_DEMO/sgl_dropdown_demo.c) — 完整功能下拉 + 只读禁用下拉 + 选中信息标签

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_DROPDOWN` 开关宏后编译运行。

---

## 6. 总结

- ✅ **静态省内存，动态可增删**：根据需求选择 `set_option_static` / `set_option_dynamic`。
- ✅ **事件响应**：在 `SGL_EVENT_CLICKED` 中用 `get_selected_index/text` 读取结果。
- ✅ **只读效果**：`sgl_obj_set_unclickable()` + 低透明度。

---
> 📘 **相关头文件路径**  
> - Dropdown API：`sgl/source/widgets/dropdown/sgl_dropdown.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
