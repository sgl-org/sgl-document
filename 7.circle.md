# SGL Circle 控件应用指南

## 1. 简介

`Circle`（圆形）是 SGL 中最基础的**图形绘制控件**，用于绘制实心圆形。  
在 **SGL（Simple Graphics Library）** 中，`Circle` 支持：
- 填充颜色、透明度、半径设置
- **背景图片**（pixmap）
- 边框颜色、边框宽度
- 圆心 X/Y 偏移（微调圆在控件内的位置）

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 控件尺寸（`sgl_obj_set_size`）与圆半径（`sgl_circle_set_radius`）相互独立，半径决定圆大小
> - 适合圆点、指示灯、球体基础、装饰图形等场景

---

## 2. 创建 Circle

使用 `sgl_circle_create()` 创建圆形控件：

```c
sgl_obj_t* sgl_circle_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* circle = sgl_circle_create(page);
sgl_obj_set_pos(circle, 50, 60);
sgl_obj_set_size(circle, 120, 120);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_circle_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置填充颜色 | `void sgl_circle_set_color(sgl_obj_t *obj, sgl_color_t color)` | `color`：圆填充色（如 `SGL_COLOR_CYAN`） |
| 设置半径 | `void sgl_circle_set_radius(sgl_obj_t *obj, uint16_t radius)` | `radius`：圆半径（像素） |
| 设置透明度 | `void sgl_circle_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | `alpha`：0~255，255 为完全不透明 |
| 设置背景图片 | `void sgl_circle_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | `pixmap`：背景图指针（可裁剪为圆形） |
| 设置边框颜色 | `void sgl_circle_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | `color`：圆边框颜色 |
| 设置边框宽度 | `void sgl_circle_set_border_width(sgl_obj_t *obj, uint8_t width)` | `width`：圆边框宽度（像素） |
| 设置圆心 X 偏移 | `void sgl_circle_set_x_offset(sgl_obj_t *obj, int8_t offset)` | 圆心相对控件中心的 X 偏移（可为负） |
| 设置圆心 Y 偏移 | `void sgl_circle_set_y_offset(sgl_obj_t *obj, int8_t offset)` | 圆心相对控件中心的 Y 偏移（可为负） |

> 💡 通过 X/Y 偏移可实现偏心圆（如仪表刻度、太阳光照效果）。

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 第一个圆：带背景图 + 边框
    sgl_obj_t* yuan1 = sgl_circle_create(page);
    sgl_obj_set_pos(yuan1, (SGL_SCREEN_WIDTH - 120) / 2, 60);
    sgl_obj_set_size(yuan1, 120, 120);
    sgl_circle_set_color(yuan1, SGL_COLOR_CYAN);
    sgl_circle_set_radius(yuan1, 50);
    sgl_circle_set_alpha(yuan1, 200);
    sgl_circle_set_pixmap(yuan1, &pic1_pixmap);        // 背景图片
    sgl_circle_set_border_color(yuan1, SGL_COLOR_NAVY);
    sgl_circle_set_border_width(yuan1, 6);

    // 第二个圆：纯色 + 圆心偏移
    sgl_obj_t* yuan2 = sgl_circle_create(page);
    sgl_obj_set_pos(yuan2, (SGL_SCREEN_WIDTH - 120) / 2, 210);
    sgl_obj_set_size(yuan2, 120, 120);
    sgl_circle_set_color(yuan2, SGL_COLOR_GOLD);
    sgl_circle_set_radius(yuan2, 50);
    sgl_circle_set_x_offset(yuan2, 13);                // 圆心右移
    sgl_circle_set_y_offset(yuan2, -5);                // 圆心上移
    sgl_circle_set_border_color(yuan2, SGL_COLOR_BROWN);
    sgl_circle_set_border_width(yuan2, 4);
```

![Circle 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Circle 完整示例 sgl_circle_demo.c](SGL_DEMO/sgl_circle_demo.c) — 两个圆形演示全部 `sgl_circle_xxx` 接口

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_CIRCLE` 开关宏后编译运行。

---

## 6. 总结

- ✅ **半径与尺寸分离**：圆的大小由 `sgl_circle_set_radius()` 决定。
- ✅ **图片圆形裁剪**：设置 pixmap 后图片按圆形区域裁剪显示。
- ✅ **偏心圆用偏移**：X/Y offset 微调圆心位置。

---
> 📘 **相关头文件路径**  
> - Circle API：`sgl/source/widgets/circle/sgl_circle.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
