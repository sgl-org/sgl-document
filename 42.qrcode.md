# SGL QRCode 控件应用指南

## 1. 简介

`QRCode`（二维码）是 SGL 中用于**生成并显示二维码**的控件，内嵌 QR 码生成算法，无需外部图片资源。  
在 **SGL（Simple Graphics Library）** 中，`QRCode` 支持：
- 二维码内容设置（URL、文本等，扫码可识别）
- 版本（1~5）与**纠错等级**（ECC 0~3）
- 格子颜色、背景色、格子圆角
- 边距（zone）、缩放（scale）
- **中心 Logo** 及 Logo 圆角

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 二维码内容填入 URL 即可被扫码软件识别跳转
> - 版本上限由 `CONFIG_SGL_QRCODE_QR_VERSION_MAX` 控制（默认 5）
> - 适合支付码、设备配网、链接分享等场景

---

## 2. 创建 QRCode

使用 `sgl_qrcode_create()` 创建二维码控件：

```c
sgl_obj_t* sgl_qrcode_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* qr = sgl_qrcode_create(page);
sgl_obj_set_pos(qr, 50, 15);
sgl_obj_set_size(qr, 140, 140);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_qrcode_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置版本 | `void sgl_qrcode_set_version(sgl_obj_t *obj, uint8_t version)` | 二维码版本（1~5，越大容量越大） |
| 设置纠错等级 | `void sgl_qrcode_set_ecc(sgl_obj_t *obj, uint8_t ecc)` | 纠错等级（0~3，越高容错越强） |
| 设置内容 | `void sgl_qrcode_set_text(sgl_obj_t *obj, const char *text)` | 二维码内容（URL/文本） |
| 设置透明度 | `void sgl_qrcode_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置背景颜色 | `void sgl_qrcode_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 背景色（通常白色） |
| 设置格子颜色 | `void sgl_qrcode_set_cell_color(sgl_obj_t *obj, sgl_color_t color)` | 格子颜色（通常黑色） |
| 设置格子圆角 | `void sgl_qrcode_set_cell_radius(sgl_obj_t *obj, uint8_t radius)` | 格子圆角半径 |
| 设置边距 | `void sgl_qrcode_set_zone(sgl_obj_t *obj, uint8_t zone)` | 二维码四周留白 |
| 设置缩放 | `void sgl_qrcode_set_scale(sgl_obj_t *obj, uint8_t scale)` | 格子放大倍数 |
| 设置中心 Logo | `void sgl_qrcode_set_logo(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 中心 Logo 图片 |
| 设置 Logo 圆角 | `void sgl_qrcode_set_logo_radius(sgl_obj_t *obj, int16_t radius)` | Logo 圆角半径 |

> 💡 默认常量：`SGL_QRCODE_ZONE_DEFAULT`（边距默认 1）、`SGL_QRCODE_SCALE_DEFAULT`（缩放默认 4）。

---

## 4. 应用示例

```c
    extern const sgl_pixmap_t pic1_pixmap;   /* Logo 图片资源 */

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建二维码（带 Logo）
    sgl_obj_t *erweima = sgl_qrcode_create(page);
    sgl_obj_set_pos(erweima, (SGL_SCREEN_WIDTH - 140) / 2, 15);
    sgl_obj_set_size(erweima, 140, 140);
    sgl_qrcode_set_version(erweima, 5);          // 版本 5
    sgl_qrcode_set_ecc(erweima, 2);              // 纠错等级 2
    sgl_qrcode_set_text(erweima, "https://sgl-docs.readthedocs.io");  // 内容 URL
    sgl_qrcode_set_alpha(erweima, 240);
    sgl_qrcode_set_bg_color(erweima, SGL_COLOR_WHITE);
    sgl_qrcode_set_cell_color(erweima, SGL_COLOR_BLACK);
    sgl_qrcode_set_cell_radius(erweima, 2);
    sgl_qrcode_set_zone(erweima, 2);             // 边距
    sgl_qrcode_set_scale(erweima, 3);            // 缩放
    sgl_qrcode_set_logo(erweima, &pic1_pixmap);  // 中心 Logo
    sgl_qrcode_set_logo_radius(erweima, 4);

    // 第二个二维码（不带 Logo）
    erweima = sgl_qrcode_create(page);
    sgl_obj_set_pos(erweima, (SGL_SCREEN_WIDTH - 140) / 2, 175);
    sgl_obj_set_size(erweima, 140, 140);
    sgl_qrcode_set_version(erweima, 5);
    sgl_qrcode_set_ecc(erweima, 2);
    sgl_qrcode_set_text(erweima, "hello sgl");
    sgl_qrcode_set_alpha(erweima, 240);
    sgl_qrcode_set_bg_color(erweima, SGL_COLOR_WHITE);
    sgl_qrcode_set_cell_color(erweima, SGL_COLOR_BLACK);
    sgl_qrcode_set_cell_radius(erweima, 2);
    sgl_qrcode_set_zone(erweima, 2);
    sgl_qrcode_set_scale(erweima, 3);
```

![QRCode 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [QRCode 完整示例 sgl_qrcode_demo.c](SGL_DEMO/sgl_qrcode_demo.c) — 带 Logo / 不带 Logo 两个二维码

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_QRCODE` 开关宏后编译运行。

---

## 6. 总结

- ✅ **内容即生成**：`set_text()` 填入 URL/文本即可生成二维码。
- ✅ **版本与纠错**：`set_version()` + `set_ecc()` 平衡容量与容错。
- ✅ **Logo 美化**：`set_logo()` 添加中心图标。
- ✅ **白底黑格**：背景白色 + 格子黑色保证可扫性。

---
> 📘 **相关头文件路径**  
> - QRCode API：`sgl/source/widgets/qrcode/sgl_qrcode.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
