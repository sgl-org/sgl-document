# SGL TextList 控件应用指南

## 1. 简介

`TextList`（文本列表）是 SGL 中用于**显示纯文本选项列表**的控件，用户可通过上下键/触摸选择一项。  
在 **SGL（Simple Graphics Library）** 中，`TextList` 支持：
- 文本项的添加、删除（按索引/按文本）
- 文字颜色、选中项高亮颜色
- 背景、边框、圆角、透明度、字体
- 选中项文本/索引读取

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 列表项为纯文本（轻量），若需复杂子项（按钮、标签等）请用 `ViewList`
> - 适合菜单列表、设置项列表、选项选择等场景

---

## 2. 创建 TextList

使用 `sgl_textlist_create()` 创建文本列表控件：

```c
sgl_obj_t* sgl_textlist_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* list = sgl_textlist_create(page);
sgl_obj_set_pos(list, 20, 20);
sgl_obj_set_size(list, 280, 200);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_textlist_create()` 返回的对象指针。

### 3.1 列表项管理

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 添加列表项 | `void sgl_textlist_add_item(sgl_obj_t *obj, char *text)` | 追加一个文本项 |
| 获取选中文本 | `char* sgl_textlist_get_selected_text(sgl_obj_t *obj)` | 返回选中项文本指针 |
| 获取选中索引 | `int16_t sgl_textlist_get_selected_index(sgl_obj_t *obj)` | 返回选中项索引 |
| 按索引删除 | `void sgl_textlist_delete_item_by_index(sgl_obj_t *obj, int16_t index)` | 删除指定索引项 |
| 按文本删除 | `void sgl_textlist_delete_item_by_text(sgl_obj_t *obj, char *text)` | 删除指定文本项 |

### 3.2 样式设置

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置圆角 | `void sgl_textlist_set_radius(sgl_obj_t *obj, uint8_t radius)` | 圆角半径 |
| 设置文字颜色 | `void sgl_textlist_set_text_color(sgl_obj_t *obj, sgl_color_t color)` | 列表项文字颜色 |
| 设置选中颜色 | `void sgl_textlist_set_selected_color(sgl_obj_t *obj, sgl_color_t color)` | 选中项高亮背景色 |
| 设置边框颜色 | `void sgl_textlist_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置边框宽度 | `void sgl_textlist_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |
| 设置背景颜色 | `void sgl_textlist_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 背景填充色 |
| 设置字体 | `void sgl_textlist_set_text_font(sgl_obj_t *obj, const sgl_font_t *font)` | 列表项字体（**必须设置**） |
| 设置背景图片 | `void sgl_textlist_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 背景图指针 |
| 设置透明度 | `void sgl_textlist_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建文本列表
    sgl_obj_t* wenben = sgl_textlist_create(page);
    sgl_obj_set_pos(wenben, 20, 20);
    sgl_obj_set_size(wenben, 280, 200);
    sgl_textlist_set_radius(wenben, 4);
    sgl_textlist_set_text_color(wenben, SGL_COLOR_WHITE);
    sgl_textlist_set_selected_color(wenben, SGL_COLOR_NAVY);
    sgl_textlist_set_border_color(wenben, SGL_COLOR_CYAN);
    sgl_textlist_set_border_width(wenben, 1);
    sgl_textlist_set_bg_color(wenben, SGL_COLOR_DARK_GRAY);
    sgl_textlist_set_text_font(wenben, &consolas14);
    sgl_textlist_set_alpha(wenben, 220);

    // 添加列表项
    sgl_textlist_add_item(wenben, "Item 1");
    sgl_textlist_add_item(wenben, "Item 2");
    sgl_textlist_add_item(wenben, "Item 3");
    sgl_textlist_add_item(wenben, "Item 4");
    sgl_textlist_add_item(wenben, "Item 5");

    // 读取选中项
    char *sel = sgl_textlist_get_selected_text(wenben);
    int16_t idx = sgl_textlist_get_selected_index(wenben);

    // 删除项
    sgl_textlist_delete_item_by_index(wenben, 4);
    sgl_textlist_delete_item_by_text(wenben, "Item 3");
```

![TextList 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [TextList 完整示例 sgl_textlist_demo.c](SGL_DEMO/sgl_textlist_demo.c) — 文本列表全部接口演示

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_TEXTLIST` 开关宏后编译运行。

---

## 6. 总结

- ✅ **纯文本列表**：轻量高效，适合菜单/选项。
- ✅ **选中反馈**：`set_selected_color()` 高亮当前项。
- ✅ **增删灵活**：`add_item` / `delete_item_by_index` / `delete_item_by_text`。

---
> 📘 **相关头文件路径**  
> - TextList API：`sgl/source/widgets/textlist/sgl_textlist.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
