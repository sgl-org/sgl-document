# SGL Canvas 控件应用指南

## 1. 简介

`Canvas`（画布）是 SGL 中的**自定义绘制控件**，通过注册**绘制回调函数**，用户可自由使用底层绘制 API 在画布上绘制任意内容。  
在 **SGL（Simple Graphics Library）** 中，`Canvas` 支持：
- 注册用户绘制回调（`sgl_canvas_set_painter_cb`）
- 携带用户私有数据（`sgl_canvas_set_private`）
- 回调中可调用任意 `sgl_draw_*` 底层绘制函数（直线、矩形、圆、文字等）

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，但通过回调获得最大的绘制自由度
> - 绘制回调在控件需要重绘（dirty）时被调用
> - 适合自定义图形、仪表盘刻度、图表绘制等需要底层绘制的场景

---

## 2. 创建 Canvas

使用 `sgl_canvas_create()` 创建画布控件：

```c
sgl_obj_t* sgl_canvas_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），可为 `NULL`（创建为顶层）

示例：
```c
sgl_obj_t* canvas = sgl_canvas_create(page);
sgl_obj_set_size(canvas, 200, 150);
sgl_obj_set_pos(canvas, 20, 20);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_canvas_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置绘制回调 | `void sgl_canvas_set_painter_cb(sgl_obj_t *obj, sgl_painter_cb_t painter)` | `painter`：用户绘制函数，签名见下 |
| 设置私有数据 | `void sgl_canvas_set_private(sgl_obj_t *obj, void *priv)` | `priv`：用户自定义数据，回调中可获取 |

### 绘制回调类型

```c
typedef void (*sgl_painter_cb_t)(sgl_surf_t *surf, sgl_area_t *area, sgl_obj_t *obj);
```

回调参数：
- `surf`：绘制表面（含目标缓冲、裁剪区域）
- `area`：需要重绘的区域
- `obj`：画布对象（可通过 `obj->...` 访问尺寸，或用 `sgl_canvas_get_private` 获取私有数据）

---

## 4. 应用示例

```c
/* 绘制回调：在画布上画一条水平线和一条对角线 */
static void painter_func(sgl_surf_t *surf, sgl_area_t *area, sgl_obj_t *obj)
{
    /* 在画布范围内绘制 */
    sgl_draw_fill_hline(surf, area, obj->coords.y1 + 10, obj->coords.x1, obj->coords.x2, 5, SGL_COLOR_BLACK, 255);
    sgl_draw_line(surf, area, obj->coords.x1, obj->coords.y1, obj->coords.x2, obj->coords.y2, 2, SGL_COLOR_BLUE, 255);
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建画布并注册回调
    sgl_obj_t* canvas = sgl_canvas_create(page);
    sgl_obj_set_size(canvas, 200, 150);
    sgl_obj_set_pos(canvas, 20, 20);
    sgl_canvas_set_painter_cb(canvas, painter_func);
    sgl_canvas_set_private(canvas, my_user_data);   // 可选：绑定用户数据
```

![Canvas 示例](imag/0/SGL_logo.png)

> 💡 可用的底层绘制函数（`sgl_draw.h`）：`sgl_draw_fill_hline`（水平线）、`sgl_draw_line`（直线）、`sgl_draw_rect`（矩形）、`sgl_draw_circle`（圆）、`sgl_draw_text`（文字）等。

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Canvas 完整示例 sgl_canvas_demo.c](SGL_DEMO/sgl_canvas_demo.c) — 画布自定义绘制演示

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_CANVAS` 开关宏后编译运行。

---

## 6. 总结

- ✅ **自由绘制靠回调**：`set_painter_cb` 注册自定义绘制函数。
- ✅ **用 obj->coords 定位**：回调内通过 `obj->coords.x1/y1/x2/y2` 获取画布范围。
- ✅ **私有数据传参**：`set_private` 绑定业务数据，回调内获取。

---
> 📘 **相关头文件路径**  
> - Canvas API：`sgl/source/widgets/canvas/sgl_canvas.h`  
> - 底层绘制 API：`sgl/source/draw/sgl_draw.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
