# SGL Slider 控件应用指南

## 1. 简介

`Slider`（滑动条）是 SGL 中用于**数值调节**的交互控件，用户可通过拖动滑块（knob）设置 0~100 的值。  
在 **SGL（Simple Graphics Library）** 中，`Slider` 支持：
- **水平 / 垂直**两种方向
- 轨道色、填充色（已滑过部分）、滑块（knob）颜色
- 圆角、粗细（thickness）、边框
- 值读取/设置、事件回调（拖动实时反馈）

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 滑块大小由控件**高度**决定（knob_r = 高度 / 2 - 1，高度越大球越大）
> - 适合音量调节、亮度调节、参数设置等场景

---

## 2. 创建 Slider

使用 `sgl_slider_create()` 创建滑动条控件：

```c
sgl_obj_t* sgl_slider_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* slider = sgl_slider_create(page);
sgl_obj_set_pos(slider, 20, 20);
sgl_obj_set_size(slider, 280, 30);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_slider_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置值 | `void sgl_slider_set_value(sgl_obj_t *obj, uint8_t value)` | 当前值（0~100） |
| 获取值 | `uint8_t sgl_slider_get_value(sgl_obj_t *obj)` | 返回当前值 |
| 设置方向 | `void sgl_slider_set_direct(sgl_obj_t *obj, uint8_t direct)` | `SGL_DIRECT_HORIZONTAL`(0) / `SGL_DIRECT_VERTICAL`(1) |
| 设置填充颜色 | `void sgl_slider_set_fill_color(sgl_obj_t *obj, sgl_color_t color)` | 已滑过部分颜色 |
| 设置轨道颜色 | `void sgl_slider_set_track_color(sgl_obj_t *obj, sgl_color_t color)` | 未滑过部分颜色 |
| 设置滑块颜色 | `void sgl_slider_set_knob_color(sgl_obj_t *obj, sgl_color_t color)` | 拖动球颜色 |
| 设置圆角 | `void sgl_slider_set_radius(sgl_obj_t *obj, uint8_t radius)` | 圆角半径 |
| 设置粗细 | `void sgl_slider_set_thickness(sgl_obj_t *obj, uint8_t thickness)` | 轨道粗细（越大轨道越粗） |
| 设置边框宽度 | `void sgl_slider_set_border_width(sgl_obj_t *obj, uint8_t width)` | 边框宽度 |

> 💡 方向常量定义于 `sgl_types.h`：`SGL_DIRECT_HORIZONTAL` = 0，`SGL_DIRECT_VERTICAL` = 1。

---

## 4. 应用示例

```c
static sgl_obj_t *g_val_label = NULL;

/* 滑动条值变化回调 — 更新标签显示 */
static void on_slider_event(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_PRESSED || e->type == SGL_EVENT_RELEASED ||
        e->type == SGL_EVENT_MOVE_DOWN || e->type == SGL_EVENT_MOVE_UP ||
        e->type == SGL_EVENT_MOVE_LEFT || e->type == SGL_EVENT_MOVE_RIGHT)
    {
        sgl_obj_t *sl = sgl_event_get_target(e);
        uint8_t val = sgl_slider_get_value(sl);
        sgl_label_set_text_fmt_dynamic(g_val_label, "Slider: %d", (int)val);
    }
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 水平滑动条
    sgl_obj_t* sl = sgl_slider_create(page);
    sgl_obj_set_pos(sl, 20, 20);
    sgl_obj_set_size(sl, 280, 30);
    sgl_slider_set_direct(sl, SGL_DIRECT_HORIZONTAL);
    sgl_slider_set_radius(sl, 4);
    sgl_slider_set_thickness(sl, 10);          // 轨道粗细
    sgl_slider_set_border_width(sl, 1);
    sgl_slider_set_value(sl, 50);
    sgl_obj_set_event_cb(sl, on_slider_event, NULL);

    // 值显示标签
    g_val_label = sgl_label_create(page);
    sgl_obj_set_pos(g_val_label, 20, 60);
    sgl_obj_set_size(g_val_label, 200, 24);
    sgl_label_set_font(g_val_label, &consolas14);
    sgl_label_set_text_fmt_dynamic(g_val_label, "Slider: %d", 50);

    // 垂直滑动条
    sl = sgl_slider_create(page);
    sgl_obj_set_pos(sl, 320, 20);
    sgl_obj_set_size(sl, 20, 160);
    sgl_slider_set_direct(sl, SGL_DIRECT_VERTICAL);
    sgl_slider_set_thickness(sl, 6);
    sgl_slider_set_value(sl, 70);
    sgl_slider_set_fill_color(sl, SGL_COLOR_LIME);
    sgl_slider_set_track_color(sl, SGL_COLOR_DARK_GRAY);
    sgl_slider_set_knob_color(sl, SGL_COLOR_YELLOW);
    sgl_slider_set_radius(sl, 4);
```

![Slider 示例](imag/0/SGL_logo.png)

> 💡 也可通过按钮加减值：`sgl_slider_get_value()` 读取后加减再 `sgl_slider_set_value()` 写回（详见 SGL_DEMO 的 -/+ 按钮）。

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Slider 完整示例 sgl_slider_demo.c](SGL_DEMO/sgl_slider_demo.c) — 水平/垂直滑动条 + 加减按钮控制 + 实时标签

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_SLIDER` 开关宏后编译运行。

---

## 6. 总结

- ✅ **方向可切换**：`SGL_DIRECT_HORIZONTAL` / `SGL_DIRECT_VERTICAL`。
- ✅ **滑块大小由高度决定**：改控件高度即可调节滑块大小。
- ✅ **拖动实时反馈**：在 PRESSED/MOVE 事件中读值更新 UI。

---
> 📘 **相关头文件路径**  
> - Slider API：`sgl/source/widgets/slider/sgl_slider.h`  
> - 方向常量：`sgl/source/include/sgl_types.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
