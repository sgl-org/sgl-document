# SGL Statusbar 控件应用指南

## 1. 简介

`Statusbar`（状态栏）是 SGL 中用于**顶部/底部信息展示**的控件，通过"槽位（Slot）"机制在左右两侧排列文字信息。  
在 **SGL（Simple Graphics Library）** 中，`Statusbar` 支持：
- **左右两侧槽位**的添加、设置、删除、查询
- 每个槽位独立的文本、颜色、透明度
- 槽位间距（slot_space）、左右边距（slot_margin）
- 背景色、背景透明度、背景圆角、统一字体

> ⚠️ **重要特性**：
> - 是**容器控件**，可挂载子控件（如把 Battery 作为右侧子控件）
> - 槽位是**按索引管理的文本位**，左右各自独立编号（从 0 起）
> - 适合状态栏、标题栏、信息展示条等场景

---

## 2. 创建 Statusbar

使用 `sgl_statusbar_create()` 创建状态栏控件：

```c
sgl_obj_t* sgl_statusbar_create(sgl_obj_t *parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* bar = sgl_statusbar_create(page);
sgl_obj_set_pos(bar, 0, 0);
sgl_obj_set_size(bar, SGL_SCREEN_WIDTH, 26);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_statusbar_create()` 返回的对象指针。

### 3.1 背景与字体

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置字体 | `void sgl_statusbar_set_font(sgl_obj_t *obj, const sgl_font_t *font)` | 左右槽位文本共用字体 |
| 设置背景颜色 | `void sgl_statusbar_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 背景填充色 |
| 设置背景透明度 | `void sgl_statusbar_set_bg_alpha(sgl_obj_t *obj, uint8_t alpha)` | 背景透明度 |
| 设置背景圆角 | `void sgl_statusbar_set_bg_radius(sgl_obj_t *obj, uint8_t radius)` | 背景圆角（贴边时设 0） |
| 设置槽位间距 | `void sgl_statusbar_set_slot_space(sgl_obj_t *obj, uint8_t space)` | 相邻槽位之间的间距 |
| 设置左右边距 | `void sgl_statusbar_set_slot_margin(sgl_obj_t *obj, uint8_t left, uint8_t right)` | 左侧、右侧边距 |

### 3.2 左侧槽位

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 添加左侧槽位 | `void sgl_statusbar_add_left_slot(sgl_obj_t *obj, uint8_t index, char* slot)` | 在指定索引添加文本槽位 |
| 设置左侧槽位 | `void sgl_statusbar_set_left_slot(sgl_obj_t *obj, uint8_t index, char* slot)` | 更新指定索引的文本 |
| 删除左侧槽位 | `void sgl_statusbar_remove_left_slot(sgl_obj_t *obj, uint8_t index)` | 删除指定索引槽位 |
| 设置槽位透明度 | `void sgl_statusbar_set_left_slot_alpha(sgl_obj_t *obj, uint8_t index, uint8_t alpha)` | 指定槽位透明度 |
| 设置槽位颜色 | `void sgl_statusbar_set_left_slot_color(sgl_obj_t *obj, uint8_t index, sgl_color_t color)` | 指定槽位颜色 |
| 查询槽位索引 | `int16_t sgl_statusbar_get_left_slot_index(sgl_obj_t *obj, char *slot)` | 按文本查找槽位索引 |

### 3.3 右侧槽位

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 添加右侧槽位 | `void sgl_statusbar_add_right_slot(sgl_obj_t *obj, uint8_t index, char* slot)` | 在指定索引添加文本槽位 |
| 设置右侧槽位 | `void sgl_statusbar_set_right_slot(sgl_obj_t *obj, uint8_t index, char* slot)` | 更新指定索引的文本 |
| 删除右侧槽位 | `void sgl_statusbar_remove_right_slot(sgl_obj_t *obj, uint8_t index)` | 删除指定索引槽位 |
| 设置槽位透明度 | `void sgl_statusbar_set_right_slot_alpha(sgl_obj_t *obj, uint8_t index, uint8_t alpha)` | 指定槽位透明度 |
| 设置槽位颜色 | `void sgl_statusbar_set_right_slot_color(sgl_obj_t *obj, uint8_t index, sgl_color_t color)` | 指定槽位颜色 |
| 查询槽位索引 | `int16_t sgl_statusbar_get_right_slot_index(sgl_obj_t *obj, char *slot)` | 按文本查找槽位索引 |

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 创建状态栏
    sgl_obj_t* zhuangtai = sgl_statusbar_create(page);
    sgl_obj_set_pos(zhuangtai, 0, 0);
    sgl_obj_set_size(zhuangtai, SGL_SCREEN_WIDTH, 26);
    sgl_statusbar_set_font(zhuangtai, &consolas14);
    sgl_statusbar_set_bg_color(zhuangtai, sgl_rgb(26, 30, 36));
    sgl_statusbar_set_bg_alpha(zhuangtai, 235);
    sgl_statusbar_set_bg_radius(zhuangtai, 0);       // 贴边平直
    sgl_statusbar_set_slot_space(zhuangtai, 8);      // 槽位间距
    sgl_statusbar_set_slot_margin(zhuangtai, 10, 10);

    // 左侧槽位
    sgl_statusbar_add_left_slot(zhuangtai, 0, "SGL UI");
    sgl_statusbar_add_left_slot(zhuangtai, 1, "Status Demo");
    sgl_statusbar_set_left_slot_color(zhuangtai, 0, SGL_COLOR_CYAN);
    sgl_statusbar_set_left_slot_color(zhuangtai, 1, SGL_COLOR_WHITE);
    sgl_statusbar_set_left_slot_alpha(zhuangtai, 1, 220);

    // 右侧槽位
    sgl_statusbar_add_right_slot(zhuangtai, 0, "17:49");
    sgl_statusbar_add_right_slot(zhuangtai, 1, "WiFi");
    sgl_statusbar_set_right_slot_color(zhuangtai, 0, SGL_COLOR_WHITE);
    sgl_statusbar_set_right_slot_color(zhuangtai, 1, SGL_COLOR_CYAN);

    // 查询槽位索引
    int16_t idx = sgl_statusbar_get_right_slot_index(zhuangtai, "WiFi");

    // 电池作为状态栏子控件（右侧）
    sgl_obj_t* battery = sgl_battery_create(zhuangtai);
    sgl_obj_set_pos(battery, SGL_SCREEN_WIDTH - 28 - 8, (26 - 14) / 2);
    sgl_obj_set_size(battery, 28, 14);
    sgl_battery_set_level(battery, 76);
    sgl_battery_set_border_color(battery, SGL_COLOR_WHITE);
    sgl_battery_set_fill_color(battery, SGL_COLOR_LIME);
    sgl_battery_set_bg_color(battery, sgl_rgb(26, 30, 36));
    sgl_battery_set_charging(battery, false);
    sgl_battery_show_percentage(battery, false);
```

![Statusbar 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Statusbar 完整示例 sgl_statusbar_demo.c](SGL_DEMO/sgl_statusbar_demo.c) — 左右槽位全部接口 + 电池子控件

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_STATUSBAR` 开关宏后编译运行。

---

## 6. 总结

- ✅ **槽位机制**：左右独立编号（0 起），添加/设置/删除/查询齐全。
- ✅ **子控件可挂载**：作为容器可放电池等控件。
- ✅ **边距预留**：右侧边距可预留空间给子控件，避免重叠。

---
> 📘 **相关头文件路径**  
> - Statusbar API：`sgl/source/widgets/statusbar/sgl_statusbar.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
