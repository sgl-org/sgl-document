# SGL Switch 控件应用指南

## 1. 简介

`Switch`（开关）是 SGL 中用于**切换两种状态（开/关）**的交互控件。  
在 **SGL（Simple Graphics Library）** 中，`Switch` 支持：
- 开关状态设置与读取（`true`=开，`false`=关）
- 开关主体颜色（开）与背景颜色（关）
- 滑块（knob）颜色、滑块与边框间距
- 圆角、边框、透明度、背景图片

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 点击/触摸可切换状态，切换后可用 `get_status()` 读取
> - 适合设置页开关、功能使能、模式切换等场景

---

## 2. 创建 Switch

使用 `sgl_switch_create()` 创建开关控件：

```c
sgl_obj_t* sgl_switch_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* sw = sgl_switch_create(page);
sgl_obj_set_pos(sw, 20, 20);
sgl_obj_set_size(sw, 60, 30);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_switch_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置开关状态 | `void sgl_switch_set_status(sgl_obj_t *obj, bool status)` | `true`=开，`false`=关 |
| 获取开关状态 | `bool sgl_switch_get_status(sgl_obj_t *obj)` | 返回当前状态 |
| 设置主体颜色 | `void sgl_switch_set_color(sgl_obj_t *obj, sgl_color_t color)` | 开启时主体颜色 |
| 设置背景颜色 | `void sgl_switch_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | 关闭时背景颜色 |
| 设置滑块颜色 | `void sgl_switch_set_knob_color(sgl_obj_t *obj, sgl_color_t color)` | 拖动球颜色 |
| 设置背景图片 | `void sgl_switch_set_pixmap(sgl_obj_t *obj, const sgl_pixmap_t *pixmap)` | 背景图指针 |
| 设置透明度 | `void sgl_switch_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | 0~255 |
| 设置圆角 | `void sgl_switch_set_radius(sgl_obj_t *obj, uint16_t radius)` | 圆角半径 |
| 设置边框颜色 | `void sgl_switch_set_border_color(sgl_obj_t *obj, sgl_color_t color)` | 边框颜色 |
| 设置边框宽度 | `void sgl_switch_set_border_width(sgl_obj_t *obj, int16_t width)` | 边框宽度 |
| 设置滑块间距 | `void sgl_switch_set_knob_margin(sgl_obj_t *obj, int16_t margin)` | 滑块与边框的间距 |

> 💡 控件高度决定整体大小，圆角设为大值可得到圆形开关。

---

## 4. 应用示例

```c
/* 开关事件回调 — 打印当前状态 */
static void on_sw_event(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_PRESSED) {
        sgl_obj_t *SW = sgl_event_get_target(e);
        if (SW) {
            sgl_log("[SWITCH]", "SW=%d", sgl_switch_get_status(SW));
        }
    }
}

    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 绿色开关（开启状态）
    sgl_obj_t* sw = sgl_switch_create(page);
    sgl_obj_set_pos(sw, 20, 20);
    sgl_obj_set_size(sw, 60, 30);
    sgl_switch_set_status(sw, true);                       // 默认开
    sgl_switch_set_color(sw, SGL_COLOR_GREEN);
    sgl_switch_set_bg_color(sw, SGL_COLOR_DARK_GRAY);
    sgl_switch_set_knob_color(sw, SGL_COLOR_WHITE);
    sgl_switch_set_border_color(sw, SGL_COLOR_DARK_GRAY);
    sgl_switch_set_border_width(sw, 1);
    sgl_switch_set_radius(sw, 12);
    sgl_switch_set_alpha(sw, 200);
    sgl_switch_set_knob_margin(sw, 2);
    sgl_obj_set_event_cb(sw, on_sw_event, NULL);

    // 长方形红色开关（关闭状态）
    sw = sgl_switch_create(page);
    sgl_obj_set_pos(sw, 20, 60);
    sgl_obj_set_size(sw, 100, 30);
    sgl_switch_set_status(sw, false);
    sgl_switch_set_color(sw, SGL_COLOR_RED);
    sgl_switch_set_bg_color(sw, SGL_COLOR_DARK_GRAY);
    sgl_switch_set_knob_color(sw, SGL_COLOR_WHITE);
    sgl_switch_set_radius(sw, 15);                         // 半圆开关
```

![Switch 示例](imag/0/SGL_logo.png)

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Switch 完整示例 sgl_switch_demo.c](SGL_DEMO/sgl_switch_demo.c) — 圆形/长方形多种开关 + 状态事件回调

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_SWITCH` 开关宏后编译运行。

---

## 6. 总结

- ✅ **状态读写**：`set_status()` / `get_status()` 管理开/关。
- ✅ **颜色区分状态**：主体色（开）与背景色（关）形成视觉反馈。
- ✅ **事件驱动**：PRESSED 事件中读取状态做业务处理。

---
> 📘 **相关头文件路径**  
> - Switch API：`sgl/source/widgets/switch/sgl_switch.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
