# SGL Arc 控件应用指南

## 1. 简介

`Arc`（弧形）是 SGL 中的**弧形/扇形控件**，用于绘制圆弧、环形弧段（类似仪表盘扫过的角度）。  
在 **SGL（Simple Graphics Library）** 中，`Arc` 支持：
- **起始角 / 结束角**设置（角度制）
- **四种绘制模式**：普通、环形、平滑（抗锯齿）
- 前景色（弧本身）与背景色（弧底槽）
- **触摸交互**：拖动可改变结束角度（角度选择器）
- 通过 `sgl_arc_get_included_angle()` 读取当前夹角（可换算百分比）

> ⚠️ **重要特性**：
> - 是**终端控件（leaf widget）**，**不能包含子控件**
> - 角度以**度数**为单位（0~360），结束角需大于起始角
> - 适合进度环、音量/亮度调节、角度选择器等场景

---

## 2. 创建 Arc

使用 `sgl_arc_create()` 创建弧形控件：

```c
sgl_obj_t* sgl_arc_create(sgl_obj_t* parent);
```

- **`parent`**：父对象指针（如 `Page` 或 `Rect`），不可为 `NULL`

示例：
```c
sgl_obj_t* arc = sgl_arc_create(page);
sgl_obj_set_pos(arc, 50, 50);
sgl_obj_set_size(arc, 120, 120);
```

---

## 3. 属性设置接口

所有设置函数的第一个参数均为 `sgl_obj_t* obj`，即通过 `sgl_arc_create()` 返回的对象指针。

| 功能 | 函数原型 | 参数说明 |
|------|--------|--------|
| 设置起始角 | `void sgl_arc_set_start_angle(sgl_obj_t *obj, int16_t angle)` | 起始角度（度，0~360） |
| 设置结束角 | `void sgl_arc_set_end_angle(sgl_obj_t *obj, int16_t angle)` | 结束角度（度，0~360） |
| 设置弧颜色 | `void sgl_arc_set_color(sgl_obj_t *obj, sgl_color_t color)` | `color`：弧段颜色（如 `SGL_COLOR_CYAN`） |
| 设置背景色 | `void sgl_arc_set_bg_color(sgl_obj_t *obj, sgl_color_t color)` | `color`：底槽（背景弧）颜色 |
| 设置透明度 | `void sgl_arc_set_alpha(sgl_obj_t *obj, uint8_t alpha)` | `alpha`：0~255，255 为完全不透明 |
| 设置模式 | `void sgl_arc_set_mode(sgl_obj_t *obj, uint8_t mode)` | 见下方模式常量 |
| 设置内外半径 | `void sgl_arc_set_radius(sgl_obj_t *obj, int16_t radius_in, int16_t radius_out)` | 环形模式下的内外半径 |
| 获取夹角 | `int16_t sgl_arc_get_included_angle(sgl_obj_t *obj)` | 返回当前起始角到结束角的夹角（度） |

### 模式常量（定义于 `sgl_draw.h`）

| 常量 | 值 | 说明 |
|------|----|----|
| `SGL_ARC_MODE_NORMAL` | 0 | 普通模式（填充扇形） |
| `SGL_ARC_MODE_RING` | 1 | 环形模式（弧带） |
| `SGL_ARC_MODE_NORMAL_SMOOTH` | 2 | 普通 + 平滑（抗锯齿） |
| `SGL_ARC_MODE_RING_SMOOTH` | 3 | 环形 + 平滑（抗锯齿） |

---

## 4. 应用示例

```c
    // 创建主页面
    sgl_obj_t* page = sgl_obj_create(NULL);
    sgl_screen_load(page);

    // 环形模式弧（可触摸调节角度）
    sgl_obj_t* hu1 = sgl_arc_create(page);
    sgl_obj_set_pos(hu1, (SGL_SCREEN_WIDTH - 120) / 2, 50);
    sgl_obj_set_size(hu1, 120, 120);
    sgl_arc_set_start_angle(hu1, 20);
    sgl_arc_set_end_angle(hu1, 290);
    sgl_arc_set_color(hu1, SGL_COLOR_CYAN);
    sgl_arc_set_bg_color(hu1, SGL_COLOR_NAVY);
    sgl_arc_set_alpha(hu1, 200);
    sgl_arc_set_mode(hu1, SGL_ARC_MODE_RING);
    sgl_arc_set_radius(hu1, 30, 50);
    sgl_obj_set_event_cb(hu1, arc_event_cb, NULL);   // 触摸回调

    // 平滑模式弧（动画驱动角度）
    sgl_obj_t* hu2 = sgl_arc_create(page);
    sgl_obj_set_pos(hu2, (SGL_SCREEN_WIDTH - 120) / 2, 220);
    sgl_obj_set_size(hu2, 120, 120);
    sgl_arc_set_start_angle(hu2, 0);
    sgl_arc_set_end_angle(hu2, 0);
    sgl_arc_set_color(hu2, SGL_COLOR_GOLD);
    sgl_arc_set_mode(hu2, SGL_ARC_MODE_NORMAL_SMOOTH);
    sgl_arc_set_radius(hu2, 10, 45);
```

事件回调示例（读取夹角换算百分比）：

```c
static void arc_event_cb(sgl_event_t *e)
{
    if (e->type == SGL_EVENT_PRESSED || e->type == SGL_EVENT_RELEASED ||
        e->type == SGL_EVENT_MOVE_DOWN || e->type == SGL_EVENT_MOVE_UP ||
        e->type == SGL_EVENT_MOVE_LEFT || e->type == SGL_EVENT_MOVE_RIGHT) {
        /* arc 控件本体负责更新 end_angle，这里读取夹角换算百分比 */
        int percent = (int)(sgl_arc_get_included_angle(e->obj) * 100 / 360);
        sgl_label_set_text_fmt_dynamic(g_lab, "%d%%", percent);
    }
}
```

> 💡 用动画驱动角度可实现"扫描/填充"动效：`sgl_anim_set_path(anim, cb, SGL_ANIM_PATH_LINEAR)`，回调中调用 `sgl_arc_set_end_angle(obj, value)`，`sgl_anim_start(anim, SGL_ANIM_REPEAT_LOOP)` 循环播放。

---

## 5. 相关 Demo

可点击跳转到 SGL_DEMO 中的完整示例源码进行对照学习：

- [Arc 完整示例 sgl_arc_demo.c](SGL_DEMO/sgl_arc_demo.c) — 可触摸调节角度的环形弧 + 动画驱动的平滑弧

> 💡 也可在 `sgl_demo.h` 中打开 `SGD_ENABLE_ARC` 开关宏后编译运行。

---

## 6. 总结

- ✅ **角度制驱动**：起始角/结束角单位为度（0~360）。
- ✅ **环形模式配半径**：使用 `SGL_ARC_MODE_RING` 时需设置内外半径。
- ✅ **交互与动效**：支持触摸改角度、动画驱动角度两种用法。

---
> 📘 **相关头文件路径**  
> - Arc API：`sgl/source/widgets/arc/sgl_arc.h`  
> - 模式常量：`sgl/source/draw/sgl_draw.h`  
> - 核心对象：`sgl/source/include/sgl_core.h`
---
